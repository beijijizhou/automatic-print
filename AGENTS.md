# 项目交接规则

## 跨项目交互要求

- 先显示可操作的界面，再执行耗时扫描、读取或计算；不能让用户面对无窗口的等待。
- 所有耗时行为展示当前步骤、进度、处理对象和完成/失败/停止状态；不可在用户不知情时长时间后台工作。
- 耗时计算不得阻塞 GUI 线程。能确定进度则展示真实计数，不能确定时展示明确的进行中状态，不伪造百分比。
- 此为用户跨项目要求，后续项目也应遵循；本文件记录不表示其他项目文件已自动修改。

开始本项目任务前，完整阅读 [用户硬性需求](docs/hard-requirements.md)。
该文档用于跨对话保留生产约束，最新用户指示优先。

- 当前优先本地排版；暂时隐藏 ERP 工作流。
- 代码文件尽量约 200 行，不能超过 250 行；按功能目录模块化。
- 同订单多件和双面图片不可拆散，省材料不能优先于生产安全。
- 修改后验证整批刀位及真实像素通道，不能只用两个样本推断整批安全。
- 先检查 Git 状态，保留现有未提交和未跟踪修改，不擅自清理或重置。
- 用户确认方向后直接实施，不反复只复述方案。
- 未发布代码与已发布版本明确区分，不能把测试通过当成生产验收。
- 用户要求修改验证后持续提交并推送源码到 GitHub，供测试电脑拉取；源码推送与 Release 安装包发布分别说明。
