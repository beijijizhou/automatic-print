# Automatic Print Project Guide

每个新对话开始时，在检查代码、设计方案或修改文件之前，先运行
`git ls-files '*.md'`，完整阅读 Git 跟踪的全部项目 Markdown。不要读取依赖、
缓存、生成目录或未跟踪的第三方说明。一次对话内无需在每个请求前重复全文读取。

默认使用精简模式以节约用户 token：只读取完成当前请求必需的文件和调用链，只做最小完整修改，
运行最窄的针对性检查，过程说明和最终报告保持简短。不得为未来假设扩展范围、顺手重构无关代码、
主动运行全量测试或重复输出已知信息。

大范围修改必须在开始前取得用户对本次范围的明确肯定，包括但不限于：跨多个业务模块重构、改变
核心架构或数据协议、批量迁移、全量测试、真实多平台批次回归、批量机器更新及生产部署。用户只确认
方向、讨论方案或说“继续”不自动授权扩大范围；未获确认时停留在分析和最小安全修复。

## 开始修改前

- 先检查 Git 状态，保留用户已有修改，不清理或覆盖无关文件。
- 先读 `docs/FUNCTION_CATALOG.md`，再按业务字段、中文界面文字、数据结构、测试和
  调用方搜索 `layout_engine/`、`ui/`、`history/`、`automation/`，不能只搜索准备
  创建的新函数名。
- 复用顺序固定为：组合已有能力；扩展已有接口；抽取共享核心并迁移所有活跃重复；
  确认语义全新后才创建新实现。发现活跃重复时，不得再增加第三套实现。
- 阅读真实活动调用链。仓库存在旧自动化和兼容入口，文件名相似不代表仍被主界面使用。

## 代码和目录

- Python 文件通常保持 100–200 行。200 行不是机械拆分线；完整的静态映射或单一算法
  可以合理超出，但普通控制器和新业务组件不应继续膨胀。
- 现有超过 200 行的遗留文件由测试冻结上限：修改涉及其职责时应优先抽取，不得继续增长。
- 页面和窗口只负责编排数据、状态、信号和视图。排版业务属于 `layout_engine/`，界面属于
  `ui/`，持久化属于 `history/` 或明确的存储模块，外部平台属于 `automation/api/<provider>/`。
- 同类模块放在同一功能目录，目录保持少量职责清晰的核心文件。不要制造无业务含义的转发层。
- 外部平台按提供商归档认证、请求、分页、响应解析和平台规则；页面只调用公共门面。
- 更新 `docs/CURRENT_STATE.md` 记录活动模块归属，更新 `docs/FUNCTION_CATALOG.md` 记录共享能力。

## UI、线程和生产安全

- 先显示可操作界面，再执行耗时扫描、读取、测量、排版或保存；耗时工作不得阻塞 GUI 线程。
- 有可靠总数时显示真实计数或百分比；未知总量显示进行中状态，不伪造进度。始终显示当前步骤、
  对象、完成/失败/停止状态。
- 父级选项变化必须在第一次交互中更新全部下游选项并清除过期状态、预览和详情。
- 同订单多件和双面不可拆散，省膜不能优先于生产安全。修改排版或渲染后按风险核验整批刀位、
  真实像素通道、原图尺寸和订单连续性，不能只用两个样本推断。
- 可恢复冲突采用有依据的降级继续处理，并显示原值、采用值、影响及修改入口；不可恢复或生产
  安全冲突保留可恢复状态并给出完整可复制诊断，不猜值绕过。
- 这是跨项目最高级交互规则：程序和代理都无权替用户暂停、取消或结束任务。存在可靠降级时继续
  运行并完整说明发生了什么；不存在安全降级或继续会产生不可恢复影响时，保持程序、任务上下文和
  已完成结果可恢复，展示“继续 / 修改 / 放弃”等明确选择，由用户决定下一步。不得用抛出异常、
  退出进程或静默丢弃状态代替用户决策；单个记录失败时继续处理其他互不依赖的安全记录。

## 验证与推送门禁

- 窄逻辑修改：语法检查和针对性测试。
- 共享业务逻辑：针对性测试加相关集成测试。
- UI 交互修改：自动测试后做一次真实界面检查。
- 排版、渲染、保存或生产安全修改：使用完整代表批次验证输出事实。用户要求“完整测试”时，
  在本地执行完整自动测试套件，从本次受影响或当前选择的生产平台可下载批次中随机抽取3个已生成
  批次并下载到隔离测试目录，同时从`\\192.168.11.28\dtf`读取不同文件结构的真实批次做跨来源回归；
  生产平台不足3个可用批次时测试现有全部批次并明确报告缺口。真实回归只下载既有生产图并执行本地
  预览、排版、渲染和像素验证，不生成真实平台批次、不导入RIIN、不创建或发送PRN，也不写入生产输出目录。
  样本至少覆盖本次受影响平台和其他文件结构不同的平台，逐批核验标签/二维码区域、左侧识别刀码、右侧分割刀码、
  纵向刀位、旋转后的标签与刀码方向、订单/双面连续性及实际输出像素。只运行 pytest 或只测试
  单一平台不得称为完整测试；缺少某平台样本时继续完成可用样本并明确列出未覆盖项。
- 默认只跑本次调用链的针对性测试及必要集成检查；专用自托管 Runner 禁止使用。
  未经用户针对本次任务明确同意，不得在本地运行完整测试，也不得创建会自动触发完整测试的
  PR、推送或其他 CI 任务；合并到 main 和准备发布本身不构成完整测试授权。
  用户明确要求完整测试时，才在本地运行完整测试与真实批次回归。
- 测试分支推送以针对性测试通过为门禁；已运行的针对性测试失败时禁止推送，先修复并重新验证。
  完整测试未运行、失败或缺少真实样本时均不得报告完整测试通过。合并或发布前必须核对该提交所需
  的检查通过，不能把源码推送成功当作验收完成。
- 向用户直接说明实际运行的测试命令及最终通过/失败结果。源码推送、Release 安装包和生产验收
  分别说明；未运行完整测试时必须明确标注。

## 文档职责

- `docs/BUSINESS_RULES.md`：当前有效生产和产品规则。
- `docs/PROJECT_RULES.md`：稳定工程、架构、UI、验证和发布规则。
- `docs/CURRENT_STATE.md`：当前活动入口、模块归属和已知结构债务。
- `docs/FUNCTION_CATALOG.md`：共享能力及唯一所有者，是新增代码前的复用检查入口。
- 文档记录当前事实，不按版本追加活动日志，不复制聊天历史。旧决策由 Git 历史追溯。
