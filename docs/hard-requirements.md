# Automatic Print：用户硬性需求与新对话交接

> 最新要求：批次结束暂时不自动画线。第114节覆盖此前默认保留结束红线的规则。

> 膜标签与下方图案之间的空白默认补足至40毫米，可调整，不缩放图案；见第115节。

> 最新图片上下垂直间距为8毫米，覆盖旧5毫米；见第116节。

更新时间：2026-09-14。本文整理用户明确提出的需求，不代表全部功能已通过生产验收。
发生冲突时，以用户最新明确要求为准；不要擅自恢复已废弃的规则。

## 1. 当前目标与协作方式

- 用户经营 POD（按需印刷）公司，排版错误会造成实际生产损失。
- 当前第一优先级是本地图片文件夹排版，不是 ERP 接单自动化。
- 平台接单、生产批次、本地批次管理等暂时隐藏，不删除底层功能。
- 主界面不显示“Haloo：本地有 0 个本地批次……”等平台或缓存提示。
- 用户确认实现方向后，应直接修改代码、测试和验证，不要反复只复述方案。
- 不要未经验证就说“已经解决”；单元测试通过不等于生产文件已经安全。
- 保留用户已有修改，不擅自覆盖参数、删文件、重置代码或触发不可撤销的生产动作。
- 跨项目要求：先展示界面，再执行耗时读取/计算；所有耗时行为都需让用户看到当前步骤、真实进度及完成/失败/停止状态，不允许无窗口等待或无说明的后台长任务。

## 2. 代码规范

- 一个代码文件尽量约 200 行，硬上限 250 行。
- 类似功能放在同一个功能目录，代码可复用、模块化、可维护。
- 不把 UI、平台接口、排版算法、文件处理和线程生命周期混在一个大文件中。
- UI 使用中文，界面说明文字可复制。
- 导航和设置遵循“父分类 → 子分类 → 具体记录”的层级结构。
- 父项变化时一次交互就更新合法子项，清除过期选择，不要求用户选择两遍。

## 3. 界面和参数持久化

- 主界面围绕本地排版：文件夹入口、常用标签文字、机器号、图片手动旋转、即时预览。
- 不常用配置放在打印参数中，参数入口位于主界面底部。
- 色块设置及完整的标签/文字设置属于打印参数，可以单独打开设置界面。
- 标签文字要能在主界面经常编辑；预览直接可见，不需要再点击“预览”。
- 文件夹选择默认从上次打开的位置开始。
- 启动软件不自动读取或分析上次批次，即使后台也不能自动执行。仅保留路径；用户选择文件夹、点击读取/预览/生成后才加载当前批次。未加载时改参数不能触发旧批次读取。
- 用户修改的参数自动持久化，退出时补保存，下次打开恢复。
- 升级不能强制把用户已选字号、位置、膜规格等改回新的默认值。
- 主界面显示版本和检查更新入口。
- 界面版本按固定发版日期与当日更新次数显示，日期不能取软件启动当天。同日发布次数递增，换日从 01 开始；内部更新比较继续使用数字版本号，不在主界面重复堆叠显示。
- 保存排版文件后自动打开保存文件夹，不让用户手动寻找。
- 桌面需要打开入口；图标使用“HA”，代表 Haloo Automatic。

## 4. 输入图片、尺寸与文件名

- 读取图片内容前，先发现并显示图片文件名，便于查看尺码、确认正在处理什么。
- 支持文件名搜索、复制及按平台规则批量改名。
- 显示图片实际打印尺寸（毫米），可靠尺寸来自图片内嵌 DPI。
- 缺少可靠 DPI 时必须提示；估算尺寸不能当作可靠切膜生产尺寸。
- 保持源图片透明度，标签不加白色底框，不能无故输出白底。
- 右侧未使用的空白不能仅为凑宽度而消耗膜；但固定分区输出不能裁掉刀位基准。

### 4.1 多件文件名整理

- 多项多件、单项多件排版前，需要按平台文件名规则去掉生产前缀。
- 删除第一个 `-` 之前的全部字符，包含该 `-`。
- 示例：`CVC面料00004-BJRCCCQ-1-...` → `BJRCCCQ-1-...`。
- 隆丰示例：`A0000007-订单编码-...`，删除 `A0000007-`。
- 不只支持 CVC 或隆丰；以后同类平台也使用这个逻辑。
- 必须避免错误识别：已经去过前缀的文件不能再次删除订单编码。
- 同订单、同颜色的图片，尺码从小到大排序，不能按普通字母顺序排序尺码。

## 5. 生产安全：订单和双面不能散

- 同一多件订单必须保持成组、相邻，不能散到不连续的生产位置。
- 蜂鸟编码 `...-NO1-1` 与 `...-NO1-2` 表示同件商品的正反面。
- 双面两张图必须相邻：常规区可以横向并排或纵向紧邻。
- 转移到旋转区时按完整订单组转移，不能只拿其中一张正面、背面或某件商品。
- 合并几个批次排版时也必须保留订单、双面和尺码排序约束。
- 无法可靠识别订单归属时，不得乐观地把可能属于同订单的图片拆散。

## 6. 膜规格、间距和材料利用率

- 生产膜规格有 45cm 和 60cm。
- 当前默认是 60cm（600mm）、固定双列，不再默认 45cm。
- 图片之间的默认质检间距为 5mm（最新要求，覆盖旧的 8mm）。
- 旧的 580mm 默认已经被后续膜规格要求取代。
- 不单独设置左右图片边距来重复 RIIN 自带的边距功能。
- 上下留白的 UI 名字要明确，例如“批次开头与结尾留白”，不能笼统叫上下边距。
- 在物理安全约束和订单组约束下节省材料，不能为省膜破坏生产顺序。
- 显示智能排版/旋转相比基准排版缩短多少米；必须比较实际完整批次长度。

## 7. 固定刀位、分区和色块

- 色块默认正红色 `#ff0000`，正方形 10mm × 10mm。
- 色块尺寸、颜色、间距、位置应可设置并实时预览，参数放在打印参数中。
- 色块在“分区所能识别的固定左边缘”，不是跟着每张图片左侧移动。
- 左分区色块固定在膜左边缘识别区；右分区也需要色块，固定在第二刀对应的左边缘基准。
- 左右图片不得跨越固定纵向刀位或安全通道，标签及色块也不能侵入通道。
- 常规区域的刀位整区保持同一条直线，不要求在膜宽的一半。
- 自动刀位要按整个区域/批次的全部图片计算，不得每两张图片重新换一次。
- 用户可关闭自动刀位，手动调整并查看真实预览。
- 输出中必须保留完整分区坐标基准；不能裁边、居中或挪动图片导致刀位失效。
- 排版前检查所有图片和标记坐标；合成后再检查安全通道实际像素透明。
- 检查失败禁止输出可打印结果，不允许为显示预览而绕过生产检查。

## 8. 旋转区域（最新确认的规则）

- 图片可手动左旋、右旋、还原；默认旋转方向为向左。
- 原“切膜模式一律不允许旋转”已被以下分区规则取代，不能简单恢复全局禁止。
- 常规区和旋转区使用各自独立的固定刀位。
- 从常规区进入旋转区时允许调整一次刀位；同一个区域中不能持续移动刀位。
- 旋转区同一水平行只能放一张图片，不允许两张并排。
- 多件订单和双面图片必须作为完整订单组进入旋转区，并保持组内相邻。
- 加上区域边界和间距之后，仍比不分旋转区更省材料，才自动启用旋转区。
- 若不能减少整批用膜，保持原常规排版，不为旋转而旋转。
- 预览明确显示区域名称、各区刀位、贯穿安全通道和换刀位置。
- 批次记录保存每张图所在区域及其实际刀位，不能只保存一个模糊的全局刀位。

## 9. 标签和文字

- 用户标签示例：`CY 1001Mt______26`，多余的连续下划线要移除。
- 机器号为大写 `M1` 至 `M11`，支持选择并保存，也可加入标签文字。
- 默认中文六号字，即 7.5 磅，不是 6mm 或 6 磅。
- 支持修改文字内容、字号、位置、间距、微调及添加日期。
- 用户强调文字不能大到影响剪膜，也不能小到肉眼看不清。
- 标签应与对应色块组成标记组，不能一个跟二维码走、另一个留在其他位置。
- 原要求：识别生产图中膜标签的实际区域，整段文字（含换行）与它等高，并限制宽度。
- 不能把二维码中心或二维码高度直接冒充整条膜标签高度；不能用默认 10mm 猜测。
- 自动适配要测量真实字形和整段文字，必要时换行，不能拉成长条增加排版长度。
- 识别失败应明确提示，不悄悄猜位置、猜字号或挡住二维码、图案。
- 后续明确的切膜要求：标记组在分区左边缘，文字贴近色块，默认六号字。
- 两者的优先级：切膜分区与标记组安全位置优先；自动膜标签适配不能把标签移出标记组。
- 若等高、限宽、可读字号不能同时满足，应提示冲突，不可悄悄缩到看不见。
- 生产图片旋转时要重新适配参考区域；文字保持正向，色块仍在分区固定左边缘。

## 10. 预览、生成、停止及输出

- 默认显示整批总览，可滚动查看全部图片，而不只是两个样本。
- 保留双图细节视图，便于检查左右图片、标签、色块及刀位。
- 预览使用实际排版数据，与输出位置、旋转和刀位一致，不画虚假的安全示意图。
- 参数变化自动刷新；连续联动的中间状态应合并刷新。
- 参数不安全时仍显示图片，标红越界区域，并明确“仅供检查，禁止输出”。
- 读取/刷新失败不能无说明变成白屏，错误信息必须可见。
- 生成新批次时清除旧批次预览、缩略图、错误和旧数据，显示加载状态。
- 新排版数据到达后立即替换；不能把旧批次图片和新批次进度混在一起。
- 生成过程中能看实际排版预览；保存大图期间保留本次预览并显示持续写入状态。
- 必须有停止功能，保存阶段安全停止，不能破坏已经写完的文件。
- 输出文件名取标签文字，例如 `CY 1001Mt26.png`，不能固定叫 `print.png`。
- 日期/机器号等模板变量先展开，非法文件名字符安全处理。
- 重名不能覆盖旧图；新生成批次不能覆盖旧批次。
- 保存进度、完成提示和批次记录必须使用真实输出文件名。

## 11. 大文件、并发和 Qt 线程

- 允许图片处理并行，但并发受内存限制，不能一次加载整批原图。
- 测试尽量用少量文件，避免因大量图片让测试过慢。
- 预览使用缩略图和有界缓存，整批总览按需读取可见图片。
- 大 ZIP 和原始图片不上传，ZIP 同样太大。
- 下载并行，使用浏览器原生流式传输，不能把多个 ZIP 全部放进内存。
- Qt 控件、QTextDocument、预览绘制只在 GUI 线程操作。
- 不跨线程 setParent、installEventFilter，不在工作线程直接更新 UI。
- QPainter 显式结束，工作对象/线程按正确生命周期清理，不能销毁仍在运行的 QThread。

## 12. ERP 功能（保留需求，当前隐藏）

- ERP 连接曾使用 Haloo（蜂鸟）和隆丰等客户端名称；最新澄清蜂鸟是 ERP，不是实际生产平台。生产标记的平台名称不能照搬 ERP 客户端列表，其他平台不能凭空猜名单。
- 订单进入 ERP → 已接单 → 按物流分拣 → 按单多件规则生成生产批次 → 等待图片生成 → 下载。
- 批次生成不可撤销，只有一次机会；必须展示规则、数量及异常项，再确认生成。
- 物流第一优先级，然后才是单项单件、单项多件、多项多件。
- Haloo 物流分类包括 UPS、FedEx、Swift、USPS、Gofo、Yanwen、SpeedX。
- `spee` 属于 SpeedX，不能当作无法分类物流排除。
- CBT / 非 CBT 是隆丰物流规则，不是 Haloo 规则。
- 单件中的双面要单独识别并展示数量。
- 无法按规则生成的异常订单单独提出，不应阻止其他正常订单生成。
- 生成弹窗支持“按有面单生成批次”。
- 平台页名称为“已接单”和“生产批次”；已清单页已要求移除。
- 已接单显示尚未进入生产的订单数量，未进入生产的数量标红。
- 生产批次不应读取已接单 API；平台和批次数据缓存，真正需要时才刷新。
- 登录成功自动进入该平台子域名的 `factory/fnsz-sale/produceManage/produceItemsManage`。
- 复用 after-sales 的 Playwright 相关实现，登录后优先 API，避免慢速逐步网页操作。
- 支持全选、批次号范围下载（示例 `607250635009`–`607250652014`）及并行下载。
- 本地已经有文件时不重复下载，本地排版不依赖重新访问生产接口。
- 支持几个批次的图片合并排版。

## 13. 测试电脑与发布

- GitHub 项目：<https://github.com/beijijizhou/automatic-print>。
- README 显示可复制的一键 Windows 安装/更新指令，自动配置运行环境。
- 新电脑不能要求用户已经有 Python、虚拟环境或 Playwright；下载浏览器依赖也需处理。
- 区分 CMD 与 PowerShell，不把 Markdown 链接括号复制进命令，不拼接两条命令。
- 已有 Windows 引导脚本路径：`windows/bootstrap-test-computer.ps1`。
- 版本显示和实际发布必须一致，未发布的本地代码不能说测试电脑已经更新。
- 用户要求持续迭代：后续代码修改验证后提交并推送到 GitHub，供测试电脑拉取；不能默认只保留在开发电脑。推送源码和生成 Release 安装包是不同状态，需分别说明。

## 14. 当前交接状态：不要丢失未提交工作

- 最近代码修改在当前工作目录，尚未提交或发布；版本字段仍为 `0.1.61`。
- 最新一次完整测试：124 项通过，退出码 0；并非对全部生产输出的验收结论。
- 很多新增模块和测试仍是 Git 未跟踪文件，禁止清理、重置或忽略它们。
- 新对话开始先检查 `git status`，读取相关功能模块，再继续修改。
- 已检查旧批次：桌面 `JOB_20260913_001541`，89 张图，旧文件名 `print.png`。
- 该旧文件曾直接扫描约 231.1mm 刀位，通道透明；但用户提供截图并明确报告不符合切膜要求。
- 这个用户报告不能仅凭旧坐标记录关闭，仍需以新输出的整批预览和实物可切性验收。
- 新旋转分区对这 89 张图的计算：11 张进入旋转区，预估从 24.176m 降到 21.190m，少约 2.986m。
- 上述是排版计算结果，尚未替用户重新生成该批次的大图，也尚未在切膜机验收。
- 用户下一步会重启软件并重新生成，重点核验：订单成组、区域边界、刀位、全批预览刷新及真实像素安全。

## 15. 2026-09-13 开发预览与双面对齐补充

- 用户要求无需生成最终文件即可预览整批，便于快速开发和核验排版。
- 本地工作台和打印参数增加“仅预览整批（不生成文件）”：运行真实排版，跳过最终画布合成、PNG 保存和 manifest 写入。
- 常规区双面能够安全并排时采用并排，并以图片顶部对齐，不再以色块的相对高度代替图片水平对齐。
- 旋转候选以完整订单迁移对整批长度的影响评估，接受一次迁移后重新比较剩余订单；这是逐步优化，不声称全局最优。
- 旋转区仍每行一张，双面纵向紧邻；不因扩大旋转区而拆订单。
- 本次本地测试 126 项通过，含测试生成文件的像素通道检查；尚未发布。
- 当前电脑未找到交接记录中的桌面 JOB_20260913_001541，未完成该 89 张实图批次的重新验证，不能声称该批已通过生产验收。

## 16. 2026-09-13 双面漏识别与旋转组合修正

### 用户反馈

- 用户截图指出双面仍没有在一起；错位摆放的情况下应考虑全部旋转，原旋转候选逻辑有问题。
- 本节后续实图检查更新第 15 节中“未找到原批次”的状态，不代表原生产输出已经验收。

### 本地实现与核验

- 根因：双面识别包含 `CVC面料00003-` / `CVC面料00004-` 等每张图不同的生产前缀，导致同件商品被识别为两件；普通文件名相邻也不能作为双图细节的选择依据。
- 订单归组和双面识别统一按去生产前缀的身份；只在内存中整理身份和顺序，不重命名用户源文件。保持原始标签编号，正常订单按产品/颜色、尺码及件号整理；未知身份保守归组。
- 双图细节显示选中商品的实际正反面，保留真实排版坐标。
- 对全部候选固定刀位，动态规划联合比较常规区和旋转区的完整订单分配，包括整批旋转；计入区域留白和边界间距。替代第 15 节的逐个迁移算法。
- 正常排版和分区排版都保持完整订单连续；生成前独立检查源图是否丢失/重复、订单是否被插入或拆区、双面是否相邻及水平对齐。
- 已找到并读取实际源文件夹 `/Users/hongzhonghu/Downloads/609120504008`：89 张、12 个订单、12 组双面。当前保存参数下，常规区 55 张（7 组双面全部水平并排），旋转区 34 张（5 组双面纵向紧邻）；没有常规区双面纵向错位，没有订单拆区。
- 实图计算长度约 19.477 米，相比修正归组后的常规基准约 24.221 米，缩短约 4.744 米。此结果使用当前保存参数，不能与旧错误分组结果混为同一基准。
- 对 300 DPI、7087 × 230038 像素的实际 libvips 合成数据扫描两个区域的全长安全通道，alpha 最大值均为 0。未保存最终打印大图，未触发生产动作；这不是切膜机实物验收。
- 134 项测试通过；新增覆盖生产前缀、交错文件次序、尺码排序、双面细节、整批旋转组合、小批穷举对照及 12 张完整测试批次的像素通道。
- 核验快照与逐图坐标存放在本地 `output/pair-layout-review/`。本次代码仍未提交、未发布。

## 17. 2026-09-13 排版前批次分析、尺码聚集与特殊比例

### 用户新增要求

- 排版前先分析批次是单件还是多件、双面数量和商品尺码，再判断哪些完整订单适合进入旋转区。
- 2XL / 3XL / 4XL / 5XL 通常图片较大，不能强行并排。尺码用于分类与重点提示，实际可否并排必须按图片、标记组和安全通道的真实占位判断。
- 单件单面尽量按尺码集中。竖向细长图应比较旋转方案；特别小的胸口图可考虑与其他图并排。上述优化不能拆开多件订单或双面商品。

### 本地实现与核验

- 分析面板按“批次 → 单件单面 / 单件双面 / 多件订单 / 待核对归属 → 尺码或订单 → 商品 → 图片”展开；内容可复制，双击图片同步选中预览和手动旋转对象。
- 先按文件名建立商品和单双面统计，再测量尺寸、展示整单旋转候选，最后显示实际分区、搭配订单及判断依据。件数和图片数分开统计，XXL 与 2XL 等别名统一。
- 可靠的单件单面按尺码聚集，优先同尺码安全搭配；小幅图可跨尺码搭配。仅有反面或归属无法识别的图片保守归组，不纳入单件单面调序。
- “竖向细长”当前使用高宽比至少 2 的候选提示；“小幅图”当前使用原图不超过 150 × 200 毫米的提示。配对候选另外计入标记组占位。这些是开发中的几何启发式，不是用户指定的硬阈值，也不宣称已识别图案在衣服上的实际胸口位置。
- 每个固定刀位下，对单件单面生成尺码优先的搭配次序，再比较整单常规/旋转分配。固定次序的分区分配使用动态规划；不声称对所有可能重排全局最优。
- 旋转后整单任一图片完整占位超过膜宽及安全区，即不允许整单进入旋转区；不能只迁移其中可旋转的图片。
- 真实批次 609120504008 的分析：12 个多件订单，77 件 / 89 张图，12 件双面，没有单件单面订单。尺码为 S 7 件、M 22 件、L 11 件、XL 20 件、2XL 8 件、3XL 3 件、4XL 6 件。
- 该批仍保持 55 张常规区、34 张旋转区，约 19.477 米。两个区域在真实 300 DPI 合成数据上的全长通道 alpha 最大值均为 0；未保存最终打印文件。
- 143 项测试通过，覆盖分类统计、尺码别名、尺码聚集、小图搭配、大尺码实际宽度、整单旋转限制、分析面板层级和完整测试批次像素安全。
- 本地核验文件在 `output/batch-analysis-review/`。未提交、未发布，未进行切膜机实物验收。

## 18. 2026-09-13 刀位提示改为二维码高度内的红色点线

### 用户新增要求

- 切割线用红色点线展示，仅位于顶部二维码的高度范围，不能贯穿下方图案区域影响烫印；用户提供了顶部二维码标签带截图作为范围参考。
- 已向用户澄清是否也要印入最终文件，目前未收到该问题的答复。本次先落实预览展示，不把点线加入最终 PNG；不能把该实施范围记成用户已经确认的打印规则。

### 本地实现与核验

- 移除预览中贯穿整幅图案的红色虚线和绿色通道底带；真实刀位与整批安全检查继续保留。
- 在真实刀位 x 坐标画红色圆点，y 范围来自实际检测到的二维码边界。这里测量二维码高度只用于用户指定的刀位提示，不把它冒充整段膜标签高度。
- 两图同排取各自二维码高度带的交集，不能取更长的并集；旋转图片使用变换后的真实二维码位置。
- 无法识别或两侧没有共同高度带时不猜范围。二维码检测失败会显示提示，且不回退到贯穿图案的辅助线。
- 区域刀位显示在预览说明中，换刀文字位于两区之间的空白间隔，不覆盖图案。
- 后台按可见图片逐张识别，缓存仅保留有界坐标结果；切换批次时丢弃旧批次的待处理任务和返回结果。
- 实际 89 张源图均识别到二维码，得到 65 个行级点线范围。常规/旋转区在真实 300 DPI 合成数据上的全长安全通道 alpha 最大值均为 0；未保存最终打印大图。
- 149 项测试通过，包含双图高度交集、缺失识别、旋转坐标以及点线不延伸到图案高度的实际预览像素检查。
- 核验图在 `output/cut-guide-review/`。本次仍为未提交、未发布的本地修改。

## 19. 2026-09-13 用户确认辅助线写入输出图

- 用户明确要求现有辅助线显示在输出图上，方便员工识别；此要求覆盖第 18 节当时仅预览的实施范围。
- 最终 PNG 写入二维码高度范围内的红色刀位圆点，不改成贯穿图案的整条线。两图同行继续取高度交集，旋转区继续使用该区真实刀位及旋转后的二维码范围。
- 预览与输出共用点线坐标及物理尺寸。目前开发默认圆点直径 0.6 毫米、中心间距 1.4 毫米，不是用户指定的数值。
- 合成原图先核验全长透明切割通道，再添加点线；大图输出保存后用精确圆点透明度掩码复核，不能放行整个二维码高度带或任意红色内容。
- 二维码识别失败时不猜测、不画贯穿线，输出结果的 `printed_guides.missing_qr` 记录缺失文件；换刀说明仍为预览提示，本次未增加输出文字。
- 153 项测试通过，新增两种引擎实际保存 PNG 的点线位置核验、精确掩码与额外通道内容拦截、识别失败无回退测试。尚未重新生成用户的整批生产大图，不能当作生产验收。
- 此修改尚未提交、尚未发布；旧 PNG 不会自动改变，需重新生成。

## 20. 2026-09-13 源码测试版 0.1.62

- 用户要求持续推送代码，另一台测试电脑通过已有引导脚本拉取 `main`，不能等到安装包发布才看到结果。
- 本次将第 14 至 19 节累积的本地排版、订单/双面归组、旋转分区、整批预览、批次分析、参数保存和输出辅助线修改一起提交，源码版本改为 `0.1.62`。
- 完整测试 153 项通过，包含代码文件不超过 250 行检查；未完成切膜机实物验收。
- 此版本仅安排源码推送，没有生成新的 Release 安装包；“检查更新”按钮仍检查 Release，不能用它判定 `main` 是否包含更新。

## 21. 2026-09-13 源码版 0.1.64：主界面预览和总结恢复

- 用户反馈生成时预览、用膜总结及批次信息消失。检查发现生成会隐藏参数窗口，但完成总结/进度控件仍在参数窗口；整批预览位于高大的分析树下面，首屏不可见。
- 主界面新增本次批次总结，持续显示批次文件夹、订单/件数/图片数、双面数量、长度、常规基准、节省米数及旋转数量；计算阶段和实际保存完成状态分开说明。
- 整批真实预览放在批次分析树之前，使用独立的有界滚动区域，不再让整批长图撑开主页面；分类与尺码/订单树仍在预览下方，向下滚动可查看。
- 保存期间及生成/仅预览完成后保留本批次预览和总结，新任务清除旧结果。失败或停止在主界面显示明确状态，不能把未完成结果当作可打印文件。
- 已检查离屏实际窗口截图，并增加首屏总结可见性、长图独立滚动、保存期间保留、完成及新任务切换的界面回归测试。此版本不修改排版算法，仍需测试电脑和实际生产验收。

## 22. 2026-09-13 源码版 0.1.65：先显示界面，再后台预览

- 用户反馈测试电脑长时间不显示窗口，随后才打开；明确要求所有耗时行为可见，这是跨项目要求。
- 窗口构造不再扫描上次文件夹、解码样图或同步运行整批排版；先显示启动组件加载提示和主窗口，预览随后通过后台纯数据任务扫描、分析尺寸与计算。
- 主界面持续展示扫描、文件名/尺寸读取、刀位比较、预览完成或失败状态。文件名先回到手动图片选择列表，不等整批计算结束。
- 连续参数变化合并，后台同时只执行一个预览计算，旧请求协作取消；切换批次、开始生成或关闭窗口后丢弃旧结果。
- 提供“停止后台预览计算”。选择已有批次的其他图片或切换总览/细节复用当前排版，不重复计算整批。
- 工作线程只处理路径、尺寸和排版数据，实际控件/预览安装通过 GUI 线程接收。生产输出的独立订单与刀位检查未移除，不安全的人工旋转配置仅能显示检查预览，生成仍拦截。
- 新增故意阻塞后台扫描时主窗口可见、GUI 定时器仍响应、后台线程身份、旧结果隔离、无重复计算及停止待执行请求测试。需在测试电脑复核实际启动体验，未生成 Release 安装包。

## 23. 2026-09-13 源码版 0.1.66：启动不读取旧批次

- 最新要求覆盖第 22 节的自动后台启动预览：启动只恢复参数和文件夹路径，不扫描、解码或排版上次批次。
- 主界面显示“软件已就绪，未读取上次批次”，提供“读取当前文件夹（使用上次路径）”按钮。选择文件夹、点击读取或显式预览/生成后才开始处理。
- 未加载批次时，改参数及切换总览/细节不触发读取；已有批次加载后保留参数联动刷新。
- 重选同一路径也能显式开始读取，文件夹选择不再同步扫描所有文件来计算图片数。
- 回归测试覆盖窗口显示后等待仍零扫描、改参数仍零扫描、路径仍保留以及点击读取后后台扫描与新旧批次隔离。

## 24. 2026-09-13 源码版 0.1.67：界面直接更新代码

- 源码安装的“检查更新”检查官方仓库主分支，而不是 Release 安装包；显示版本、发版日期及提交数，用户确认后快进更新代码、同步当前虚拟环境依赖并安全重启。
- 检查、拉取、依赖同步及重启阶段在主界面持续可见并显示耗时；操作在后台线程执行。启动静默检查只提示，不自动应用更新。
- 排版、保存、文件读取或后台预览仍运行时不应用更新；更新应用时禁用生产操作和参数修改，线程清理完成后才重启。
- 非官方来源、非主分支、本地修改或无法快进的代码拒绝更新，不自动清理、重置或覆盖。失败保留重载保护标记，允许重新检查并修复依赖。
- 开发监视器在更新及依赖同步期间不自动重载，成功后才响应重启请求。重启失败显示手动启动提示。
- 旧测试电脑需先用已有引导指令获得此版本一次，后续通过界面更新即可；本次不生成 Release 安装包，Windows 实机更新仍需复核。

## 25. 2026-09-13 源码版 0.1.68：直接排版与主界面合成进度

- 点击“开始排版”直接使用当前参数生成，不先打开参数窗口或强制仅预览；选文件夹和开始排版分开，取消选择不能触发旧文件夹生成。
- 打印参数仍作为独立、主动打开的设置入口，不能成为开始排版的前置步骤。
- 处理进度、当前文件、停止按钮及日志放在主界面。整批真实缩略预览按引擎逐张处理信号显示待处理淡化和已处理绿色边框；不是回读最终大图像素，尤其 libvips 为延迟执行，实际写入进度单独显示。
- 保存期间保留整批预览；新任务清除旧结果。生产订单归组及整批刀位、实际像素通道检查不变。

## 26. 2026-09-13 源码版 0.1.69：输出默认跟随输入目录

- 默认保存到图片文件夹所在的同级目录，例如输入 `D:\订单\批次123`，输出为 `D:\订单\JOB_时间\标签名.png`；不再默认桌面。
- 切换输入文件夹时自动更新默认保存位置，启动仅恢复路径，不读取图片。
- 打印参数可关闭同级默认，改用自定义保存位置；选择模式和自定义路径持久化，切回默认不能丢失自定义路径。
- 保留每次独立任务目录、标签文件名及不覆盖旧图的安全规则。

## 27. 2026-09-13 源码版 0.1.70：保守上线快速模式

- 用户确认先上线保守快速模式，默认关闭自动旋转区比较及自动旋转候选；覆盖旧的默认启用旋转区要求。参数中可关闭快速模式后主动启用高级旋转区功能。
- 保留整批统一刀位自动计算、可靠 DPI 校验、完整订单与双面检查、输出实际像素安全通道及红色点线检查，不以速度换生产安全。
- 快速模式选文件夹只记录路径、清除旧结果，不扫描或计算后台预览。点击开始排版后同一个后台任务扫描一次文件名、显示名称、读取尺寸、排版、合成和保存，不要求先读取或仅预览。
- 参数修改不自动触发另一轮后台批次计算；显式仅预览、读取和手动旋转检查仍可用。生成期间继续显示本批次真实缩略预览和进度。
- 快速模式持久化；禁用旋转比较不代表大图解码、标签布局、安全扫描和磁盘保存零耗时。测试不等于 Windows 实机及切膜机生产验收。

## 28. 2026-09-13 源码版 0.1.71：扣除 RIIN 已设置的左右预留

- 用户明确 RIIN 已设置左右各 10mm，600mm 膜实际可用 580mm；允许设置左右预留并持久化。覆盖此前不提供此设置的要求。
- 膜规格保持物理 600mm / 450mm；可用宽度 = 物理膜宽 − RIIN 左预留 − RIIN 右预留，默认分别得到 580mm / 430mm。
- 这些值只扣除容量，不在 PNG 中再次添加左右空白，也不修改 RIIN 本身配置；用户应保持两边设置一致。
- 预览、统一刀位候选、旋转候选、配对、输出和安全检查共用可用宽度。刀位 x 从输出排版左边起算，实际膜上的位置还需加 RIIN 左预留。
- 默认手动刀位取可用宽度的一半，已有合法手动刀位保留；预留变化立即更新合法刀位范围。预留之和不小于物理膜宽时禁止生成，显示参数错误。
- 不改变订单/双面及实际像素通道安全规则；旧 PNG 需重新生成才能采用新宽度。

## 29. 2026-09-13 源码版 0.1.72：单件尺码连续是硬约束

- 用户反馈单件批次输出顺序乱，强调同尺码必须在一起。已识别单件单面和单件双面均按规范化尺码从小到大形成连续块，XXL 与 2XL 等别名合并。
- 取消小图跨尺码搭配；不同尺码单件不共享双列行。多件订单和无法可靠归属/识别尺码的订单不擅自拆开或归类。
- 高级旋转按完整尺码块迁移，不能把同尺码单件拆区。常规区到旋转区的实际生产顺序仍需尺码递增；不能为省膜把小尺码放到大尺码之后。
- 输出前独立按实际行及左右坐标核验尺码连续、顺序及区域，违反时禁止保存。检查结果写入批次的 `order_check.single_size_blocks` 与 `single_size_verified`。
- 保留原始图片编号，不把生产文件前缀号或源目录顺序当作尺码顺序。旧 PNG 需重新生成；用户这次反馈的实际输入文件夹尚未提供，不能声称已核验该实图批次。

## 30. 2026-09-13 源码版 0.1.73：输出保留原文件夹名称

- 输出任务目录和 PNG 文件名都必须包含输入图片文件夹名称，便于员工辨识来源；覆盖之前仅用 JOB 时间目录及标签文件名的规则。
- 默认示例：输入 `D:\订单\批次123` → `D:\订单\批次123_JOB_时间\批次123_CY26 M1.png`。自定义保存位置也保留该命名结构。
- 标签模板先展开，文件夹名及标签中的非法字符安全处理；重名不覆盖。旧输出不自动改名或删除。
- 批次辨识不依靠辅助线识别。现有二维码位置检测用于确定已要求打印的红色刀位点线范围，整批切割及像素安全检查继续保留。

## 31. 2026-09-13 源码版 0.1.74：运行中的 HA 图标

- HA 图标不只用于桌面快捷方式，运行中的主窗口、设置窗口、启动窗口和 Windows 任务栏也应使用。
- 在创建 QApplication 前设置稳定的 Windows AppUserModelID，避免与 Python 的默认任务栏分组混用；应用和主窗口显式设置 HA 图标。
- 图标资源同时打包 ICO 和 PNG，保留回退。开发命令行窗口是独立终端，不属于排版软件窗口。
- 本地可验证图标加载、窗口继承及 Windows API 调用，但真实 Windows 任务栏显示仍需测试电脑确认。

## 32. 2026-09-13 源码版 0.1.75：单排色块固定在文件左边缘

- 用户指出单排图片色块不在整个输出文件左边，机器无法识别。单排（含双面纵排及旋转单排）必须使用文件最左侧的色块基准，不能独占右分区。
- 右分区的固定色块只用于真正横向并排的右图；单排标签继续紧随其左边缘色块，不另起标记区域。
- 整批自动刀位计算必须遵守单排靠左约束；手动左区放不下时明确报错，建议自动刀位、增大左区或单列，不能绕过刀位安全检查。
- 输出前独立核验单列和双列中的右侧孤立行；预览和输出使用同一真实排版坐标。旧 PNG 必须重新生成。
- 小批完整输出同时核验左右坐标、实际左边缘红色像素、全长透明刀位通道及尺码顺序，覆盖 Pillow 和 libvips；不代表用户实物切膜验收。

## 33. 2026-09-13 源码版 0.1.76：默认图片间距 5mm

- 图片间距默认改为 5mm，界面初值、无已保存配置时的恢复值和排版核心默认值保持一致。
- 最新澄清：切膜模式的 5mm 指上下垂直间距；水平距离由整批刀位和分区计算，不额外加 5mm。
- 保留用户已保存的自定义间距；旧电脑如保存了 3mm，需在打印参数中把“图片间距”改为 5mm，之后自动持久化。
- 不将批次开头/结尾留白、刀位安全距离或色块间距误当作图片间距修改。

## 34. 2026-09-13 源码版 0.1.77：从扫描开始的分步耗时

- 主界面显示本次大步骤的实时累计秒数、占总耗时比例、状态及最耗时步骤，可直接复制。开始新批次清空旧报告；完成、失败或停止后冻结计时。
- 按后台执行时钟记录扫描文件名、订单/尺码分析、尺寸与标签、刀位排版、旋转比较、坐标检查、图片准备/合成、像素检查、二维码辅助线、保存、输出复核及批次信息整理；未执行阶段不虚构耗时，重复阶段累加。
- 输出目录保存“耗时报告.txt”，批次记录保存 `print_image.operation_timings`。该总耗时从扫描开始到批次信息整理结束，不包含报告和 manifest 自身写入、界面等待、独立后台缩略图。
- libvips 延迟计算可能将解码/合成计入安全检查或保存，不能用该保存耗时冒充纯磁盘性能；旧 reading/combining/saving 字段为兼容保留，详细分步使用新报告。
- 用户希望用耗时定位并行优化方向。标准引擎已有图片准备线程池，但本次不实施把 200 张任意拆成两个 100 张或把常规/旋转完全独立优化；需共享整批刀位、订单/双面/尺码约束及内存预算。实际提速须用测试电脑同一批次比较，不能宣称线程数翻倍就速度翻倍。

## 35. 2026-09-13 源码版 0.1.78：复用排版分析与等价刀位

- 用户要求修改算法、减少重复搜索。刀位比较复用预先建立的完整订单组、单件尺码块及高度次序；文件名订单/双面/尺码事实使用有界缓存。
- 同尺码配对先用数值占位与高度计算，不为每个试配生成 Placement/UnitMember；真实放置仍走原规划和独立安全验证。
- 合并左右可放置状态完全相同的候选刀位，保留原高度目标及靠近膜中间的平局规则，不声称对任意排列全局最优。
- 保留整批真实像素通道检查；本次未删除像素验证或改变整批 PNG 输出形式。
- 用户已确认增加可选分段输出。打印参数设置期望文件数（默认 1 保持整图）及最多并行段数（1 或 2），自动持久化；先排整批再按完整订单和整行边界分段，不拆双面、未知订单或共享行，不独立重算刀位。
- 并行同时处理最多 2 段；内存预算可设置并保存，默认 512MiB，输出画布及准备缓冲估算超过预算时自动串行。图片准备采用有界任务队列，避免提前解码全批图片。预算不是对实际峰值内存的保证，超大单个订单仍可能占用较多内存，不能仅为提速盲目增大预算。
- 输出为“原文件夹名_标签文字 第001段.png”等连续文件；批次记录的 parts 保存每段真实坐标、刀位、像素检查及耗时，耗时报告追加每段报告。总体并行处理按真实墙钟计时，不累加重叠阶段；兼容旧界面的保存耗时包含分段合成和检查。
- 每段单独核验实际像素通道；任一段失败或停止后，本次新生成 PNG 改为“禁止打印”扩展名并保留，不触碰旧输出，不把不完整批次当作已完成。增加各段开头/结尾留白会改变总用膜，节省统计按实际总长度重算。
- 多文件并行存在额外磁盘/内存竞争，不保证更快，需 Windows 同一批次比较 1/2 个并行段的耗时。本次不声称用户实际 172 秒批次已经验收。

## 36. 2026-09-13 源码版 0.1.79：操作图标与视觉层级

- 用户要求界面更精致易用，每个操作按钮配图标，重要操作使用高亮颜色。
- 主界面和已有设置窗口统一使用可缩放矢量图标，保留中文文字，不用图标替代按钮含义；开始排版/生成最终文件使用蓝色主操作，停止使用红色警示样式，普通操作使用次级样式。
- 明确悬停、按下、键盘焦点和禁用状态；输入框、分类卡片、表格及进度条采用统一视觉样式。不改排版算法、默认参数、生产安全验证和线程生命周期。
- 本地离屏截图核验窗口布局和按钮可见性，真实 Windows 缩放和显示效果仍需测试电脑确认；此次仅推送源码，不生成 Release 安装包。

## 37. 2026-09-13 源码版 0.1.80：末尾旋转、红线、并行与尺码命名

- 用户确认旋转区最左边缘色块向右 2mm，覆盖此前该区域色块必须 x=0 的规则。普通区仍 x=0；旋转区色块和标签一起右移，图片和刀位不挪。检查偏移后不碰图片、不越出画布，独立按区域核验实际刀码位置。用户已将先前“左移 2mm”改为右移，不能恢复负坐标。
- 用户指定红色横线在最后一张图下方 3mm。区域结束到旋转区开始之间及批次/分段结束处增加红线，默认线粗 0.3mm（开发初值，可修改），与实际预览一致。红线不覆盖原图、标签或色块，末尾补足必要画布空间；省膜按包含红线、区域间距及留白的真实总长度比较。
- 精确圆点和横线矩形允许通过输出像素检查，不放行额外区域。仍在加标记之前核验整批实际透明刀位通道；两种引擎验证输出的精确标记范围，额外通道像素禁止输出。
- 快速模式允许可关闭的“单件批次末尾 3XL 及以上整尺码块旋转”，默认启用，覆盖此前快速模式完全不比较旋转的规则。只比较几个从大尺码开始的完整后缀，普通区直接复用快速刀位搜索的结果，不启动全批旋转 DP；仅可靠全单件批次可走此路径。每行一张，完整订单/双面相邻、同尺码不拆区，整批确实省膜才采用，不宣称全局最优。归属不明或混合多件保守保持原方案。
- 用户明确不限制并行保存的内存预算。打印参数增加持久化“不限制并行内存预算”，未保存该选择时默认启用，勾选后不因估算预算改为串行；取消后恢复可设置预算。保留图片准备有界队列及最多 2 段的既有并发选择，内存不足仍可失败，不能承诺系统一定够用。文件数 1 仍为单张输出，主界面明确未启用多段并行。
- 输出文件名保留原输入文件夹和标签，加实际尺码与旋转区类型；分段带连续段号。只有实际生产顺序连续的标准尺码才压缩，例如 S、M、L、XL → S-XL；缺码、不按递增排列或回到前一尺码时用加号列出，不虚构区间。数字尺码不猜相邻规则；无法识别的尺码不冒充已识别。
- 主界面显示每段文件、尺码、图片数、各区刀位、刀码右移及红线位置，输出目录写“切割说明.txt”。分段末尾提示与真正批次结束区分，下一段变为旋转区时明确换刀。刀位仍从输出图左侧计算，实际膜位置需加 RIIN 左预留。
- 大图引擎原图采用可重读方式，避免实际像素验证后保存旋转图片时报 forward-only/out-of-order read；仍是延迟合成，不提前加载整批到 Python 内存。实际 Windows 性能和机器对错位刀码的暂停行为需实测；不把本地小批测试当作生产验收。本次仅推送源码，无 Release 安装包。

## 38. 2026-09-13：日期与当日更新次数

- 用户要求版本文字简洁，只显示日期与当天更新次数，例如“版本 2026-09-13 · 第20次更新”，主界面及更新提示统一格式，不再并列显示 0.1.xx 与“发版日期”。
- 本次内部版本 0.1.81，固定日期 2026-09-13、当日次数 20；按有明确日期的交接源码发布记录 0.1.62 至 0.1.81 补齐历史计数。更早未明确日期的版本不猜发布日期和次数。
- 每次发布同时维护 `__version__`、`__release_date__` 与 `__release_iteration__`。同日次数加 1，换发版日期后设为 1。次数属于发布元数据，不按启动次数、检查更新次数或当天用户操作次数增加，不在运行时读取当前日期拼版本。
- 内部数字版本继续用于兼容更新比较、Git 标签和安装程序；主界面悬停版本文字可查看内部版本。旧源码缺少当日次数时显示“历史版本”，不伪造次数。
- 未来发布 Release 安装包时，说明中写“发版日期：YYYY-MM-DD”和“当日更新次数：N”，供安装包更新检查读取，不能用上传时间代替构建版本的固定日期。本次仅推送源码，无新的 Release 安装包。

## 39. 2026-09-13：垂直间距与水平刀位分开理解

- 用户明确：图片上下垂直间距默认 5mm；水平距离取决于计算后的刀位本身，不把 5mm 同时用于横向。
- 检查确认现有单列/固定双列切膜算法已符合此规则，本次不改变刀位搜索、分区坐标或订单安全策略。界面改为“上下垂直间距（毫米）”，提示水平距离由整批刀位、分区及色块位置计算。
- 保留已有用户配置，不强制把保存的其他垂直间距重置成 5mm。独立的刀位安全距离、色块与图案间距仍保留，不把它们清零。
- 自由排版没有固定刀位，保留原先双方向间距行为；切换该模式时名称和提示即时改成自由排版说明，避免混淆。
- 回归覆盖单列及双列的真实尺寸订单：上下间距为 5mm，修改垂直间距后所有图片与色块横坐标不变；同时覆盖界面模式切换和配置持久化。
- 本次内部源码版本 0.1.82，界面版本“2026-09-13 · 第21次更新”；不生成新的 Release 安装包，旧输出图片需重新生成才会采用当前参数。

## 40. 2026-09-13：日常工作台与低频检查分离

- 最新用户要求主界面围绕排版、耗时、预览和总结，其他日常不常用的内容放在独立界面，不占主界面。保留常用标签文字和机器号入口。
- “批次详情与检查”以非模态独立窗口集中展示订单/尺码树、图片检查与手动旋转、切割明细、仅预览及日志。复用当前批次数据与原控件，不因打开详情重新访问平台或重新排版。
- 主界面保留开始/停止、真实处理阶段、分步耗时、整批/双图预览、省膜总结及完成/错误提示。打印参数继续放在底部独立入口。
- 隐藏的详情仍接收当前批次更新；不移动工作线程到 GUI，不创建重复任务，不改变算法、刀位或输出像素安全检查。
- 本次内部源码版本 0.1.83，日期 2026-09-13、当日第22次更新。针对相关界面、启动、直接生成、后台预览与耗时验证，不因 UI 调整重跑全量算法测试；仅推送源码，无新的 Release 安装包。

## 41. 2026-09-13：默认序号与醒目平台标记

- 最新要求：标签与文字默认添加每批 1 至最后一张的序号；平台使用下拉选择，当前默认“隆丰”，平台字大而醒目，在二维码旁，整段高度与二维码一致。
- 主界面保留平台选择和序号开关，参数自动保存。不替换用户已有标签文字；模板已有编号变量时不重复附加。关闭自动序号不删除用户手写的编号变量。整批源图编号在分段间保持不重复、不重置，沿用原来稳定的源图身份编号。
- 平台下拉曾误设为“隆丰 / 蜂鸟”，已由第 42 节纠正；蜂鸟是 ERP，不能当作实际生产平台。平台标记不恢复 ERP 流程、不访问平台，完整标签设置可以关闭；原有关闭标签的选择继续尊重。
- 平台名称是独立文字，不放大普通编号/标签。复用二维码真实边界及缓存，按旋转后的二维码高度测量中文粗体字形；放在二维码最近侧的图片外、与其顶部对齐。不覆盖图案、不额外增加一行高度，横向占位纳入整批刀位和可放置范围计算。
- 最左色块仍在固定基准，右图色块仍对齐右分区；平台文字也进行刀位通道及提示横线碰撞核验。宽度不足时不能偷偷挤入图案或缩到看不清，原有不能并排的订单按安全方案放置。
- 识别不到二维码或缺少中文字体时明确报错并阻止可打印输出；检查预览可以显示不带平台字的源图，但必须保留禁止输出警告，不能猜测二维码高度。
- 预览、两种输出引擎、手动旋转、旋转区坐标平移及分段输出共享平台标记的真实坐标。批次记录保存平台、序号开关及逐图平台矩形；自动附加的编号不改变原有输出标签命名，显式模板中的编号变量仍正常展开。
- 本次内部源码版本 0.1.84，日期 2026-09-13、当日第23次更新；实际切膜机可读性及用户生产批次仍需实测，旧 PNG 需重新生成。仅推送源码，无新的 Release 安装包。

## 42. 2026-09-13：手填标签与自动平台、机器号、序号

- 用户纠正蜂鸟是 ERP，不是平台本身。平台下拉默认仅提供已确认的“隆丰”，允许填写实际平台名并保存；不凭空补名单。上版误保存“蜂鸟 / Haloo”时恢复为“隆丰”，有效的自定义平台继续保留。
- 标签与文字由用户手动输入。新配置不预填 `CY 1001Mt26` 示例；已有用户保存文字不清空、不改写。
- 用户已确认“编码”指每张图片 1、2、3… 的序号，不是订单或商品编码。平台、当前机器号及图片序号自动显示，不要求手动把机器号拼进标签；删除“添加机器号”按钮，保留 M1 至 M11 选择。
- 输出复用原有标记布局：二维码旁醒目平台字；色块旁/下方的手填文字自动附带当前机器号及序号。手动模板已有机器号或编号变量时不重复添加；输入框自身不被程序塞入自动字段。
- 预览同样显示自动字段，机器选择变化直接影响后续输出；分段仍沿用整批编号。保留平台等高适配、透明底、固定刀位、全批像素及订单安全检查。
- 本次源码 0.1.85，日期 2026-09-13、当日第24次更新；验证相关标签、预览、两种引擎的整批安全和分段并行，不重跑无关平台流程。仅推送源码，无新的 Release 安装包。

## 43. 2026-09-13：可选择的并行保存段数

- 用户要求自行选择并行数量实测。打印参数中的“分段与并行保存”允许选择 1–8 段，默认仍为 2 段，参数自动持久保存；界面与后端共用上限，不再隐藏限制为 2 段。本节覆盖此前仅支持 1 或 2 段的限制。
- 实际并行不超过安全切分后的输出文件数；完整长图（文件数为 1）不启用多段并行。选择 4 段时，期望输出文件数需至少 4 个，订单或整行边界不足时不能强行拆单凑数。
- 保持不限制并行内存预算的现有默认选择、整批固定刀位、同订单/双面/尺码安全边界与逐段真实像素通道检查。批次总结和耗时报告展示实际并行数量，不把用户选择误报为实际数量。
- 两种输出引擎均验证真实 4/8 段同时执行，并检查各段 PNG 全高度刀位通道；相关回归共 28 项通过。并行更多不保证更快，用户测试电脑的实际速度仍需实测。
- 本次源码 0.1.86，日期 2026-09-13、当日第25次更新；仅推送源码，无新的 Release 安装包。

## 44. 2026-09-13：统一切膜机输出文件夹

- 用户要求所有输出收纳在“切膜机文件”中，与原排版图片文件夹放在同级。本节覆盖此前直接在同级散放任务文件夹的规则。
- 默认结构为 `原图片目录的父目录/切膜机文件/原文件夹名_JOB_时间/输出文件`。保留独立任务子目录及原文件夹、标签、尺码、段号命名，避免重复生成覆盖及混淆。
- 完整长图、并行分段 PNG、切割说明、耗时报告和批次记录共享同一任务目录；自定义保存根目录内也按“切膜机文件/任务目录”收纳。保存后仍打开本次任务目录。
- 旧输出不自动移动、删除或改名，不改变排版算法、刀位或像素安全核验。相关路径、实际 PNG 生成、分段输出及版本回归 22 项通过。
- 本次源码 0.1.87，日期 2026-09-13、当日第26次更新；仅推送源码，无新的 Release 安装包。

## 45. 2026-09-13：合并报告与平台文字复用透明空位

- 最新用户要求切割与耗时报告合为一个文件，新任务只输出“排版报告.txt”，包括完整切割说明、总计与分步骤耗时、实际并行数及分段耗时；批次记录仍保留机器可读数据。旧报告不删除。
- 平台字不能简单追加在生产图右边导致本来可并排的图被拆行。优先在旋转后二维码旁的原图透明区域内放置，保持二维码等高和真实预览一致；不得覆盖二维码、白底标签或任何有透明度的原图内容。
- 检查整个文字矩形对应的源图 alpha，并扩大源像素边界用于防止重采样邻域覆盖；无安全空位时保守左侧外置，不能为省膜覆盖原图。左侧外置仍可能占位，不能承诺所有图均无额外空间。
- 保留整批刀位、订单/双面/尺码检查和精确输出像素通道核验。新增两种引擎 12 张整批测试，确认透明头部内的平台字保持 6 行双列，而不是仅靠两个样本推断。
- 本次源码 0.1.88，日期 2026-09-13、当日第27次更新；仅推送源码，无新的 Release 安装包。实际生产图片的透明头部是否有足够空间仍需实测。

## 46. 2026-09-13：左右膜标签动态空位与双排质量提示

- 用户提供两张预览截图：标签位于左、右两种位置；右侧膜标签时平台字被外置到左侧，推远原图。600mm 膜预期双排，单排不能直接作为理想结果。
- 根因是上一版只在二维码附近最多一个二维码高度的水平范围搜索，遇到长膜标签卡片无法搜索到卡片另一侧的透明空位。现在检查整个二维码高度带的透明列，按离二维码距离选择左右候选，再核验带重采样邻域的真实源 alpha；旋转使用逆向区域变换。不覆盖源图或二维码。
- 主界面真实预览总结和统一排版报告新增双排行数、常规单排数量、旋转单排数量；常规单排明确“未达到全双排预期”，逐图显示实际尺寸与是否平台字外置。原因尚未被证明时写需核对尺寸、刀位、订单/尺码边界，不伪造精确原因。
- 新增左右长膜标签各 12 张的真实输出测试，两种引擎均检查 6 行双排、文字在图片宽度内及整批精确刀位通道；宽图单排测试验证质量提示和报告。保留订单/双面/尺码安全，不强行跨刀位或缩图，也没有改变刀位搜索的高度目标。
- 已用保留的 609120504008（89 张）在 300 DPI、580mm 可用宽度重新计算预览：16 行双排、57 张常规单排，11 张平台字仍需外置；独立坐标检查无警告。仅是保留批次的计算检查，未生成其生产 PNG，未证明所有单排不可改善，也不能代替用户当前批次实测。
- 本次源码 0.1.89，日期 2026-09-13、当日第28次更新；仅推送源码，无新的 Release 安装包。后续仍需针对当前实际单排图片继续核验，不能宣称 600mm 批次已全部双排。

## 47. 2026-09-13：平台文字独立可调大小

- 用户明确平台字过大，允许人工设置并显示。主界面及标签设置新增独立“平台文字高度”，单位毫米，默认 6mm；0 为自动二维码等高，允许 0–50mm，并显示当前数值。覆盖此前平台字必须等高、不能调小的要求。
- 新参数自动持久保存，不影响普通标签、序号字号或已有手填文字。实际平台文字高度不超过识别出的二维码高度，防止另起行占用材料；字号变化参与真实占位、安全空位及刀位计算，不只缩小预览。
- 已有批次且未生成时，调整平台字高度即启动合并的后台预览刷新；未加载旧批次时不触发读取，生成中不重复排版。预览与两种输出引擎共享最终文字尺寸及像素缓存。
- 验证主界面/设置双向同步及重启保留、两种引擎 12 张整批 6/12mm 输出字形与通道安全、左右标签及分段输出；不以小字号替代真实像素安全检查。
- 本次源码 0.1.90，日期 2026-09-13、当日第29次更新；仅推送源码，无新的 Release 安装包。旧 PNG 需重新生成，实际机器可读性仍由用户实测。

## 48. 2026-09-13：刀码与文字优先内置透明左边缘

- 用户再次强调能省的空间必须省，截图显示原图左侧透明区域可以放刀码，不应再额外占一列。切膜模式默认优先把刀码及下方文字一起放进原图最左侧透明空位；本节覆盖刀码必须放在图片矩形外侧的旧实现。
- 仍保持整批固定基准：常规左区/单排刀码在文件 x=0，右图在右分区固定基准，不随二维码位置移动。原图透明边缘充足时源图起点与刀码起点重合，减少占位；不足时保留外置，不覆盖二维码、白底膜标签或图案。
- 检查刀码和整段文字矩形及源像素重采样邻域均透明，并检查平台文字不碰标记组。规划后及每段保存前，对所有实际放置重新核验原始 alpha，不只相信排版决定或刀位通道。旋转区 2mm 右移同样重新检查透明空间，不能放行覆盖原图。
- 刀码内置与外置图片可以在同一批次混合，但固定刀位、订单、双面与尺码约束不变。总结及统一报告显示实际刀码内置数量，真实预览及两种输出引擎复用同一坐标。
- 新增 16 张混合整批测试（12 张 280mm 宽可内置、4 张膜标签在左必须外置），580mm 可用宽度得到 8 行双排。两种引擎分别验证整图及并行分段；检查原图每个非透明像素原样保留、真实红色刀码像素、整批刀位通道及故意错误内置标记被独立拦截。
- 完整回归 268 项通过。保留批次 609120504008 的 89 张实图在 300 DPI、580mm 可用宽度、平台字 6mm 下重新计算预览：78 张刀码内置、18 行双排、53 张常规单排，整批坐标及内置源 alpha 核验无警告；未保存该批生产大图，不能当作全双排或切膜机实物验收。
- 本次源码 0.1.91，日期 2026-09-13、当日第30次更新；仅推送源码，无新的 Release 安装包。原有用户间距等参数不重置，旧输出需重新生成，切膜机可读性仍需实测。

## 49. 2026-09-13：旋转刀码与二维码同高，文字在二维码下方

- 用户确认默认向左旋转后，刀码仍在整个文件左侧固定识别基准，垂直位置跟随实际旋转后的二维码顶部；旋转区保持此前向右 2mm 的错位提示。普通文字（手填内容、机器与序号）改到二维码正下方，不再跟随刀码水平偏移。本节覆盖旋转时刀码与普通文字始终绑在一起的旧规则；常规区规则不变。
- 使用真实二维码边界按源图旋转角度变换；文字从二维码下方沿同一横坐标搜索安全透明空位，刀码只在左侧基准搜索，不能为了省材料随二维码向右移动。检查完整矩形及源 alpha 的重采样邻域，并避开平台文字；透明空位不足时实际占位保留外置/下方空间，不覆盖白底膜标签、二维码或图案。增加的占位参与省膜比较，不能伪造节省。
- 已选择生产平台却无法识别二维码时，不允许手动旋转输出猜测位置。自动旋转比较排除未识别或偏移不安全的候选，完整订单仍留在常规区，不丢图、不拆双面。无平台、无二维码的旧纯几何输入保留兼容路径，不宣称已动态识别。
- 主界面实际预览、两种输出引擎和分段保存复用同一最终坐标；每段位移同步应用于二维码相对刀码及文字。规划后独立核验每张旋转图片刀码与二维码同高、文字在其正下方，以及内置标记源 alpha。故意偏离一像素的坐标测试会禁止输出。
- 新增 12 张、6 个完整双面订单的真实 PNG 整批验证，两种引擎各覆盖整图与三段输出，逐张核验原图所有非透明像素原样保留。另实际保存整批单排旋转区，核验 2mm 左基准错位、文字不随刀码错位以及整批刀位像素通道。完整回归 272 项通过；不是用户生产批次/切膜机实物验收。
- 本次源码 0.1.92，日期 2026-09-13、当日第31次更新；推送源码，不创建新的 Release 安装包。需要检查更新并重新生成批次，旧 PNG 不会自动改动。

## 50. 2026-09-13：左下二维码的刀码放左上角

- 用户纠正：向左旋转后二维码在左下角时，刀码应在旋转后图片左上角，而非继续跟底部二维码同高。按真实旋转后的二维码中心判断左下位置，刀码 y 为图片顶部；其他二维码位置保留实际二维码顶部基准。此规则覆盖第 49 节所有旋转刀码必须同高的要求。
- 刀码优先内置左上角透明区域；不足时安全外置在相同顶部高度。仍保持文件/分区左侧固定横坐标，不能覆盖原图、膜标签或跨刀位，不能新增刀码专用行。普通文字位置不在本次擅自变更，仍在二维码下方；是否增加长度继续计入真实排版占位。
- 用户要求先不考虑旋转区及 2mm 偏移，本次暂停刀码偏移机制，移除设置入口，界面生成参数与输出记录均为 0；旧保存值不删除，但不再参与输出。报告不再宣称偏移停机。其他订单/双面/尺码与固定刀位安全规则保持。
- 真实预览和两种引擎输出共享核心坐标；独立逐图检查改用新顶部规则，不能只改预览。12 张整批混合上部/左下二维码、6 个双面订单，验证整图及三段输出每张刀码像素、原图非透明像素完整保留；整批单排旋转区域另验证固定 x=0 和真实刀位通道。旧设置恢复测试验证 2mm 不重新生效。
- 本次源码 0.1.93，日期 2026-09-13、当日第32次更新；推送源码，无新的 Release 安装包。旧 PNG 不修改，需要更新后重新生成，生产机器仍需实测。
- 回归执行结果：全量中 270 项通过，2 项仍断言旧的 2mm 坐标；更新为新规则后，相关 8 项重跑全部通过（含两种引擎末尾整尺码旋转、混合二维码实图与旧设置恢复）。没有把旧断言失败当作生产代码问题，也没有跳过原图及刀位像素验证。

## 51. 2026-09-13：旋转区可直接启用，并行比较长度

- 用户要求试用旋转区且保持多件、双面成组，并行计算常规/旋转长度和省膜供人工判断。原开关被默认快速模式禁用；现双列模式可直接点击，首次启用自动退出快速模式并关闭独立的末尾旋转，避免两种自动策略混淆。单列和自由模式仍不开放固定双列旋转区。
- 启用后两个后台计算线程分别计算无自动/人工旋转的常规方案及允许完整订单旋转的方案；普通方案不受旋转方案的刀位或可变分析数据污染。两套均独立验证订单/双面/尺码、固定刀位及标记源 alpha；仍不并行生成两份生产 PNG，不更改订单安全策略。
- 主界面总结及统一报告显示两套分段前整批长度、省膜差值、实际旋转张数；包括标签、色块、区域间距、红线与批次留白。不使用“当前长度+截断后的节省”代替独立常规长度。分段额外留白与红线仍由实际输出总结另计，比较值明确标注分段前。
- 用户可启用旋转区后仅预览整批，查看真实结果，再取消开关选择常规方案或保持开关生成。保持直接开始排版的既有流程，不强制预览确认；自动旋转仍只有实际省膜/常规不可行时采用，不为展示效果强行旋转。生产输出继续检查全部区域真实像素通道。
- 新增屏障同步测试，证明两条真实计算路径同时执行；两种输出引擎覆盖完整多件/双面订单的整图和并行两段保存，以及全高通道真实 alpha。另验证默认快速模式下开关一次点击即可启用并退出快速模式。
- 本次源码 0.1.94，日期 2026-09-13、当日第33次更新；推送源码，无新的 Release 安装包。并行不保证 Windows 耗时按比例缩短，实际生产批次与切膜机需实测。
- 完整回归 277 项通过；没有生成用户当前生产批次，不替代 Windows 与切膜机实物验收。

## 52. 2026-09-13：预览放大与独立最大化检查

- 用户反馈预览区太小无法放大。本次主预览区高度改为 420–720 像素，新增 25–400% 缩放、放大/缩小、适合宽度及 Ctrl+滚轮；超过可视宽度可横向滚动，整批继续纵向滚动，不缩整批长图挤入一个窗口。
- 最大化查看将当前预览及工具栏放入独立可缩放窗口；返回主界面/关闭窗口恢复原位置和缩放。复用当前真实 Placement、刀位与标记，不重新扫描/排版，不创建另一份生产输出，不移动工作线程。主界面生成过程继续更新同一个预览。
- 放大时按倍率提高可见图片缩略图分辨率，上限 1600 像素，继续保留最多 12 张的有界缓存。不加载整批原始大图；只有显示尺度变化，不修改打印尺寸、订单归组、旋转或输出坐标。
- 实际窗口测试覆盖 300% 横向滚动、最大化、返回、恢复适合宽度、快照身份未改变及没有启动后台排版；空预览缩放/最大化不读取旧批次。已查看实际离屏截图。此为预览界面变更，不改生产算法。
- 用户询问可并行步骤：尺寸/DPI 当前按图串行读取文件头，可按图并行但此前仅约 0.46 秒；二维码及透明区域检测更值得优先优化。Pillow 图片准备已有有界线程池，分段保存已有 1–8 段选择，常规/旋转比较已有两条并行路径。订单归组和最终固定刀位必须整批统一，不能任意分两个 100 张独立排版。本次只完成预览功能与并行现状检查，未实施新的尺寸/二维码线程池。
- 本次源码 0.1.95，日期 2026-09-13、当日第34次更新；推送源码，无新的 Release 安装包。实际 Windows 视窗缩放及生产机器仍需用户复核。
- 预览、生成中显示、异步取消、窗口总结、图标样式和版本相关 19 项通过；最后另重跑预览、总结及代码行数规则 5 项全部通过。本次未重跑无关生产算法全量，也未重新生成用户批次。

## 53. 2026-09-13：移除区域之间多余红线

- 用户要求双排区与旋转区不加多余红线；覆盖此前区域结束处增加横向红线的要求。预览与输出仅保留批次/输出分段结束提示线，默认在最后一张图片下方 3mm；二维码高度内的刀位点线不在本次移除范围内。
- 区域连接不再为红线额外预留空间，保持真实图片与标记占位、区域间距和留白安全。常规区与旋转区各自固定刀位，完整订单、双面与尺码块不拆散。
- 上下垂直图片间距默认仍是 5mm，水平距离由固定刀位决定；不覆盖用户持久化的其他间距值。
- 本次源码版本 0.1.96，日期 2026-09-13、当日第35次更新；测试通过不代表用户生产批次或实物切膜验收。
- 完整回归 279 项通过，包含两种引擎整批/分段 PNG 的真实刀位像素核验、预览不绘制区域红线及垂直 5mm 间距与自定义值持久化。仅推送源码，不生成 Release 安装包；旧 PNG 需重新生成。

## 54. 2026-09-13：四方案长度、耗膜面积及占位对比

- 用户需要 60cm/45cm 膜各自不旋转/允许旋转的四套整批长度，换膜时不能仅比较长度，需比较耗膜面积和图片占比。
- 打印参数新增持久化四方案比较开关，默认关闭，避免普通生成重复额外搜索；启用后在显式预览/生成任务内计算，启动不读取。四套仅生成几何方案，不保存四份大 PNG，逐套执行避免叠加四套线程池资源争抢；主总结与排版报告显示全部结果及无安全方案的原因。
- 各膜宽均扣除同一用户 RIIN 左右预留，使用自动整批刀位、固定双列允许安全单排、无手动旋转；旋转仍以完整订单、双面、尺码块安全及真实省膜为前提，并非强制所有图片旋转。比较不自动换膜或改变用户选定的生产方案。
- 耗膜面积为物理膜宽×分段前整批长度，包含标签/刀码占位、留白、区域间距及末尾红线；可用区域面积另按扣除 RIIN 后宽度计算。实际分段增加的留白仍由选定方案输出总结计入，不冒充四方案已保存结果。
- 图片占位率按生产图片矩形面积（包含原图透明部分，不包含新增文字与刀码）除耗膜面积，另列可用区域占位率。明确不是油墨覆盖率，不做额外全批解码来统计透明像素。以耗膜平方米比较跨膜宽节省，显示每方案比面积最省方案多多少平方米；不只看米数，不宣称全局最优。
- 每套独立验证完整订单、双面、尺码、固定刀位及内置标记源 alpha；这是比较计算，不代替选定方案输出后的整批真实像素通道核验。独立显示四方案比较耗时，允许原任务停止机制取消。
- 本次源码 0.1.97，日期 2026-09-13、当日第36次更新；仅源码更新，不生成 Release 安装包，不代表用户生产批次/Windows 性能/切膜机验收。
- 四套真实小批方案验证相同源图片面积、物理/可用面积与占位率、45cm 整单双面旋转、无安全方案提示，以及启用比较不改变选定生产坐标；查看实际四方案总结截图。最后完整回归 283 项通过，包含既有两种引擎整批真实像素检查。首次全量 281 项通过、1 项旧启动测试因仅等待与 debounce 相同的 250ms 而失败；改为有上限等待实际扫描启动后重测通过，未修改生产启动行为。

## 55. 2026-09-13：参数加减按钮

- 用户反馈数值参数加减不易操作、只能手动输入。统一整数与小数数值框，明确提供独立加减 SVG 图标、28×18 像素点击区、悬停/按下/禁用状态，文本右侧预留位置避免挡住输入。保留 Qt 原有步长、范围、键盘调整、参数联动与持久化机制。
- 图标随源码与安装包资源一起配置；主界面和参数窗口共用样式。不解禁因当前模式或生产任务禁用的参数，不改生产算法与间距值。
- 本次源码 0.1.98，日期 2026-09-13、当日第37次更新；仅推送源码，无新的 Release 安装包，真实 Windows 点击体验仍需用户复核。
- 相关 15 项测试通过，覆盖 Fusion/Windows 样式下整数与小数加减实际点击、上下键和上下限、真实参数窗口点击后生成配置同步、参数持久化、缩放预览与代码行数。已查看数值框截图；本次 UI 样式变更未重跑无关生产算法全量。

## 56. 2026-09-13：恢复默认排版设置

- 用户明确要求清除残留参数，提供类似恢复出厂设置的功能。参数窗口新增“恢复默认设置并退出”，确认后清除 layout/label/color_block/cutter/riin/output 保存组及上次输入输出路径、本地测试与合并选项；不删除图片、输出文件、报告或 ERP 登录信息。
- 运行中的生成、更新、平台任务或后台预览阻止重置，必须先安全停止；确认前可取消。清除后同步配置，退出以清空旧缓存与内存状态，用户重新打开使用默认参数。
- 重置期间禁止自动保存调度和退出补保存旧参数，避免清除后被旧窗口写回。清除失败保留并尝试恢复原值，不自动退出或谎报成功。
- 默认完整长图（输出文件数 1）、最多同时处理 2 段、垂直间距 5mm、RIIN 两侧各 10mm、平台隆丰、机器 M1、四方案比较及高级旋转区关闭。机器号、手填标签、手动旋转等会清除，不能称为只重置并行参数。
- 本次源码 0.1.99，日期 2026-09-13、当日第38次更新；仅源码推送，不创建 Release 安装包。重置功能不代表已证明测试电脑 100 秒尺寸阶段的原因或修复性能瓶颈。
- 相关 16 项回归通过，包含 8 段残留参数实际清除、旧窗口退出自动保存不回写、重新构造窗口恢复默认值、取消/忙碌保护、源图与报告存在、ERP 设置保留、启动不读取旧批次与更新流程。此为配置管理变更，未重跑无关生产算法全量。

## 57. 2026-09-13：数据与预览两个主区域

- 用户要求将步骤耗时、处理进度与排版总结合并，主工作区只有数据展示与预览两个区域。标签及开始/停止/参数等操作入口继续保留，不再让三张独立数据卡片挤占页面。
- 统一“排版数据 · 进度、耗时与总结”卡片容纳真实进度条、阶段/预计耗时、当前文件、分步耗时表及复制、批次信息、用膜及四方案比较。原耗时组件去除独立边框/标题，复用现有控件、信号与计时器，不新建工作任务。
- 预览总览/双图选择移入预览区域；保存期间预览与数据保留，新任务清除旧结果。低频订单和切割详情仍在独立窗口，不恢复 ERP 界面，不改排版算法、参数或输出安全检查。
- 本次源码 0.1.100，日期 2026-09-13、当日第39次更新；仅源码推送，无新的 Release 安装包。
- 数据卡内左右分栏，左侧批次总结/实时进度，右侧步骤耗时，避免继续纵向堆三块。已查看实际窗口截图；相关 18 项回归通过，包括数据控件同一父区域、计时复制与重置、保存期间预览保留、错误/取消、缩放和配置重置。不改算法，未重跑无关生产全量。

## 58. 2026-09-13：默认四方案表与一行常用参数

- 用户明确其他三种用膜情况也必须默认展示。数据区右侧步骤耗时上方常驻四行表格，60/45cm 各自常规/允许旋转，显示米数、耗膜平方米、图片占位及可用区占位；面积最省方案标绿，无安全方案明确标注且原因可查看。结果到达前显示等待状态，不把旧批次结果保留到新任务。
- GUI 四方案比较默认开启，首次升级一次性迁移旧默认关闭值；迁移后主动关闭仍持久保存，恢复默认设置也重新开启。覆盖此前默认关闭的要求。底层纯代码 LayoutSettings 默认仍保留比较 opt-in，以免旧 API 调用被迫额外计算；GUI 生成明确传入开启值。启动不自动读取旧文件，表格可见不代表开机扫描。
- 标签文字、实际生产平台、平台文字高度及机器号合并为一行，复用原控件双向同步与保存；日期插入与序号开关保留在完整标签设置中，当前默认序号继续生效。
- 已选择图片文件夹常驻显示名称与完整路径、蓝色高亮、文字可复制；切换路径即时同步，不触发额外扫描。此处“文件”按当前排版输入文件夹显示，不将未读取图片误报为已加载。
- 本次源码 0.1.101，日期 2026-09-13、当日第40次更新；源码推送，无新的 Release 安装包。不修改生产摆放算法或删除源文件。
- 相关 21 项回归通过，覆盖旧默认关闭的一次性迁移、后续主动关闭仍保存、恢复默认开启、四方案真实结果表、四行位于主界面可见范围、四个字段同一行、已选路径高亮且启动零读取、计时/保存预览和代码行数；查看实际一行参数及表格截图。未重跑无关生产算法全量。
- 最后补充比较运行中/未启用状态，相关界面与代码行数 5 项重跑通过；未启用比较的旧 API 数据不再停留为等待开始。

## 59. 2026-09-13：修复检查更新需要第二次点击的竞态

- 用户反馈需两次点击检查更新仍存在。新增回归实际复现：确认弹窗处理嵌套事件时，检查线程已清理，旧代码在弹窗返回后才设置待更新信息，已经错过自动继续更新的回调。
- 检查返回先缓存结果并展示状态，检查线程完成清理后才询问更新确认；确认后同一次流程直接开始应用，拒绝则清除待处理状态。静默检查仍不应用更新，任务运行与代码快进/本地修改保护继续保留，确认后还独立复核任务状态。
- 测试同时发现四方案表 reset 命名覆盖 Qt 原生虚函数，clearContents 触发递归；重命名为 reset_rows，不覆盖原生方法。此为同一事件循环路径暴露的界面缺陷，补测反复清空表格无 Python override 错误。
- 本次源码 0.1.102，日期 2026-09-13、当日第41次更新；仅源码推送，不创建 Release 安装包。测试电脑必须先获得此版本才应用新流程，不能把旧版本点击行为当作修复未生效后的验收。
- 相关 18 项回归通过，包括单次点击、确认弹窗嵌套事件复现、拒绝无残留、静默检查、运行任务保护、应用失败恢复、表格反复清空、启动与参数重置及代码行数。原竞态回归在修改前超时失败，修改后通过；不改排版算法，未重跑无关生产全量。

## 60. 2026-09-13：共享测量与四方案并行几何计算

- 用户不能接受四方案逐套重新读取图片与重复进度文字。覆盖第54节逐套执行要求：源尺寸及常规/左旋转的标签、刀码测量在当前任务中共享，随后四个工作线程同时计算60/45cm常规/允许旋转方案，只处理数值几何，不合成四份PNG。
- 任务级缓存只保留尺寸、不可变单图占位和透明区判定，不保留全批像素；单张测量中的PNG解码由二维码、标签及刀码透明区搜索复用，避免每移动1毫米重新解码。常规和旋转各需必要的单图测量，不承诺无像素读取或瞬间完成。
- 刀位搜索合并等价左右可放置状态；继续按完整订单与连续尺码块动态规划选择双排、纵排及整组旋转，不做像素暴力搜索，不跨拆双面/多件，不宣称全局最优。Python线程并发不等于CPU四倍加速；本次重点是移除重复解码与等价候选。
- 运行中仅显示一处阶段和当前文件，完成后保留输出文件信息；标签与刀码测量独立计时，报告分别列共享测量与四方案并行计算耗时，便于判断真实瓶颈。
- 比较中的透明区检查复用任务内相同源文件/尺寸/旋转/矩形的测量；选定方案实际输出仍重新验证完整订单、几何刀位、真实源alpha及保存后整批通道，仅允许精确辅助点线和末尾横线像素。改变缓存不能绕过生产输出检查。
- 本次源码0.1.103，日期2026-09-13、当日第42次更新；只推送源码，无新Release安装包。测试电脑大批性能及实物切膜尚未验收。
- 并发屏障证明四工作线程同时执行，且测量后计算线程禁止再次打开源PNG的回归通过；12张真实二维码混合整双面订单在Pillow/libvips三段输出中逐段核验完整刀位通道，仅掩盖精确提示线/圆点，并逐张核对原有不透明像素未改变。最后全量296项通过、1项行数检查发现新增UI分支超过250行；合并等价条件后行数及相关UI/版本6项重跑通过。新增完整像素与并发相关6项亦通过；未把小批测试耗时冒充111张生产批次性能。

## 61. 2026-09-13：二维码只在原图左上或右上

- 用户确认原始生产图片二维码只有左上、右上两种位置；这是原图约束，不是旋转后位置约束。检测原始边界后按真实旋转变换，不重新搜索旋转图片，不恢复“所有旋转刀码都跟二维码同高”的废弃规则。
- 优先检测两个角落窗口（各60%宽、40%高，中心有重叠防止切断二维码）；失败后允许扩大同两个窗口到65%宽、75%高以兼容较高标签头部，不在整幅图或底部图案继续找第三个位置。检测输入最长边限定1200像素，先裁剪缩小再转换灰度，透明区域按白色背景参与识别。
- 优先单二维码定位，失败才调用多码定位兼容；不解码二维码内容。二维码中心、真实边界、膜标签高度分析和输出辅助线共享同一边界缓存，后者不再独立重新检测二维码。膜标签高度仍按独立墨迹带分析，不把二维码高度直接当膜标签高度。
- PNG解码仍不可省略；复用单张测量已打开的源像素，不缓存整批原图。二维码定位失败继续明确提示/记录缺失，平台标记或旋转不猜位置，完整订单和真实输出像素检查不降低。
- 本次源码0.1.104，日期2026-09-13、当日第43次更新；仅源码推送，无新Release安装包。实际111张生产批次及Windows耗时未实测，不能承诺固定提速倍数。
- 纯色角落直接跳过；很小的角落窗口按最近邻适度放大（最多4倍、目标最长边400），避免裁剪后小二维码漏检，位置仍按完整源图坐标换算。保持1200像素检测输入上限，不修改原始生产像素或打印尺寸。
- 最后完整回归300项通过、1项旧16张保守归组预览超过10秒等待上限；同模块单独重跑3项通过，预览耗时8.83秒，未放宽测试上限或宣称已解决该预览性能问题。新增4项覆盖左右真实二维码、中心/边界/膜标签分析只识别一次、下部二维码不作为头部及裁剪缩放坐标；两种引擎整批/分段真实像素、订单与双面安全测试通过。初次回归另暴露小窗口的小二维码漏检，增加最近邻放大后相关21项及最终全量相关项通过；没有跳过安全测试。

## 62. 2026-09-13：二维码异常不阻塞整批生成

- 用户明确生产不能因个别二维码识别异常停住，只需告知异常文件名。覆盖此前识别不到二维码就禁止平台标记输出/人工旋转的策略：原图仍参与排版，保留普通标签、编号与固定左边缘刀码；仅省略无法安全定位的平台字和二维码辅助线，不编造二维码高度或位置。
- 主界面增加可复制的醒目异常名单，随当前任务预览/完成数据展示，下一任务清除；统一排版报告及analysis.image_anomalies保留文件名、路径、原因与处理方式。不弹阻塞对话框，不因非致命异常把分段任务判为禁止打印。
- 自动旋转仍排除无法识别二维码的候选，按完整订单/尺码块处理，因此同订单其余图片不会单独迁走；人工旋转缺二维码时使用已测量的安全左侧外置标记，不采用二维码相对位置。订单、双面、尺码连续、可靠尺寸和真正切割通道/源像素安全检查继续阻止危险输出，不全局吞掉异常。
- 用户提供的609140634009两张实图中，A0000092-B74F6NM-2-T-LSJ-NV-黑色-M-NO1-1.png定位失败，A0000091-B74F6NM-1-T-LSJ-NV-黑色-M-NO1-1.png可以定位；不能因外观特殊就把可识别图片判成二维码异常。
- 实图测试还暴露安全配对中右侧宽图没有左侧独立排版基准，原总结min(empty)会崩溃。仅当所有独立组基准存在才累加，否则以选定长度作为无节省基准，不虚构独立基准或省膜；实际刀位安全规则不变。
- 两种引擎在临时目录实际生成用户两张原图，完整高度精确通道检查及逐张不透明源像素比对通过，两图均保留且仅92号记录二维码异常，未修改源文件。新增完整双面订单的整图/三段与四方案比较、外置人工旋转、基准缺失及异常名单刷新回归通过。
- 本次源码0.1.105，日期2026-09-13、当日第44次更新；仅源码推送，无新Release安装包。不是用户测试电脑整批性能或实物切膜验收。
- 最后完整回归308项通过；二维码缺失不再与真正危险的刀位/覆盖错误混用同一阻塞警告。旋转候选说明改为二维码或空间条件未满足，不把二维码缺失错误描述为图片尺寸超宽。新增异常名单在主界面可复制、完成保留和新任务清空测试通过。
- 最后补充旋转候选原因文案后，相关15项再次通过；首次重跑1项因仍检查“无法整体进入旋转区”字样失败，保留该准确描述并重跑通过，不修改生产坐标或跳过旧断言。

## 63. 2026-09-13：移除二维码结构识别，优先搜索膜标签

- 用户确认排版无需识别二维码本身，优先搜索标签区域。覆盖此前二维码真实边界检测要求：生产路径不调用QRCodeDetector、不查找定位点、不验证或解码码内容。参考对象变为膜标签卡片，二维码即使破损/无效也不影响纸卡定位。
- 首先只读取顶部优先区，高度按图片宽度的一半确定并受原图高度限制（小图至少96像素范围），避免固定75%高度截断矮图。区域最长边1000像素，透明像素不冒充白纸，以不透明浅色连通区域找卡片，优先上方、大面积及靠左右区域；相邻且高度重合的纸卡分块合并，不搜索底部图案中的码。
- 浅色纸卡是当前生产标签的图像假设，不是二维码形状检测。深色标签、透明标签或无法分离的区域可能定位不到，仍列出文件名并继续，保留安全外置标记。不把任意原图白色图案当成已验证的二维码。
- 平台字体上限、文字附近参考、旋转坐标、左下标签对应左上刀码和辅助点线高度现在都基于膜标签区域；点线仍不贯穿下方图案，整批真实切割通道及新增标记源alpha检查保留。原QRCode相关函数名/字段仅作旧接口兼容，含义已是标签卡片，不向用户误报已识别二维码。
- 主预览、参数文字、异常原因和计时改为膜标签搜索/辅助线；普通标签六号字、平台可调高度、机器/序号持久化及订单/双面/尺码安全不改。
- 用户两张B74F6NM实图都能找到顶部纸卡；Pillow实际保存后所有非透明源像素原样保留，libvips完整安全通道通过，但进一步严格比较半透明源像素时未全等，不能宣称其半透明像素逐值通过。本次仍需核对差异范围；不改源文件，不生成整批生产大图。
- 本次源码0.1.106，日期2026-09-13、当日第45次更新；仅源码更新，无新Release安装包，不承诺Windows实际耗时倍数或实物切膜验收。
- 半透明像素差异进一步核对：libvips两张实图的alpha完全一致、不透明像素完全一致，仅半透明像素RGB各通道最多1/255差异（91号4183个像素，92号27380个像素）；完整通道验证通过。没有把RGB舍入差异描述为完全逐值一致，也没有关闭像素安全检查。
- 最后完整回归308项通过、1项此前也出现的16张保守归组预览超过10秒等待上限；单独模块重跑该项仍超时（其余13项通过），未放宽时间上限或宣称全部测试通过。补充纸卡连通组件纯NumPy后备核验及候选原因文案后相关14项通过。初次全量还暴露旧计时名称断言与旋转纸卡长宽比例过严，修正为新标签阶段名和兼容竖向纸卡后相关项通过。尚未解决的预览等待超时单独保留，不把它与二维码已移除混为同一结论。

## 64. 2026-09-13：透明空间批量测量

- 用户要求优化标签与刀码测量数据。水平平台字候选和旋转文字垂直候选改为共享透明带的alpha投影与前缀区间判断，不再为每个候选裁剪并扫描整块图片。仍检查全部非零alpha（包括很淡的半透明像素），保留源像素3像素重采样扩边、候选优先顺序、原1mm垂直步距和平台文字碰撞排除。
- 一张图的常规与旋转占位共用同一已解码源图，单张完成即释放；已缓存的四方案几何计算不重新打开PNG，不缓存全批原图，不新增无界线程池。独立输出时的原图标记检查和整批刀位通道检查不取消。
- 开发电脑同一12张真实源图，各运行3次、清理标签区域结果缓存、预热依赖加载；共享测量中位数由0.704秒降为0.431秒（约39%）。两方向全部逐项几何字段与标签内容一致。此为小批测量阶段对比，不代表总生成快39%，不保证Windows大批耗时。
- 源码版本0.1.107，固定日期2026-09-13、当日第46次更新；源码推送与Release安装包区分，旧PNG不自动改变。生产订单、多件、双面、尺码和左右固定刀位规则不变。
- 全量回归320项通过、1项此前已知的16张保守归组预览等待10秒超时，未放宽上限或宣称预览性能已解决；通过项包括两种引擎完整批次/分段真实像素通道及订单安全检查。新增11项覆盖四方向、水平/垂直候选与独立像素判断一致、500候选仅裁剪1次、淡alpha不放行、常规/旋转只解码一次和缓存比较零打开。发版次数断言同步更新后另行重测。
- 最终相关35项重测通过，包含发版次数、文件行数、透明搜索、并行四方案及两种引擎整批/分段旋转与内置刀码实际像素检查；已知预览等待超时仍保留为未解决问题。

## 65. 2026-09-13：未来膜规格与生产选择

- 用户明确机器并不充足，不能默认使用面积最省方案；实际输出始终使用用户选定规格及参数，比较不自动切换。面积最省只是几何参考，不等于当前可生产，也不考虑机器排队、库存及换机成本。
- 用户新增目前没有、未来可能使用的40cm和80cm膜比较。现有60/45cm列在前，80/40cm标注“参考”，每种常规/允许旋转共8套。均扣除用户RIIN预留，并展示整批米数、物理膜耗用平方米、图片及可用区占位率，不能跨宽度只比较长度。
- 仍共享单图测量，最大参考宽度按800mm准备旋转候选；8套数字方案最多4路并行，不扩大至8路图像解码，不输出8份PNG。理论最省可来自未来规格，但不会替代用户生产选择；没有安全方案单独提示。
- 源码0.1.108，固定日期2026-09-13、当日第47次更新；仅源码推送，无新Release安装包。未来规格结果是物理约束下的算法估算，不是设备实物验收或全局最优证明。
- 相关21项通过，覆盖8套面积与占位、80cm可容纳现有宽度无解的源图、参考标记、首屏8行、比较不改变选定排版及两种引擎12张完整双面批次三段输出的全长通道和原图不透明像素。已查看实际八行表格截图；未重跑全量回归，此前保守归组预览超时未在本次修复。

## 66. 2026-09-13：5厘米间隔比较与本地历史

- 用户明确膜宽只按5cm间隔，比较40/45/50/55/60/65/70/75/80cm，共9种宽度18套常规/允许旋转方案，覆盖此前只比较4种宽度。现有45/60cm与未来参考规格区分；仍最多4路数值计算、共享测量，不自动选择理论面积最省结果。
- 显式排版生成或仅预览任务成功完成后，将比较数值、时间、批次来源、输出位置、软件版本、当次完整参数、图片/订单数保存到本地logs目录的“用膜历史.sqlite3”。生产保存成功后才标记已生成；仅预览与已生成分开。未启用比较不创建记录；失败/停止任务不冒充成功，参数自动刷新预览不写历史。
- 主界面增加“用膜历史记录”入口，独立界面按批次记录选择，再查看各宽度方案和可复制详情；开机不加载日志，不重新读取历史原图。数据库长期保留与输出目录分离，删除输出不会删除记录，不自动导入旧manifest，因此旧版本尚未记录的数据不会凭空出现。
- 支持CSV导出全部历史，每次任务每套方案一行，数值保留原精度，包含宽度、RIIN预留、长度、面积、占位、旋转数量、耗时、异常及来源。UTF-8 BOM适配Windows表格软件；历史写入失败只显示醒目警告，不阻断已完成生产输出。
- 源码0.1.109，固定日期2026-09-13、当日第48次更新；仅源码推送，无新Release安装包；统计参考不等于设备实物验收。
- 相关30项通过，包含18套面积/占位、4路计算、两种引擎完整双面批次三段输出真实通道及原图像素、历史跨连接读取、36行CSV导出、未启用比较零建库、停止不写入及日志失败仍发出完成信号。历史页面图标/宽屏截图后相关8项重跑通过，全部代码文件不超过250行。已查看真实历史界面截图；未重跑无关全量，旧保守归组预览超时不在此次修复范围。

## 67. 2026-09-13：多文件夹用膜数据分析

- 用户要求一次提供多个文件夹以大量测试膜规格。主界面新增“批量分析文件夹”独立窗口：可Ctrl/Shift选择多个目录，或添加上级目录下所有直接子批次（跳过“切膜机文件”），开始前列表可检查、移除与去重。
- 当前实现是每个目录独立用膜分析，不合并不同批次的订单，不生成PNG、不修改生产选择，也不调用ERP。每批比较40–80cm每隔5cm的18套，沿用订单/双面/尺码与刀位安全；目录逐批处理，单批最多4路数字方案，防止多目录线程池叠加和全批解码。
- 全程独立窗口显示当前批次、文件、阶段和计数，可安全停止；失败目录显示原因后继续后续目录。关闭运行窗口先请求停止，线程结束前不销毁窗口/线程。完成的每批立即持久化“仅分析”记录，统一分析组编号，已完成历史在停止后保留；日志失败列为该目录失败，不谎称保存成功。
- 完成后按宽度/旋转汇总成功批次的总米数、总平方米及面积加权占位，仅覆盖全部成功批次的安全方案参与面积排名。某规格部分目录无解必须标注覆盖数量，不把更小样本的面积拿去与完整覆盖比较。失败/未完成目录不计入排名且单独列出；不同目录不开跨批次拼版优化。
- 历史新增“汇总同组批次”，从持久化组编号重算同次批量数据汇总；CSV追加分析组编号便于统计去重。普通历史记录不擅自与不同参数任务混为同组。
- 源码0.1.110，固定日期2026-09-13、当日第49次更新；仅源码推送，无新Release安装包。不承诺1000件瞬时完成，也不是用户大批性能或实物切膜验收。
- 相关29项通过，包括真实独立批次同订单编码不合并、18套/批日志分组、空目录继续、停止保留首批、部分无解不参与面积排名、历史重算同组、工作线程不在GUI、运行中关闭请求停止后正确清理、两种引擎完整双面三段输出像素与通道、版本和250行限制。查看真实批量窗口截图，未重跑无关全量；已知保守归组预览超时仍未修复。首次界面测试暴露忙检查将QObject.thread方法误当任务，修正为QThread.isRunning；测试App持有生命周期同步修正，初次组合测试崩溃后上述完整相关集重新通过，不把初次失败隐去。

## 68. 2026-09-13：开发者模式收纳数据工具

- 用户要求将用膜历史和批量分析归入开发者模式。主界面底部增加“开发者模式”开关，默认关闭，明确保存选择；关闭时隐藏两个入口，直接调用打开方法也不会创建工具或读取日志。开启后显示入口，关闭后已创建历史标签页同步隐藏，不让隐藏工具残留在日常界面。
- 这是工具入口可见性控制，不修改生产参数、不删除已有日志，也不关闭生产任务原有自动数值记录。主界面排版、进度、总结和预览继续正常使用。
- 用户再次强调批量分析没有最终文件：只扫描源图、计算18套数字方案、保存历史与显示汇总，绝不调用最终PNG渲染/保存。CSV仅在主动导出时写入，不能把数据分析当作已生成可打印文件；批量窗口标题和说明标明不生成最终文件。
- 批量任务未清理时禁止关闭开发者模式，主窗口任务检测也包含开发者批量任务，防止退出、更新或恢复默认设置时销毁活线程。运行窗口自身关闭继续采用先停止、后清理规则。
- 源码0.1.111，固定日期2026-09-13、当日第50次更新；只推送源码，无新Release安装包，不修改既有排版算法或做实物验收。
- 相关27项通过，覆盖默认隐藏且禁止直接打开、不启动历史读取、模式选择持久化、已有历史页隐藏、运行任务阻止退出与关闭模式、批量停止与线程清理、源图目录无新增PNG、日志及CSV、更新/恢复默认保护、版本与250行限制。查看真实默认关闭模式的主界面截图；未重跑无关生产算法全量，旧保守归组预览超时未在本次处理。

## 69. 2026-09-13：普通模式只比较45/60厘米

- 用户明确普通模式只需要45cm、60cm，其余40/50/55/65/70/75/80cm全部归入开发者模式。普通任务只计算4套常规/允许旋转方案，不仅隐藏多余行，也不消耗时间计算参考膜宽；低层LayoutSettings默认亦为普通范围。
- 开发者模式开启后，新任务扩展至9种膜宽18套；开发者批量分析明确启用参考范围。模式切换即时筛选已有表格数据且调整表格高度，不读取图片、不自动重新分析；旧数据若只有4套，新增规格要下次显式分析才有结果。切换不修改生产膜宽或旧历史数据。
- 比较设置名称与报告范围显示真实45/60或40–80范围。普通显示的面积最省高亮按当前可见方案重算，不能拿已隐藏参考宽度作为普通表格比较基准。历史保存实际已计算的方案数量，保留过去完整开发者结果。
- 源码0.1.112，固定日期2026-09-13、当日第51次更新；只推送源码，无新Release安装包，既有输出刀位、订单/双面/尺码和实际像素验证规则不变。
- 相关35项通过，覆盖普通数值计算仅4套、开发者18套、已有18套缓存即时过滤并还原且不改历史原值、GUI设置随模式同步、分组历史/CSV及停止、两种引擎完整双面三段输出真实像素/刀位、异常不阻塞、版本和250行限制。已查看普通模式实际截图，仅45/60四行；未重跑无关全量，已知保守归组预览超时未在本次修复。

## 70. 2026-09-13：统一文件夹选择默认位置

- 用户发现单文件夹、多文件夹打开位置不同并要求修改。统一共享最近浏览目录，单选、多选、添加上级目录都从该位置打开；首次没有共享值时回退当前生产目录或上次source_location，失效目录跳过，不把空字符串当成当前工作目录。
- 单选成功记住所选目录，多选记住所选批次共同上级目录，添加上级目录记住该上级位置；不同Windows驱动器没有共同父目录时回退首个批次父目录。取消不修改记录，退出重开仍恢复，恢复默认设置会清除该路径记忆。
- 浏览位置和生产输入分别保存，开发者批量选择不会替换主界面正在使用的生产文件夹、触发旧批次扫描或更改输出位置。多选仍用支持扩展选择的非原生窗口，不把界面样式差异误称为读取路径规则不同。
- 源码0.1.113，固定日期2026-09-13、当日第52次更新；只推送源码，无新Release安装包。本次不修改排版算法或降低任何生产安全检查。
- 相关20项通过，覆盖单选、多选与上级目录默认位置同步、跨窗口持久化、取消不更新、失效路径回退、批量选择不改生产源、切膜机输出容器跳过、开发者任务线程/停止、恢复默认及版本/250行限制。不重跑无关生产算法全量，不代表Windows原生选择窗口实机验收。

## 71. 2026-09-13：只保留添加上级目录批次

- 用户明确不需要添加多个文件夹，批量数据分析只保留“添加上级目录中的批次”。移除多选目录按钮、Qt非原生多选窗口和已经无用途的共同父目录记忆函数，不保留第二套路径选择界面。
- 与单批次一样使用系统原生目录选择窗口，共享最近浏览位置。选择后列出直接子目录批次，跳过“切膜机文件”，可检查/移除并去重；取消不改路径，批量选择不替换当前生产源。
- 仍仅限开发者模式、独立批次数字分析、不生成最终PNG、历史日志和分组汇总、安全停止及线程保护不变。没有新增“目录中直接有图片自动排版”行为，不能因选择上级目录而误触发生产输出。
- 源码0.1.114，固定日期2026-09-13、当日第53次更新；只推送源码，无新Release安装包，不修改排版算法。
- 相关20项通过，覆盖上级目录原生选择调用、移除多选入口/方法、默认路径与取消/持久化、源目录不变、批次去重/停止与线程清理、开发者模式、历史日志、版本和250行限制。未重跑无关生产算法全量，不代表Windows选择窗口实物交互验收。

## 72. 2026-09-13：批量目录主动并行

- 用户要求独立目录默认并行，不应每次提醒。批量分析默认同时4批，可选择1–8批，实际并发不超过输入批数；选择持久化，恢复默认设置清除该并行参数。以后独立任务应主动评估并发，不将线程数量等同速度承诺。
- 用户明确要求滚动补位：任意一批完成并保存后立即调度下一批，不等待同组其他批次完成。调度采用FIRST_COMPLETED；新增两路三批测试，故意阻塞第二批，验证第一批完成后第三批能在第二批结束前启动。此为0.1.115现有行为的回归保护，不另改生产算法或发布编号。
- 批次级使用有界线程池，只保持并发数个在途任务，停止后不再提交后续目录；完成批次立即独立持久化，保留停止前结果，异常目录继续。每批订单、双面和尺码归组独立，不合并不同目录，不生成最终PNG。
- 每批方案并发随批次并发预算降低：1批最多4路、2批每批2路、默认4批每批1路；主动选择8批时每批1路。普通单批生产仍默认4路，不引入4批各4路叠加。历史记录保存实际批次并发。
- 界面显示并发批次数及列表各批阶段/计数；列表路径单独存储，进度文字不会污染重跑输入。后台只发信号，Qt界面操作仍在主线程，任务结束后清理线程。
- 源码0.1.115，固定日期2026-09-13、当日第54次更新；推送源码，无新Release安装包。并发正确性验证不等于Windows大批性能或实物切膜验收。
- 相关25项通过，覆盖真实4批同时运行及每批1路预算、并发停止不提交剩余目录、SQLite独立保存、不生成PNG、Qt工作线程清理、原生目录默认路径、两种引擎整批双面三段像素与刀位、版本及250行限制；未重跑无关全量。

## 73. 2026-09-13：普通模式多批次生成

- 用户明确普通模式多批次读取就是各自生成最终排版文件，不是仅分析。新增普通模式“多批次排版”入口，选择上级目录下的直接子批次，沿用原生窗口、最近浏览位置、排除切膜机文件和去重规则；不要求启用开发者模式。
- 生产与仅分析共用有界滚动调度：默认同时4批，可选1–8批，任何一批完成并保存后立即补下一批。每批复用完整GenerateWorker流程，独立生成PNG、manifest及统一排版报告，不混合订单，保留订单/双面/尺码/刀位及源像素安全检查。
- 默认输出在各批同级“切膜机文件”中，任务目录及文件名保留批次名称；自定义输出沿用用户设置，任务编号包含随机后缀避免同秒碰撞。每批从头计序号，各批使用同一份启动时打印参数快照，不在计算期间改变参数。
- 批次级并行时每批分段串行保存，图片准备线程按批次数分配、比较几何线程同步降低，避免套娃线程池；分段数量及图像安全仍按用户参数。单批入口原有并行保存不受影响。
- 独立批量窗口显示各批阶段和进度，可选择批次显示真实整批预览，支持缩放及最大化，切换时不显示旧批预览。完成展示各批输出路径、省膜和各阶段耗时，完整报告在输出目录。不自动打开多个输出目录或多次弹窗阻塞队列。
- 安全停止不提交未启动目录，保留已完成文件，失败批次显示原因后继续。主窗口退出、更新及恢复默认设置包含普通批量任务检测；QObject在同一池线程创建/执行/释放，所有预览与控件更新由GUI排队信号处理。
- 源码0.1.116，固定日期2026-09-13、当日第55次更新；推送源码，无新Release安装包。Windows大批性能和实物切膜仍需验证。
- 相关27项通过，包含两种引擎各3个完整12张双面批次的最终PNG、manifest、报告、逐张源非透明像素及整批刀位验证，普通入口与运行退出保护、真实12张批次预览、滚动补位/停止、开发者门控、版本及250行限制。未重跑无关全量，不把小批并发验证当成Windows大批性能验收。

## 74. 2026-09-13：图片及嵌套线程治理

- 用户要求一并处理图片处理的线程问题，不只批次队列。图片准备、膜比较、旋转比较、分段保存的单路路径直接在当前工作线程执行，不再额外套单线程池；多路路径保留有界线程池并命名便于诊断。旋转比较遵守批次分配的film_geometry_workers预算，独立单批仍默认两路比较。
- 图片旋转后立即关闭原始解码图；准备生成器负责统一关闭交付图像，合成异常/停止时关闭大画布，并等待图片池结束、回收未使用图片。保存监测线程结束时必须join，不以1秒超时留下仍访问工作对象回调的线程。
- 保存进度按字节计数，可能超过Qt int的32位范围。生产工作信号、GUI桥接、主界面槽和普通批量桥接统一使用Python object承载计数，保留5GB等真实值，不截断、不溢出。Qt控件仍只由GUI排队槽更新；不改变生产刀位/订单/双面安全及最终像素检查。
- 源码0.1.117，固定日期2026-09-13、当日第56次更新；推送源码，无新Release安装包。此为具体线程及资源问题修正，不能宣称所有Windows异常退出或性能问题已消除。
- 相关回归覆盖单路无嵌套池、合成异常画布及在途图片释放、旋转解码图关闭、保存监测join、5GB计数排队传递、两引擎整批/分段源像素和刀位、滚动补位/停止、Qt线程生命周期、阶段计时和版本/250行限制。初次新增旋转预算测试遗漏真实订单分析输入导致KeyError，改用analyze_batch真实报告后重测，不修改生产归组接口。
- 最终相关47项通过，未重跑无关全量，未生成用户当前生产批次；Windows机器与大批性能仍待实测。

## 75. 2026-09-13：独立图片测量并行

- 用户要求研究可并行阶段并直接实现。现有批次滚动并行、数字方案比较、图片准备和分段保存保留；新增每图尺寸/DPI、膜标签、刀码/文字透明空间和常规/旋转占位的整体并行测量，最多4路，受图片准备线程参数及多批次分配预算限制。少于4图或线程参数1直接串行，避免小任务线程开销。
- 每张图片的两方向共用该线程唯一解码图，完成即释放；最多并发数个在途任务，停止不提交后续图片、等待工作线程释放资源。ContextVars保留统一批次时间/几何缓存，解码SOURCE仍线程隔离，结果按输入顺序汇总并保留原序号、标签和人工旋转映射；打印尺寸及DPI来源继续显示。
- 整批订单/双面/尺码顺序、最终统一刀位、单张最终画布写入仍必须有序或统一协调，不将它们拆成任意片段独立排版。不同批次及输出段可并行保存；同一PNG不能由多个线程任意交错写入。
- 开发电脑12张测试图放大5倍（约1250×1500像素，127DPI保持物理尺寸），常规与旋转测量各跑4次、清纸卡坐标缓存、剔除首轮，后三轮中位数：串行0.243秒、4路0.135秒，约44%下降；全部几何与标签相同。这是测量小批测试，不是整批生成加速44%，不保证Windows实测倍数。
- 源码0.1.118，固定日期2026-09-13、当日第57次更新；推送源码，无新Release安装包。色块初始定位抽到独立模块，所有代码保持250行内。
- 相关49项回归通过，并再次整组重跑；包含四路屏障、序号/几何/标签与串行全等、停止不排入全批、线程清理、两引擎整批/分段真实源像素和刀位、滚动补位及Qt大计数。初次扩展组合测试曾以132退出，macOS报告显示libvips线程池/缓存构建栈；随后单独12项及整组49项通过，未确定原生退出根因，不能宣称该退出已修复。初次代码文件255行，抽出色块定位模块后行数检查通过。

## 76. 2026-09-13：范围扫描与标签测量捷径

- 用户要求优化测量，并强调记录刀位范围与后续范围比较。刀位等价状态去重改为每图左列可行下界、右列可行上界的事件扫描，跨候选只更新越过的边界，不再每候选遍历全部图片。保留原候选集合、同状态靠近中间的代表及完整订单DP；不把多个可行区间强行压成一个，也不擅自拆订单。
- 1000张数字测试的候选去重：原逐候选逐图片0.1141秒，边界扫描0.0017秒，候选完全相同；仅此阶段约67倍，不等于整个排版67倍，最终DP仍计算各非等价候选的实际高度。
- 平台文字先查膜标签旁优先空位，可用即返回，才在必要时搜索整条横向标签空间；候选优先顺序、3像素源重采样扩边、全部非零alpha和安全外置回退不改。文字字体按线程独立LRU缓存最多32字号，同线程重复字号复用，不跨线程共享FreeType字体面。
- 刀位范围判断与源/输出像素检查不是同一功能。范围证明几何分区可行；透明区域验证证明刀码/文字不会覆盖图案；输出检查证明合成位置/旋转实际正确。重复矩形检查已有批次内缓存，最终输出检查保留，不用范围结论跳过真实通道核验。
- 源码0.1.119，固定日期2026-09-13、当日第58次更新；推送源码，无新Release安装包。本轮首次组合测试亦出现132原生退出，随后含同一输出测试的28项组合通过；沿用第75节未定位状态，不宣称原生退出修复。
- 扩展组合又一次132退出。发现普通批量生成测试在创建QApplication前构造Qt工作对象，改为模块初始化时在主线程创建并持有App，保证测试环境满足生产GUI前提；重跑52项通过。不能仅据测试App修正就推断原生退出根因已经定位。范围状态新增20组含不同刀码偏移/内置偏移的全候选旧实现对照；仍验证两引擎整批/分段实际像素安全、标签空位捷径、字体线程隔离和250行限制，未做Windows实物验收。
- 最终52项组合连续两次通过（13.72秒、9.64秒）；不重跑无关全量，不宣称测试电脑性能或原生退出问题已验收。

## 77. 2026-09-13：开发者算法开销页面

- 用户要求各步骤算法开销在开发者模式可查看。新增“算法开销”入口及批次详情下独立页面，默认隐藏，直接打开亦受开发者开关约束；关闭模式隐藏已创建页，不加载历史或原图。
- 按步骤列理论复杂度、规模变量、重复工作原因及优化现状，明确单件配对O(n²)、整体刀位O(k×n²)、旋转待配对状态增长、总结O(n²)。不得用局部刀位去重67倍表述整个算法高效；大整数位集合操作/哈希有随图片数增长的成本，不能当作严格常数。
- 同页展示已有主界面批次规模及实际阶段耗时，区分理论上界与实测。没有记录的实际k不猜测，批量独立窗口全部任务不冒充主界面当前任务；不会因打开页面扫描、解码或生成图片。文字及表格可复制，提供复制整份开销与耗时按钮。
- 源码0.1.120，固定日期2026-09-13、当日第59次更新；仅源码推送，无新Release安装包。本轮不修改排版算法，不宣称第75/76节原生退出已修复。
- 相关界面/门控/计时/版本/行数12项及原有整批两引擎真实像素/刀位与范围去重7项通过。已查看算法页面截图，补充选中步骤完整说明，避免表格窄列省略文字影响阅读；页面可见时每秒刷新已有工作计时，不重算排版。

## 78. 2026-09-13：统一批次输入与操作层级

- 用户要求批次详情不要与读取文件夹并排，单批次和多批次输入放一起并改善界面。新增“批次输入”父区域，蓝色单批次卡片含选择目录/开始排版，绿色多批次卡片含独立滚动排版入口；说明、图标、40px按钮及留白明确区分两种工作方式。
- 批次详情、停止和开发者工具移至下方排版数据区域底部，输入行不混入检查工具。日常标签字段、数据、真实预览及底部打印参数入口保留，开发者工具仍默认隐藏。复用既有点击流程，不改读取、生成、线程调度及安全算法。
- 源码0.1.121，固定日期2026-09-13、当日第60次更新；源码推送，无新Release安装包。界面层级验证不等于Windows展示或生产机器验收。
- 相关12项通过，覆盖单/多输入卡片共同父区域、详情与停止的下方数据区归属及实际纵向位置、开发工具默认隐藏/门控、算法页、版本与250行限制，及两种引擎多批次整批真实像素和刀位。已查看实际主窗口截图；未重跑无关全量，不宣称此前原生退出已修复。

## 79. 2026-09-13：选择即排版，停止回到排版旁

- 用户纠正停止按钮必须与批次排版操作在一起；单批次卡片中主按钮与停止并排，停止不再放在数据区。详情与开发者工具继续在下方，批量窗口自身停止仍与批量排版并排。
- 主界面单批次默认合并为“选择文件夹并开始排版”：原生选择成功立即调用完整生成流程，不再选择后另点开始；取消绝不使用旧路径生成，有活动任务时不打开目录选择或重入生成。兼容manual_layout_button引用同一主按钮，主界面不保留第二个重复选目录按钮。
- 打印参数内原有目录浏览及生成仍保留用于检查/复用；多批次窗口继续选择上级目录、确认队列及并发参数后滚动生成，不在此次隐式跳过队列确认。默认浏览位置、参数持久化、订单/双面/尺码及真实刀位/像素安全不变。
- 源码0.1.122，固定日期2026-09-13、当日第61次更新；仅源码推送，无新Release安装包。保留未定位原生退出状态，不宣称Windows生产验收。
- 相关16项通过，覆盖选择成功立刻调用生成、取消不重用旧路径、忙时不选择/重入、单批次主按钮唯一及停止同卡片、详情下方、开发门控、最近目录/取消路径持久化、版本和行数，及两引擎完整多批次输出像素/刀位。未重跑无关全量。

## 80. 2026-09-13：精简操作与打印设置分层

- 最新要求覆盖第78/79节输入卡片：主界面批次操作只保留“单批次、多批次、暂停批次”三个按钮，详情与开发工具仍在数据区下方。单批次选择后直接生成，多批次保留上级目录与队列确认，不重复展示并发参数。
- 打印设置按“膜的设置 → 排版规则 → 标签与文字 → 输出与并行”分类，一次只展示所选类别；复用现有控件、信号和保存逻辑。膜规格及RIIN预留归膜设置，刀位/旋转/间距归排版规则，完整标签与色块子界面归标签分类，保存/分段/线程及同时批次数归输出与并行。
- 新配置与恢复默认后的图片线程、并行保存、批次并行默认4路；原来保存的选择继续保留。实际并行不超过任务数，多批次仍分配每批预算并滚动补位，不创建4批各4路的嵌套并发。两方案旋转比较不凭空创建4个任务。
- “暂停批次”当前明确为协作安全停止，保留已完成文件，不承诺断点续跑；多批次停止不再启动队列余项。
- 源码0.1.123，固定日期2026-09-13、当日第62次更新；仅推送源码，无新Release安装包。相关15项通过，覆盖四分类控件归属、间距模式联动、参数持久化、新默认与恢复默认、生产窗口隐藏并发参数、三按钮层级及两种引擎完整多批次真实PNG的刀位/源像素安全。已查看实际设置截图；未重跑无关全量，不宣称Windows生产验收或旧原生退出已修复。

## 81. 2026-09-13：不同批次图标与主界面仅预览

- 用户要求按钮保留“排版”二字，并明显区分单批次与多批次图标。统一样式之前按文字重设图标，使两按钮都落入默认more图标；新增单张排版页与多张叠页图标及明确映射，样式应用后亦保持区别，单批次仍为高亮主操作。
- 主操作区为“单批次排版、多批次排版、仅预览排版、暂停批次”。仅预览使用当前有效目录，无有效目录才调用共享原生选择；取消或有活动任务不生成。复用真实preview_only流程，扫描/计算/展示整批与用膜信息，不合成最终画布或创建打印输出目录，不把预览当作像素验收。已有成功预览的用膜历史保存规则保留。
- 单批次生成或预览运行中禁用单批、多批及预览入口，结束后恢复，暂停按安全停止语义保留。打印参数分类与已保存参数不变。
- 源码0.1.124，固定日期2026-09-13、当日第63次更新；仅源码推送，无新Release安装包。相关12项通过：样式应用后图标像素确实不同、按钮名称/归属、当前目录与首次选择/取消/忙时预览、禁止预览合成或创建输出、两引擎完整多批次真实像素与刀位、版本及250行限制。查看实际主界面截图，未重跑无关全量或做Windows实物生产验收。

## 82. 2026-09-13：统一单多批次工作台与仅预览勾选

- 用户明确纠正第81节：仅预览不是独立按钮，而是“仅预览，不生成文件”勾选项，对单批次、多批次共同生效；主操作区仅三个按钮，打印参数分类保留。启动时快照该选择，活动任务期间禁用修改；选择自动保存，新配置默认不勾选。
- 多批次不打开额外窗口、不要求再次添加目录/点击开始。原生选择上级目录成功后，直接把其子目录批次交给滚动队列，排除切膜机文件；取消或忙时不执行。原普通生产弹窗不再属于主入口。
- 同一主界面展示选中批次的进度、当前文件、步骤耗时、订单分析、真实预览、用膜比较和总结；数据区内仅增加批次状态下拉切换。缓存只保留各批数值数据和路径，不保留整批像素；切换先清旧图再安装所选结果，未完成批次不借用旧预览。开始单批次时隐藏多批次切换项。
- 多批次仅预览沿用GenerateWorker的preview_only路径，不合成最终画布、不创建切膜机输出目录、不保存PNG/manifest/排版报告；仍可按原规则保存用膜历史。正常多批次继续独立生成完整文件。公共暂停控制直接请求当前队列安全停止，保留已完成文件，不再启动后续批次。
- 主窗口活动任务检测包含新队列控制器，关闭/更新/恢复默认不能销毁活线程；工作信号排队交付GUI，结束后回收QThread。逐批异常在批次状态及主数据警告、日志可查，失败批次不阻塞剩余队列。
- 源码0.1.125，固定日期2026-09-13、当日第64次更新；仅推送源码，无新Release安装包。相关15项通过，另切换扩展3项通过：三个主按钮与勾选、单批勾选传递/取消/忙时、多批原生选择即执行且无弹窗、三批仅预览无输出、真实主界面两批预览切换与已完成计时/线程清理、两种引擎完整多批次PNG源像素/整批刀位、参数分类和版本/250行限制。查看实际主工作台截图；未做Windows实物验收，未重跑无关全量，不宣称已知原生退出已解决。

## 83. 2026-09-13：多批次选中目录显示同步

- 用户明确单批、多批都只选择一个文件夹；多批选择的是包含子批次的上级目录。选择成功立即在主界面高亮显示“已选择多批次目录：名称 · 完整路径”，当前子批次只在下方数据区切换，不替换输入根目录显示。
- 输入身份显示抽为共用函数，记住根目录和单/多模式；取消不改记录，重启仅恢复显示而不扫描。单批选择使用同一显示函数，即使选择路径与此前单批路径相同，也清除旧多批显示身份。恢复默认通过layout组一并清除新记录。
- 多批选择根目录不写入单批生产源控件，避免原有folder.textChanged触发上级目录的错误扫描；生产队列继续使用子目录独立输出，单批源与多批输入身份内部区分。未改排版、输出刀位或像素验证算法。
- 源码0.1.126，固定日期2026-09-13、当日第65次更新；仅源码推送，无新Release安装包。相关12项通过：立即高亮、取消/重启保留、相同路径切回单批、两批切换仍显示根目录、仅预览无输出/线程清理、两种引擎完整多批PNG实际源像素/整批刀位、恢复默认及版本/250行限制。查看实际主界面根目录高亮截图；未做Windows实物验收，不宣称旧原生退出已修复。

## 84. 2026-09-13：暂停与主窗口关闭直接退出

- 用户最新明确要求覆盖此前等待当前文件写完的协作停止规则：点击主界面“暂停批次”或关闭软件主窗口，保存参数后直接终止应用进程，不等待QThread、线程池、PNG编码或预览线程完成，不使用危险的QThread.terminate。暂停目前含义就是退出软件，不是保持窗口/断点续跑；打印设置窗口单独关闭仍仅关闭设置。
- PNG编码写入“原名.png.未完成”，完整编码返回后才改成PNG；中途退出不会留下编码残缺却以.png结尾的文件。保留未完成文件，不自动删除；再次保存同名时避开已有未完成文件，防止覆盖恢复材料。
- 每个正常生产批次工作前写“批次未完成，禁止打印.txt”，所有PNG安全检查、分段、manifest、排版报告和历史保存处理完成后才移除。编码后复核或多段中途退出时，此提示仍在：本目录全部禁止打印，应重新生成；已完成其他批次没有此提示，不受影响。仅预览不创建输出目录或此标记。
- 退出采用进程级结束而非跨线程销毁QObject，生产写入保护在退出前就存在；不在关闭时遍历/改名正在写入的文件，避免Windows打开句柄阻止退出。开发监视器正常退出码不会重启应用。
- 源码0.1.127，固定日期2026-09-13、当日第66次更新；仅源码推送，无新Release安装包。首轮19项通过，扩大组合再次132原生退出；随后退出/版本/行数7项、生产/分段/预览/开发模式/恢复默认16项分别通过。真实子进程在保存线程永久阻塞时仍以0退出，保留参数、未完成编码和批次警告；两种引擎整批源像素/刀位与分段安全回归通过。仍未定位旧原生退出，不宣称其修复或Windows实物生产验收，未重跑无关全量。

## 85. 2026-09-13：直接展示三组批次状态

- 用户要求选中多批次上级目录后直接展示子批次，不再用下拉框。主数据区增加并列“已完成、进行中、未完成”三组列表，带组标题图标、数量、每行状态图标；直接列出子文件夹，继续排除切膜机输出目录。
- 每个进行中批次独立显示当前执行步骤和实际计数，保存阶段显示已写字节；完整文件名/路径放在悬停说明及选中批次的主详情。等待/失败批次归未完成，失败显示原因；仅预览完成明确写批次预览完成，不冒充打印文件生成。
- 点击任一行共用主预览/耗时/总结；批次跨组迁移保留选中对象，不自动切走用户正在检查的批次。新目录清空旧状态，空目录亦清旧预览，单批模式隐藏三组列表。每组超出可视高度时可滚动，不漏掉队列后续目录。
- 源码0.1.128，固定日期2026-09-13、当日第67次更新；仅推送源码，无新Release安装包。相关11项通过，另原有完整输出与预览6项通过：六批可见/四批独立进行中、完成和失败归组/图标/计数、跨组不改变选中、点击未完成、重置、主界面两批预览切换、输入根目录高亮及立即退出保护、两种引擎真实整批源像素/刀位、版本与250行限制。查看三组实时状态及真实主窗口截图，未做NAS/Windows实物生产验收，旧原生退出仍未定位。

## 86. 2026-09-13：多文件重复查询与几何计算优化

- 用户要求实际优化多文件效率，而不是单纯增加线程数。任务内每个文件的元数据/绝对路径共享一次查询，不同文件仍可并行；计算结束重新查询验证源文件未改变，生产输出的独立源alpha与全批像素通道检查保留。
- 膜标签坐标新增批次内缓存（含未找到结果），避免512张全局LRU在大批次或多批并行时淘汰后重复搜索。仅保留坐标、尺寸、判定，不缓存整批原图；任务结束释放。
- 同尺码单件配对按高度、图片相对顶部、旋转与左右可放置状态建立等价桶，只检查每桶首个有效成员。保持原贪心评分和平局次序，不拆多件/双面、不跨尺码。配对为O(n×b)，b为同尺码等价桶数，全部几何不同仍最坏O(n²)；开发者算法开销同步更新，不声称全局最优。
- 同膜宽常规结果与实际刀位只算一次，允许旋转方案复用该基准；无常规解仍允许安全整单旋转。正常/失败Future都交付等待者，先调度各膜宽常规计算避免等待者占满线程池。每套方案仍独立执行安全检查，不自动替用户选择规格。
- 1000张相同尺寸的纯数字配对，五次中位数：旧贪心0.1564秒，新索引0.00137秒；评分次数约25万次降为500次。这是单个配对阶段基准，不代表整批、NAS或Windows提速114倍。50组随机几何/旋转/尺码/偏移与旧配对全等；18套小批膜数据与独立重算全等。
- 源码0.1.129，固定日期2026-09-13、当日第68次更新；推送源码，无新Release安装包。相关36项及另外20项通过，包含批次缓存、并行元数据、源变更拒绝、四路比较、滚动多批、两引擎整批/分段真实源像素及完整刀位、随机刀位对照、旋转组合、开发者复杂度、版本和250行限制。未做用户当前NAS大批实测，不宣称旧原生退出已修复。

## 87. 2026-09-14：批次总结线性索引

- 用户指出蜂鸟ERP单件尺码群与多件订单归组不应重复全批扫描。finish_analysis原本逐订单遍历planned，并在内层重复建立订单图片集合，确有不必要的平方级开销；改为一次建立图片归属和区域/行订单索引，再汇总同行伙伴。
- 固定双列每行至多两个订单，批次总结O(n)，图片列表仅遍历两次，不随订单数增加全批扫描次数。伙伴按实际生产行首次出现顺序展示，取消非必要的字母排序；同尺码说明改为不跨尺码搭配，订单、双面、刀位与输出像素检查不改。
- 一般自由排版的总结为O(n+E)，E为实际需要输出的同行订单关系数；若单行有大量不同订单且要求逐订单列出所有伙伴，输出本身可为平方级，不将此例冒充固定双列生产成本。订单归组可用哈希索引期望O(n)，任意未排序数据的比较排序与最省膜搜索不承诺严格O(n)，开发者页面分别说明，不能用总结线性声称整个优化器线性或全局最优。
- 1000/2000/4000张纯数字双列总结，预热后各五次中位数约0.00701/0.01222/0.02337秒；测试精确计数1000与4000张均仅2n次planned访问，混合多件与区域同行伙伴集合和旧定义一致。这不是NAS或整批生成性能验收。
- 源码0.1.130，固定日期2026-09-14、当日第01次更新；仅推送源码，无新Release安装包。相关19项通过，包含总结、新旧同行关系、批次分析、18套比较、两引擎真实全批/分段源像素及刀位、复杂度页面、版本和250行限制；跨日版本元数据及分析界面另8项通过。旧原生退出仍未定位。

## 88. 2026-09-14：单件双排优先与线性旋转筛选

- 用户要求逐项提升性能，单件同尺码不按任意排列优化；只有不能并排才比较旋转，已经安全双排的优先保留。新纯单件路径先复用常规整批刀位及实际排版，取消该路径逐刀位完整旋转组合DP；混合/多件继续原完整订单优化器，本次未改。
- 按实际图片顶部同高判断双排，不能仅按row_y_px判断：双面纵排也可能共用同一单位row_y。已有双排的完整尺码块保护，未保护的末尾单排尺码块才比较旋转。一个块有双排也有落单时，整体保留常规，不能只旋转落单破坏同尺码不拆区；较小尺码不能跨过后续被保护的大尺码进入旋转区。此生产策略不声称全局材料最优。
- 筛选、累计正常前缀高度与旋转后缀高度、选中后一次构建实际坐标为O(n)；实际需要的旋转测量像素成本另计。普通旋转排版只测量可选尾部，默认大尺码末尾策略复用同一模块并保留3XL及以上限制。共享膜规格比较仍共用两方向测量，因为不同宽度下双排可行性不同；四/十八套数字方案都采用新的纯单件决策，不自动换膜。
- 双面与完整订单仍纵向相邻进入旋转区，每行一张，各区固定刀位；包含区域间距、留白和末尾红线后不省膜则原样返回常规。只更新选中旋转图片的标签内容，不能用其他膜宽/方向标签覆盖正常图片。步骤显示单件旋转筛选，开发者开销区分纯单件线性筛选与多件旧DP，比较范围文本标明双排优先策略。
- 1000张重复尺寸可双排纯数字图，五次中位数：原旋转搜索约0.01428秒，新筛选约0.00164秒；这是旋转决策阶段，不含常规刀位、读取图片、合成或保存，不承诺整批8.7倍。测试验证千图无旋转候选、已双排细长双面即使旋转可能更短也不测量、仅测量完整单排尾部、多件路径不受改动。
- 源码0.1.131，固定日期2026-09-14、当日第02次更新；仅推送源码，无新Release安装包。相关38项通过，含12张完整混合双排/单排尾部在Pillow/libvips整图和3段输出中逐像素与原图全等、全高刀位验证、双面顺序、18套比较、真正两路多件比较、默认末尾策略、计时页面、版本与250行限制。首轮旧双面旋转测试仍使用可双排细长图而期待旋转，按新策略更换为无法双排的宽图，并新增原可双排图必须保留的测试；另修正把纵排同单位误判双排的问题后相关回归通过。未做用户当前Windows/NAS生产批次实测，不宣称旧原生退出已修复。
- 比较范围文案补充后，相关12项及批量/计时/总结17项再次通过；源码推送不等于用户批次实物验收。

## 89. 2026-09-14：多层批次目录与对应输出结构

- 用户要求多层嵌套目录也展示文件信息，排版输出保留对应文件夹结构。多批次排版选择一个最上层目录，后台scandir逐目录递归，不在GUI同步遍历；显示已扫描目录数与当前路径，未知总量不伪造百分比。
- 每个直接含支持图片的目录作为独立批次，只处理本层图片，不再次递归合并其子批次。根目录本身含图也处理；空目录不作为失败批次。跳过各层“切膜机文件”及目录/图片符号链接，防止输出回读和循环；无法读取的分支记录错误，继续其他分支。不同目录的同名文件和订单编码不擅自合并。
- 已完成/进行中/未完成三组显示批次相对路径与图片数量；悬停可看完整目录及文件名，选中尚未有排版数据的批次也能在图片列表查看本批文件名。独立状态、预览、耗时和总结仍按批次索引对应，输入根目录高亮不被子批次覆盖。
- 默认输出为“所选根目录的父目录/切膜机文件/所选根目录名/原相对路径/原批次名_JOB时间与唯一标识/PNG、报告和manifest”。例如HL/黑色/大码/批次A对应切膜机文件/HL/黑色/大码/批次A/本次任务目录。自定义保存根目录同样保留HL及后续相对层级；保留每次独立任务、不覆盖、未完成禁止打印保护。仅预览完全不创建输出目录。旧输出不自动移动或删除，原单批次读取语义暂不改变。
- 扫描结果的直接图片列表交给完整生成器，不重复rglob扫描、不将父子图片重复生成；批次滚动并行与每批线程预算不变，订单/双面/尺码及真实源/刀位像素检查保留。
- 源码0.1.132，固定日期2026-09-14、当日第03次更新；推送源码，无新Release安装包。初轮8项中2项暴露扫描前选择空列表及未初始化inventory，修正后相关14项通过；扩展组合再次以132原生退出，随后嵌套8项、原批量/UI/版本/行数11项分组通过。覆盖根层与两级/三级同名批次、36张不重复、两个引擎默认/自定义镜像输出、完整刀位验证、仅预览无文件、GUI外扫描、计算前文件名显示、不可读分支继续与取消。未定位此前原生退出，不宣称其修复，也未做用户Windows/NAS当前目录实测。

## 90. 2026-09-14：跨重启持久化整批排版缓存

- 用户要求相同文件再次排版不要重复计算。增加本机logs目录“排版缓存.sqlite3”，自动保存通过订单与刀位几何检查的完整排版、标签文字/位置、尺寸报告、整批分析和四/十八套用膜数据；JSON加完整性摘要，不使用可执行pickle、不存原图或大PNG。新版本首次显式排版/预览建立缓存，重启后继续有效；开机不读取旧批次。
- 键包含输入顺序及绝对文件身份（路径、修改时间、大小）、几何/标签/平台/机器/膜与RIIN等参数、软件版本。日期模板按当前实际格式化日期进入键，日期变化不能复用旧标签。图片新增/删除/改名/修改、影响排版的参数或软件版本变化自动失效；线程数、PNG引擎/压缩、分段文件数及保存预算不影响整批几何，改变这些可继续命中。
- 命中时先校验完整图片/订单/尺码和整批刀位，再恢复实际刀位、坐标及分析，通过原信号显示真实预览与总结。文件信息逐张检查有计数及当前文件；显示缓存命中、未命中、保存或不可用提示，主批次信息保留命中标识。缓存读取/写入独立计时；复用用膜结果的原计算耗时不冒充本次耗时，当前计算耗时置0并保留历史秒数。
- 命中只免去尺寸/标签占位/刀位与布局及膜规格重算，不免除文件新鲜度检查、独立源alpha、真实合成和保存后全批像素通道验证；再次输出PNG仍需合成保存。生成中源文件改变仍拒绝使用旧规划。只保存安全几何计划，不缓存失败或仅供检查的危险规划；缓存损坏/缺项/不安全/不可读/磁盘写失败，明确提示后正常计算，不把缓存问题变成生产阻塞。
- 多批与嵌套批次使用同一SQLite WAL缓存，各批独立键和坐标，短事务及显式关闭连接；不共用全批像素。已有输出不被覆盖、移动或删除，未完成保护与镜像目录结构不改。测试缓存目录逐项隔离，避免写入用户真实缓存；新进程测试显式读取父进程刚创建的数据。
- 源码0.1.133，固定日期2026-09-14、当日第04次更新；推送源码，无新Release安装包。缓存10项通过：新进程不重新计算/不打开源像素、四批并行同库、日期/版本/参数/文件失效、非几何参数命中、损坏与缺图回退、写入失败不阻塞、命中期间源变更拒绝、12张完整双面批次Pillow/libvips整图/3段源像素及全批刀位核验。另原相关28项通过；扩展含嵌套原生输出组合两次以132退出，分组缓存10项、非libvips嵌套6项、界面/版本/行数/计时9项及批量预览/总结/缓存14项通过。记录既有原生退出仍未定位，不宣称修复或全量通过，也未做Windows/NAS当前大批性能验收。

## 91. 2026-09-14：排版缓存24小时保留与测量算法说明

- 用户指定持久化排版缓存只保留一天。以每次保存时间为起点，24小时整即失效；命中不续期。下次访问缓存时通过时间索引删除全部过期记录，不在启动时扫描、不设置后台定时任务、不在生产任务中VACUUM。SQLite空闲页可复用，删除记录不代表数据库文件立即缩小。只删除排版缓存记录，原图、输出、用膜历史均不删除。
- 当前切膜路径不解码二维码内容。header_region在顶部宽度一半高度范围（至少96像素，不超过原图高度）裁图，缩至最多1000×1000，以alpha>=240、RGB各通道>=220构造白卡片掩码；连通域筛选靠左右且靠顶部的独立膜标签。OpenCV为可选连通域后端，缺少时采用逐行游程合并，病态掩码的相邻游程匹配存在额外开销，不能声称所有图案严格像素线性。
- 文字先格式化并生成实际字形占位；切膜模式按字号/刀码宽度，自由模式动态等高最多15次二分字号。平台文字先搜索卡片左右邻近透明矩形，失败再投影搜索整个对应高度带的空区。刀码在分区左边缘测试透明安全；旋转文字在卡片下方按1毫米步长寻找安全位置，候选矩形按共用条带聚合，alpha投影加前缀和一次处理，不逐候选重复扫描整个条带。透明检查保留原图映射及3源像素缓冲，不能以刀位几何替代半透明像素安全。
- 即使只裁顶部，Pillow裁PNG通常仍触发整张像素解压；首次测量成本包含源图读取/解压、白卡片定位、文字字形、透明条带/矩形扫描，而不是纯尺寸运算。正常与旋转共享单张已解压源，批内测量复用，膜规格数字方案不逐方案重读PNG；跨重启24小时缓存命中可跳过占位与规划，但真实生成安全检查、合成保存仍执行。当前没有用户Windows/NAS测量子步骤剖析，不把读取、定位、搜索之一冒称已实测主因。
- 源码0.1.134，固定日期2026-09-14、当日第05次更新；仅源码更新，无新Release安装包。本次仅修改缓存生命周期，不改标签/刀码算法。13项通过（2项libvips输出测试未执行），覆盖24小时边界、不续期、全过期记录清理、不删除其他文件、跨进程及四批同库、缓存安全回退与Pillow全批真实输出验证；另验证版本及代码行数。此前原生退出仍未定位，未做测试电脑大批性能验收。

## 92. 2026-09-14：输出文件名包含订单数量

- 用户要求输出文件名字带本批次多少单。保留原批次文件夹名称、手填标签、实际尺码/旋转区及段号，增加“批次N单”。按现有生产订单身份去前缀后去重，同订单多件、正反面只算一单，不把件数/图片数当订单数；计数一次哈希集合，期望O(n)，不读取图片像素。
- 分段PNG保留整批数量，另加“本段N单”，例如“609140634009_批次12单 本段4单 CY26 S-XL 第001段.png”。即使整单不能拆而最终只有一段，也显示真实整批和本段数量。不改变分段边界、线程或刀位。无法识别归属时显示“订单待核对”，混合输入仅标已识别订单数，不把未知安全归组假称真实一单。
- 生成、进度、结果记录和报告沿用真实文件名；旧输出不自动改名或覆盖，新生成采用新命名。源码0.1.135，固定日期2026-09-14、当日第06次更新；仅推送源码，无新Release安装包。21项相关回归通过，包含前缀/多件/双面去重、未知归属提示、整图真实命名与重名避让、两种引擎三段并行订单归属及全高真实刀位通道、不可拆单和失败隔离、版本及250行检查。本次只改命名，不改排版算法，未做Windows实物生产验收。

## 93. 2026-09-14：测量子步骤独立计时

- 用户要求标签/刀码测量分子步骤计时，不能凭总耗时断定瓶颈。保留透明检查：整批刀位范围只证明分区可行，原图透明检查证明内置刀码、普通标签与平台文字不覆盖图案/纸卡/半透明墨迹，两者不可互相代替。
- 当前任务记录尺寸与DPI文件信息读取、源图片像素读取与解压、膜标签卡片定位、普通标签文字测量、平台文字测量、平台透明空位搜索、透明候选条带扫描、刀码/标签透明矩形检查的耗时和真实调用次数。定位/搜索内部的首次Pillow像素load单独计时；不在开文件时无条件解压，不为计时增加整批像素缓存。
- 子步骤使用线程隔离嵌套栈，扣除嵌套子步骤耗时后线程安全累积；注明“线程累计独占耗时”，不冒充整批墙钟耗时，不把并发各线程相加到总计，也不以它计算总墙钟百分比。调用次数包含缓存查询/返回的实际调用，卡片定位LRU命中不进入定位正文。未覆盖的任务开销仍在原大步骤，不能声称子步骤完整覆盖所有耗时。
- 每次分析结果到达后主数据区显示可复制明细，整批报告与analysis.measurement_timings及持久化历史保存同一数据，新任务清除旧明细。当前不是每个活动子步骤实时推送，已有主步骤/图片进度仍持续显示。命中24小时排版缓存时本次未执行测量显示0秒/0次并注明缓存命中，原始测量记录另存cached_original_steps，不冒充本次。
- 新计时及栈追踪纠正此前“同一任务源图只解压一次”的过宽描述：12张冷缓存四方案测试有24次实际像素解压，12次发生于共享预处理的纸卡定位，另外12次发生于单图占位中的平台空位搜索；同一单图占位两方向共享源仍有效，但预处理与占位之间未共享已解压像素。本次如实计数，未修改这条重复读取路径，也未将其冒称Windows/NAS实测主因。
- 源码0.1.136，固定日期2026-09-14、当日第07次更新；仅推送源码，无新Release安装包。31项测量/缓存/并行/透明搜索/界面明细/版本/行数通过（2项libvips缓存输出未执行），另10项两引擎完整订单三段输出全高真实通道及大步骤计时通过。首轮新增测试预期12次解压失败、实际24次，核查调用栈后改为记录真实次数，不修改计时数据迎合假设。未改生产刀位、透明判据、排序或分段规则，未做Windows当前大批性能或实物验收，旧原生退出仍未定位。
- 最后单独运行新界面测试时因未在该模块配置离屏Qt平台，在创建QApplication处以134退出；此前组合依赖其他模块配置。补充本模块明确离屏配置后再次验证，此为测试运行环境修正，不宣称原应用原生退出修复。

## 94. 2026-09-14：已知空白膜标签行的短路搜索与两方向一次解压

- 用户确认原图二维码所在一行除标签内容外为空白，要求据此优化。平台文字优先只检查最靠近膜标签的第一个候选矩形，透明即可返回，不先处理所有水平平移候选；旋转后的普通标签优先检查膜标签下方第一个位置，安全且不碰平台文字即可返回，不扫描一直到图片末尾的全部纵向候选。若不安全，仍按原优先次序及1毫米步长搜索剩余候选，保留原图alpha、重采样3像素缓冲、边界及文字碰撞检查。
- 不能只从一个样本推出所有图片同一行均透明，纸卡/二维码/标签内容本身仍不可覆盖。因此捷径仍核验实际首个矩形，不删除独立源标记或最终输出全高通道检查；样本确认是数据特征，不是全批安全证明。
- 修正常规与旋转分开测量的重复解压：开启膜规格比较时，在常规切膜测量的同一个单图源作用域预先缓存两方向不可变占位，常规结果仍只取第一方向，不擅自启用旋转；后续四/十八套数字比较及旋转候选复用结果。独立膜规格分析同样主动预备两方向。关闭比较仍不全批预测旋转，纯单件尾部策略保持必要候选才测量；手动旋转与比较无手动方案参数不同，不能承诺所有设置都每图恰好解压一次。
- 进一步检查第93节24次解压的原因，实际与常规/旋转分两轮源作用域有关：一轮首次使用像素在卡片定位，另一轮首次使用像素在平台空位搜索，不能只依据栈顶不同就概括为所有卡片预处理都额外解压。无人工旋转12张冷缓存四方案测试，优化后真实像素解压为12次，仍执行两方向的必要占位测量；不是所有阶段包含输出验证的总解压次数，也不是整批耗时减半承诺。
- 当前用户此前两张B74F6NM实图已不在所给Downloads/609140634009路径，本机现有606111951001旧输入实际无膜标签，不能冒称其验证了标签行空白。用户截图仅可视觉参考，不能替代原PNG alpha检查。本次未验证用户当前生产源批次的空白特征，完整测试批次仍逐图/整高检查。
- 源码0.1.137，固定日期2026-09-14、当日第08次更新；仅推送源码，无新Release安装包。首轮22项含两引擎12张完整批次三段真实原图像素/精确通道与18套数字结果一致通过；新增短路、旋转/内置标记、尾部策略、并行测量、平台空间、版本/行数32项通过；另测量、持久化缓存、旋转区/末尾及膜规格21项通过（5项libvips测试未执行，该引擎已在前两组完整输出验证）。首次候选测试证明只检查1个矩形，旋转空白首位测试禁止调用整尾条带扫描仍通过。未做Windows/NAS大批耗时或切膜机实物验收，不宣称旧原生退出修复。

## 95. 2026-09-14：主界面刀码四种情况预览

- 用户要求刀码预览在主界面，并同时举例原膜标签在左/右，各自不旋转/向左90度，共四例，优先生产图。主预览区域增加“整批排版预览/刀码四种情况”两个视图，不增加第三个独立工作区。未读取批次时默认显示四例；排版结果到达后切回真实整批，四例仍可在同一主界面查看，不放进打印参数窗口。
- 四例不是蓝色图片框的人工位置示意，均调用生产read_items及相同文字生成模块，按实际源DPI和用户字号、色块尺寸/颜色、机器、平台、标签参数计算相对位置。切膜示例固定左基准，正常/向左旋转的膜标签坐标真实变换；左下膜标签的刀码仍左上，普通标签仍在标签卡片下方。保留棋盘透明背景及每例实际刀码/普通标签距原图顶的毫米数。
- 当前批次排版数据到达后，仅抽样其前24张路径选择左/右膜标签生产图，不扫描上次目录、不为示例递归读取整个批次、不镜像生产图冒充另一侧。没有对应侧样图时使用明确标注“当前抽样未找到该侧膜标签”的绘制样板，不声称整个批次无该侧，也不把测试样板冒称真实生产文件。生产图完整路径在悬停及放大说明可查看，源文件不修改，不生成打印PNG。
- 首次打开先展示界面，四例读取/像素和占位在独立后台线程完成；参数变化合并刷新，运行中到达新批次或新参数丢弃旧结果并随后重算。主线程仅安装像素和控件，不在paintEvent读取/布局原图。每例带“放大检查”图标按钮，复用已生成预览像素，不重新读取或排版。原打印参数色块设置示意保留，但不作为主生产验收依据。
- 单例检查图仅展示实际单图装饰位置，不是整批刀位安全证明；真实整批预览/完整订单/双面/尺码/源及保存后像素检查未更改。整批预览放在有独立布局的标签页容器中，保留最大化/还原控件归属，不直接把其可重定位视口放入Qt内部堆叠布局。
- 源码0.1.138，固定日期2026-09-14、当日第09次更新；仅推送源码，无新Release安装包。四例/缺侧示意/源不变、主界面显示后后台准备、参数刷新、放大复用、版本/行数等界面相关22项通过；另单多批切换、真实两引擎多批输出、异步预览/取消及双图11项通过，最后新增放大检查后4项重测通过。初轮截图暴露缩略图高度超出控件导致刀码裁切，按实际可用高度缩放并复查四例完整截图；标签页变更暴露旧测试默认整批可见及直接堆叠视口最大化问题，增加独立布局容器并更新测试选择对应视图后通过。未验证当前Windows生产图/机器，测试中模拟输入不冒称用户真实生产样本，旧原生退出仍未定位。

## 96. 2026-09-14：切膜机只识别固定左边缘刀码（硬性约束）

- 用户再次明确机器只识别左边的刀码。刀码只能在输出文件/所属分区的固定左边缘，可按规则位于左上或沿左边缘相应高度；绝不能因为膜标签/二维码在右侧或图片旋转，把刀码放到图右边或右上/右下。原图膜标签的左/右位置只能影响上下参考及普通标签位置，不能决定刀码左右位置。空间优化必须服从该机器限制。
- 单排和旋转单排刀码必须处于文件左基准x=0；双排右图刀码处于右分区自己的固定左边缘，不是右图右边缘，不随单张图或膜标签移动。缺少可内置透明空位时，在同一固定左基准安全外置，不能改右侧寻找空位；独立整批输出检查继续拒绝未对齐左基准的刀码。
- 当前代码已有上述横向限制：item_block把右侧位置规范到左侧，rotated_marks只尝试左基准内置，cut_validation独立检查单排x=0及双排各固定分区左基准。此次补充生产硬性说明，不修改算法或生成新版本/安装包；不把需求确认冒称新增功能，也不恢复已废弃旋转区横向偏移。
- 相关切膜布局、两种引擎旋转刀码整批/分段实际像素与主界面四例14项回归通过；未做Windows切膜机实物验收。

## 97. 2026-09-14：输出末尾批次信息与剪切留白

> 最新变更：用户随后要求先不显示末尾信息，等测试完成再启用；第98节优先于本节的默认启用描述。

- 用户要求批次结束时记录批次信息，尤其双排区和旋转区，并指出旧结束线太近、不好剪切。输出末尾从全部图片、普通标签、平台文字、刀码的最低处计算留白，不只看最后一张图片；先打印批次信息，再留剪切间距画结束红线，不覆盖生产图。
- 主界面使用的打印参数默认启用批次信息，文字4毫米，剪切间距10毫米。相同间距用于内容到信息、信息到红线；文字大小、间距与启用状态持久化，可调整。升级时仅一次把旧3毫米默认迁移到10毫米，其他自定义间距保留；此迁移不能辨别曾手动设为3毫米的用户。低层LayoutSettings兼容调用默认不启用末尾信息，界面明确传入启用值。
- 信息包含源批次文件夹名、订单数（无法解析时待核对）、图片数、生产平台、机器号、手动标签；分别记录双排/常规区与旋转区的图片数、双排行数、尺码范围、区内占用长度和实际刀位。区内占用长度不含末尾信息和剪切留白，不冒称整段输出长度。双排区与旋转区之间仍不增加红线。
- 分段输出在各段末尾记录本段信息及整批信息，末尾红线可注明分段结束/下段旋转换刀。保留完整订单与双面分段约束及整批刀位。末尾信息和留白计入实际输出高度及用膜比较，不当作免费空间。红线与信息可分别关闭。
- 整批预览显示同一字体生成的末尾信息与红线，最后图片详情包含完整末尾；调整末尾打印参数会刷新已加载且非运行中的预览。切割与耗时合并报告保存同一批次信息，不把文字区域称为红线。
- 独立源及保存后像素检查保留；信息穿过刀位通道时仅放行真实文字字形的alpha支撑，文字框内空白仍不得有额外墨迹。Pillow按区一次准备窄条字形，避免每8像素通道条带重新生成整个文本。两引擎完整12张/6单双面且含常规与旋转区域的整批及三段测试验证全部原图不透明像素未改变、刀位通道通过、内容和信息/红线留白准确；额外字形空白墨迹两引擎均拒绝。测试图不是用户当前生产样本，未做Windows切膜机实物验收，旧原生退出仍未定位。
- 源码0.1.139，固定日期2026-09-14、当日第10次更新；末尾信息、两引擎整批/分段输出、精确字形通道、预览、设置迁移持久化、结束线/旋转、缓存过期、版本与行数42项回归通过。源码提交推送与Release安装包分开，本次无新安装包。旧PNG不自动修改，需要重新生成批次。初轮旧回归测试以“必有旋转”为前提而未真正制造错位，改为独立制造刀码错位；旧标签碰线预期改为验证自动移到全部内容下方，不放宽像素判据。

## 98. 2026-09-14：末尾批次信息暂时关闭

- 用户要求先不显示新加的末尾信息，等测试完再说。预览和新输出默认关闭末尾批次文字，底层功能与打印设置开关保留；首次升级一次关闭上一版持久化的启用值，之后手动启用可正常持久化，不在每次启动反复重置。
- 保留结束红线及10毫米默认剪切间距，用户自定义间距、字号和其他参数不变；现有批次分析、报告和主界面数据不隐藏。关闭末尾信息不生成其额外文字空间，旧PNG不修改。
- 源码0.1.140，固定日期2026-09-14、当日第11次更新；仅源码更新，无新Release安装包。

## 99. 2026-09-14：左侧图片刀码改为原图外置

- 用户明确左侧图片的刀码不能放在原图内部，要向左约5毫米。固定文件左基准不能变成负坐标：刀码仍x=0，左侧原图右移，为完整刀码宽度及默认5毫米图外间隙留空间。使用现有色块间距参数，至少一个输出像素间隙；不是把刀码x改为-5毫米而被画布裁掉。
- 主界面排版启用外置左侧规则，单排及旋转单排同样属于左侧；双排右图保持原来的固定分区左边缘及安全内置/外置规则，不整体禁用右图的透明空位利用。常规普通标签保留刀码下方，旋转标签跟随真实膜标签，平台文字与原图一起平移，不移动源图内容或源文件。
- 增加可复用的不可变左侧占位变体，仅在左分区使用；双排可行性、尺码搭配、刀位候选/等价状态及旋转区宽度均使用真实左侧外置占位，不能排完再右移导致跨刀位。缓存键包含新规则及软件版本，旧计划不能误复用。
- 主界面四种情况预览同样使用左侧占位变体，并显示实际图外间隙；独立整批几何检查拒绝左刀码进入原图或间隙不足，原图/最终输出像素和通道检查仍执行。低层兼容API默认不启用新外置开关，主界面明确开启，测试直接调用须传cutter_left_marker_external=True。
- 源码0.1.141，固定日期2026-09-14、当日第12次更新；两种引擎各12张/6单双面三段整批测试覆盖不旋转/含旋转方案，全部原图不透明像素保留、完整刀位通道验证、左侧图外间隙、右图相对刀码位置不变；四例真实几何、窗口加载/刷新、切膜/旋转区/尾部策略、版本/250行等27项回归通过。未做当前Windows生产图/机器实物验收，无新Release安装包，旧PNG需重新生成。

## 100. 2026-09-14：左侧刀码抬高并复用垂直间距

- 用户要求左图刀码顶端比图片顶端高1.5毫米，避免横切伤及图片，且允许调整。主界面打印设置新增“左图刀码高于图片（毫米，占用现有垂直间距）”，默认1.5毫米，自动持久化；左图保持外置5毫米间隙和文件左基准，右图不应用抬高。常规左侧普通文字随刀码上移，旋转文字仍跟随膜标签，不挪动原图或平台文字。
- 抬高量直接作用于刀码相对坐标，不增加每排占位高度，不改变图片垂直坐标或原5毫米排间距；设定值不得超过当前图片垂直间距或小于0，越界明确拒绝生产。默认批次开头留白3毫米足够容纳1.5毫米抬高；用户留白不足时，批次/分段开头保留足够空间避免裁掉刀码，不让每排重复增加留白。
- 独立几何检查核验真实抬高像素及非负画布位置；旋转标记独立检查接受启用外置规则后的图顶减抬高量，不错误要求原二维码高度。实际源像素与整批通道检查保留。四例及选中图片详情从最低刀码坐标调整可见范围，确保高出原图的刀码不在预览中裁掉；已加载非运行预览可随抬高参数刷新。
- 低层LayoutSettings的抬高量默认0以兼容旧直接调用，主界面明确传1.5毫米或用户保存值。源码0.1.142，固定日期2026-09-14、当日第13次更新，仅源码更新，无新Release安装包；旧PNG需重新生成。测试用25.4 DPI样本因像素取整不能精确表示1.5毫米，初轮硬编码2像素失败，改为核验与实际mm_to_px一致；不是隐藏坐标错误或声称低DPI精度优于一个像素。
- 31项左侧外置/抬高、两引擎完整双面批次三段源像素与刀位通道、抬高前后图片坐标/长度相同、间距越界拒绝、设置默认及持久化、四例窗口/刷新、切膜/旋转/尾部、版本及行数通过；另18项末尾信息、并行膜规格数字计算和分段输出通过。未做当前Windows生产批次或机器实物验收，不宣称旧原生退出修复。

## 101. 2026-09-14：刀码预览位置尺寸示意

- 用户要求刀码预览更清楚展示刀码与图片的关系。四种情况与放大检查在已生成样图像素之外增加纯预览尺寸层：蓝色虚线是原图边界、红框是实际刀码范围、橙色尺寸线展示刀码右边缘到图片左边缘的水平间隙，以及刀码顶端与图片顶端的高度差。标题显示实际毫米值，不画虚假的5毫米/1.5毫米坐标。
- 原始样图像素及真实排版几何不修改，辅助尺寸层不进入生产PNG、切割线或合并报告；附加预览空间不是新增打印留白。绘制在GUI线程使用QPainter且显式结束，后台源测量规则不变；切膜抬高参数也触发四例合并刷新，放大复用同一标注图，不重复读取或规划。
- 源码0.1.143，固定日期2026-09-14、当日第14次更新；四例生产/缺侧示意、源及样图像素不变、尺寸层绘制、显示后准备、抬高参数刷新、放大复用、版本及250行8项测试通过，并检查四例主界面截图，边界和尺寸线完整可见。未做Windows当前生产图或机器实物验收，无新Release安装包。

## 102. 2026-09-14：整批旋转独立候选与膜标签/图案空白禁用

- 用户澄清旋转省膜是主逻辑，快速模式仅限制计算方法，不能以它排除整批旋转。主界面本地切膜单/双列默认独立比较一个整批向左旋转候选，每行一张，保持现有完整订单、双面相邻及尺码顺序；即使双排尺码受到旧尾部保护或不属于3XL以上，仍比较全批，不修改旧局部筛选的复杂度。仅实际总长度更短且独立订单、刀位和原图标记检查通过才采用；用户显式手动旋转不被整批自动候选覆盖。
- 普通模式/快速路径的实际排版在现有常规或局部方案后比较一次全批候选。关闭“尾部大码旋转”不关闭独立全批候选。用膜比较的允许旋转行也比较全批候选，复用预先测量的旋转占位，不逐规格重复解压；不旋转行仍不旋转，不根据比较擅自换膜。软件升级和新规则参数使旧规划缓存失效。
- 全批候选布置与长度累计为一次遍历，不搜索所有区域组合；既有排序、订单/尺码独立验证仍有排序成本，不能把整个流程及像素检查宣称严格O(n)。比较计入图片、全部标记占位、真实结束留白及可选末尾信息；选中全批旋转后更新已有旋转比较结果的实际长度/数量。候选缺少可用旋转图或安全检查不通过时显示原因并保留原安全方案。当前先取得现有方案，现有方案本身失败时仍按既有错误路径处理，不冒称所有常规不可用输入已能由全批候选救援。
- 最新硬约束：膜标签/二维码与生产图案之间的空白不得有任何标注，透明不代表可用。旋转普通文字改为原图外侧刀码列，旋转平台文字原图外置；常规平台仍可在膜标签同一高度行的安全透明空位，普通标签不自动内置。独立验证拒绝原图内、膜标签行之外的任何普通/平台文字；旋转文字要求全部原图外置，不再强制其在旋转膜标签下方（该旧位置可能就是禁止使用的空白）。原图内容不改变，固定左刀码/抬高/双面安全规则继续执行。
- API兼容默认cutter_compare_whole_rotation=False、preserve_header_gap=False，主界面明确启用两项。源码0.1.144，固定日期2026-09-14、当日第15次更新，无新Release安装包。新增4张同尺码单件全部常规双排却整批旋转更短的快速路径用例、两种引擎8张/4单双面三段完整原图像素/全高刀位/文字外置用例、禁用区域错误标注拒绝及60厘米比较行全批候选测试；测试图不冒称当前用户生产源样本，未做Windows机器实物验收。初轮新增LayoutItem字段位置误配被真实抬高检查拒绝，修正字段顺序后原外置回归通过，未放宽生产判据。
- 最终整批旋转、禁用区域、两引擎整批/分段源像素与通道、左侧外置/抬高、四例界面、并行膜规格、尾部/旋转区、24小时缓存、末尾信息、版本/250行41项回归通过。旧PNG不自动修改，需重新生成；源码推送不代表机器生产验收。

## 103. 2026-09-14：刀码固定直线与刀位点线移除

- 用户明确双排左侧刀码必须整批处于固定竖直线，不随单张图片位置变化。现有生产布局及独立检查已要求左刀码x=0、右刀码为所属固定分区左基准；此次不重新挪动刀码，不改变外置5毫米和抬高1.5毫米规则，以整批两引擎/分段回归确认。
- 主界面真实排版、预览、输出及膜规格参数关闭刀位红色虚线/点线，直接用刀码指示刀位；不再为刀位点线扫描膜标签。批次结束红线及刀码预览里的原图边界、尺寸示意保留，尺寸示意并非生产刀位线。整批几何和保存后实际安全通道检查继续执行，不因为不画刀位线而省略。
- 低层API的cutter_knife_dots默认True仅兼容旧直接调用，主界面固定False，没有新增普通用户开关。源码0.1.145，固定日期2026-09-14、当日第16次更新；整批旋转、两引擎常规/旋转12张6单双面三段真实源像素及通道、刀码固定基准及无点线、预览禁用点线不调用标签搜索、四例界面、结束线、版本/行数29项通过。未做Windows当前生产批次或切膜机实物验收，无新Release安装包，旧PNG需重新生成。

## 104. 2026-09-14：输出文件夹与PNG同名，不含JOB编号

- 用户要求输出文件夹不带JOB及其后日期/编号，名字与输出文件一致。开始生成时临时使用源批次名称、不含JOB；生成PNG并通过检查后，同一目录整体改为实际输出PNG的文件名（去掉.png），保留批次名、订单数量、标签、尺码等真实文件信息。多段同属一个目录，目录以第一段PNG名称为准，不拆出每段独立目录。
- 保留“切膜机文件”容器、单/多批共用流水线、嵌套目录镜像结构；已有同名最终目录不覆盖，增加(2)、(3)等避让后缀。内部job_id继续用于历史/任务记录，不拼到目录名，不自动重命名旧输出。仅预览不生成或改名目录。
- 重命名时未完成禁止打印提示随目录一起保留，最终报告和manifest写入新目录；全部处理成功才移除禁止提示。历史、完成通知、自动打开目录、主界面所示输出路径、多批最终状态均使用真实新目录。文件夹整理显示步骤/目标，不在GUI线程处理。
- 源码0.1.146，固定日期2026-09-14、当日第17次更新；命名/同名旧像素保留、单批界面路径与自动打开、两引擎多批、嵌套结构、仅预览、版本及250行32项通过。未做Windows当前生产实物验收，无新Release安装包。

## 105. 2026-09-14：单列模式逐完整订单选择方向，无第二刀位

- 用户明确单排不存在第二刀位，一张图可比较旋转/不旋转哪个更省膜。主界面单列切膜开启两方向选择，候选按实际可打印膜宽过滤，不扣第二刀安全距离；不生成或显示虚构刀位，所有Placement.cut_knife_x_px及结果cutter_knife_mm、cut_corridor为None。双列常规区仍有贯穿固定刀位，不能把其单独一排擅自拓宽到跨刀位。
- 保持完整订单、双面纵向相邻和单件尺码顺序。单件一张图比较自己的两方向高度；多件/双面按整单累计高度选择统一方向，不为局部更省打散订单。显式人工旋转只有一个指定候选，不被算法覆盖；两方向都超实际膜宽或缺可靠DPI明确拒绝输出。
- 单排所有图共享“单排区”，允许不同完整订单采用不同方向，无需在方向切换处新增刀位或提示红线。继续固定左刀码x=0、默认外置5毫米、可调抬高1.5毫米、5毫米垂直间距及膜标签与图案间标注禁区。用实际标记占位高度比较，数字选择一次遍历O(n)，源测量、字体/像素验证和排序另计；开发者算法开销界面记录此流程，进度及计时纳入排版计算。
- 不旋转方案只要有图无法安全放下，就不伪造一个混合替代的“不旋转基准”省膜数，基准回退为实际选定高度。API兼容cutter_single_row_rotation默认False，主界面明确True。单列整批旋转备选也不扣第二刀安全区，保持None刀位。
- 源码0.1.147，固定日期2026-09-14、当日第18次更新，无新Release安装包。两引擎8张/4单双面三段测试验证M码旋转、L码不旋转且均无刀位，全部原图不透明像素保留；430毫米可打印宽度下旋转后包括刀码/间隙恰好占满430毫米的边界用例通过。首次扩展计时回归发现旧测试仍读取上一版重命名前out目录，改为使用worker.output实际最终目录，未修改输出行为迎合旧路径。未做当前Windows生产图/机器实物验收，旧PNG需重新生成。
- 最终单排方向选择/实际宽度边界、两引擎分段原图像素、整批旋转/标注禁区、左刀码/抬高、切膜、多批生成、版本/250行及独立耗时32项回归通过。

## 106. 2026-09-14：颜色优先于尺码，完整订单仍最高优先

- 同批黑白颜色必须分别聚集，颜色优先于尺码。生产文件名尺码前一段作为颜色，Black/黑/黑色统一为黑色，White/白/白色统一为白色；默认黑色组在白色组之前，其他已知颜色独立分组，不猜测无法识别的颜色。
- 完整多件订单和双面相邻高于颜色聚集。单一颜色的完整订单按颜色分组；混色或无法识别颜色的订单整体保留在前，不为聚色拆单。混色订单内部先颜色、再尺码、再商品/件号，双面重新作为相邻对保留。
- 同色单件按尺码从小到大；同色同尺码内可按高度优化双排配对，不能跨颜色配对。旋转筛选、尺码块连续检查使用颜色与尺码联合键；旋转分区若打散整单颜色顺序则拒绝候选并保留安全常规方案。
- 独立整批订单颜色检查保留，实际源像素和保存后全高刀位检查不省略。排序仍为O(n log n)，颜色归一化/分组为线性扫描；不能把排序虚称O(n)。源码0.1.148，日期2026-09-14、当日第19次更新，仅源码更新，无新Release安装包；旧PNG需要重新生成，未做Windows机器实物验收。

## 107. 2026-09-14：当前批次与选用膜规格高亮

- 用户明确当前批次信息及当前选用膜规格是关键生产信息，必须高亮。当前批次名称、图片数量、订单/件数/双面统计等沿用同一主数据控件，增加蓝色边框、底色及17像素粗体；文本可复制，按纯文本显示，不把文件夹中的特殊字符解释为HTML。
- 已选输入目录旁常驻橙色膜规格信息框，显示当前物理膜宽（厘米）、扣除RIIN预留后的可打印宽度（毫米）及单列/固定双列/自由排版模式；切换膜、模式或RIIN预留后第一次交互立即更新，不扫描图片、不重新计算其他膜方案。预留无效时标红并明确禁止生成。
- 当前选用膜与面积最省比较结果明确区分，不自动换膜。多批次上级输入目录仍独立显示，主数据蓝色信息框显示当前被选中的子批次，切换、完成及新批次继续使用原流程，不增加后台任务或修改生产算法。
- 源码0.1.149，固定日期2026-09-14、当日第20次更新；7项界面回归通过，覆盖首次切膜/预留联动、无额外读取、子批次与根目录身份、可复制高亮、无效宽度警告、完成/新任务保留与清理、版本。已检查实际离屏窗口截图；仅源码更新，无新Release安装包，未做Windows实机显示验收。

## 108. 2026-09-14：原生快速PNG保存与保存子步骤

- 用户报告71.08秒任务中保存占46.71秒，要求独立研究更快编码库并更新试用；不要求获取厂家内部实现。原有整批刀位计算0.04秒不是此轮优化对象，订单/双面/颜色/尺码/旋转及坐标规则不修改。
- 新增可复用png_codecs目录：固定PNG标准UP行滤波，用NumPy逐行向量差分及imagecodecs内原生libdeflate压缩，流式写入标准IHDR、pHYs、多个IDAT和IEND，各块带CRC。RGBA每个通道无损，物理尺寸以DPI转每米像素保留；不是调色板量化或有损降分辨率，不采用fpng专用解码器。固定滤波可能影响文件大小，实际需Windows/RIIN验证。
- GUI打印参数“输出与并行”默认启用原生快速保存，可取消对比旧方式，选择自动持久化。保留原来的Pillow/libvips画布引擎及低层API默认关闭；不擅自切换用户画布引擎。现代Python使用imagecodecs==2026.8.16预编译依赖，已确认PyPI有Windows64位Python3.12兼容wheel；fpng_py仅有旧Python预编译包，不让测试电脑更新时被迫安装编译器。旧Python/缺依赖有明确兼容回退。
- 快速路径需要整张原始像素、滤波及压缩缓冲，不冒称零拷贝或低内存流式编码；写盘仅按4MiB压缩块写入，不复制整份压缩数据。用户勾选不限制内存预算时按选择执行；取消时按约16字节/像素预估比较保存预算，超预算退回原引擎，该估计不是峰值内存保证。原生编码失败可回退，磁盘失败/取消不能被吞掉；Pillow画布始终释放，写完才从.未完成改为PNG，任务禁止打印保护不变。
- 保存报告记录实际编码器、像素提取（libvips包含延迟合成）、滤波与压缩、文件写入独立秒数，兼容路径明确合计含可能延迟计算；主总结显示当前/首段结果，完整排版报告与“复制耗时”包含全部段子步骤，并行段不可相加冒充墙钟总计，新批次清旧保存报告。保存开关属于非几何参数，改变它不导致同版排版缓存失效；升级版本仍使旧计划失效。
- 生产前独立源标记/整批刀位像素检查保留。快速保存后额外复核实际全高刀位通道，精确放行已有提示线/字形；没有libvips时用Pillow复核，失败按禁止打印处理。整单/双面分段、滚动并行及旧输出避让不变。
- 开发电脑20百万像素透明测试长图（2000×10000，部分随机图案，两列），三次中位数：编码测试Pillow0.800秒、原生固定UP/libdeflate0.233秒；包含像素提取、DPI/CRC和写盘的保存Pillow0.477秒、新路径0.327秒，约减少31%。文件25.75MB与20.95MB，所有RGBA像素比对一致；用scripts/benchmark_png_encoders.py可重复。不是用户生产批次、Windows/NAS或厂家软件对照，不承诺71秒直接变20秒。
- 源码0.1.150，固定日期2026-09-14、当日第21次更新；仅源码更新，无新Release安装包。验证覆盖两画布引擎、0/1/3压缩、随机半透明/全透明通道逐值、中文文件名、长图多IDAT的CRC与DPI、依赖/预算/编码失败回退、取消、GUI持久化与计时复制、新任务清理、完整黑白单双面批次三段真实源像素/全高刀位、真实混合旋转单排源像素、多段输出、立即退出保护、缓存、版本与250行规范。未做Windows/RIIN实际生产验收。
- 最终上述相关回归共62项通过（8.41秒），git差异空白检查通过；测试通过不等同测试电脑生产验收。

## 109. 2026-09-14：真实大批次退回原生分块流水线，消除重复解压

- 用户本地实测新编码总计102.89秒：提取18.294秒、压缩11.199秒、写盘20.738秒，另有保存后复核20.99秒。此前小样提速不能代表生产大图，新全数组路径不再作为GUI默认或可用路径；不将本地瓶颈归因于网络。
- 下载目录真实609142039033共58张，测试参数为580毫米可打印宽度、300DPI、固定双列、左刀码外置/抬高、标签CY 1001Mt26、平台隆丰8毫米，允许整批和尾部旋转，膜方案比较开启。画布6850×242714，原始RGBA约6.65GB。本机8GB，不运行整批全数组/整图滤波测试；同6张生产子集串行旧原生保存3.38秒、新全数组5.48秒，全部RGBA像素一致，证明小样结论不适用。
- 新GUI“输出与并行”默认大图分块流式合成保存，独立output/stream_png偏好持久化，旧output/fast_png不再启用全数组路径。原始RGBA达到64MiB且libvips可用时选择原生画布，即使用户原普通引擎记录仍保留；不改变膜、图案、分段、订单、双面、颜色、尺码或标签参数，不强制分割完整长图。小图或无原生库时兼容Pillow；低层API通过png_streaming显式启用，旧fast API仅兼容已有调用，不再增加imagecodecs强制依赖。
- 原生流水线由libvips按需生成分块并直接PNG编码，不调用整图write_to_memory/np.asarray，不分配整图UP滤波缓冲；既有原图/DPI/标记及几何独立检查继续保留。最终加完标签、色块、辅助线的真实画布逐区域检查完整高度的切割通道，只放行精确标记像素；原生新模式不再先重复检查无标记画布或保存后完整解压。已有库无损编码消耗该画布，保存后仅读33字节头和12字节尾校验尺寸/RGBA8/IHDR CRC/IEND，未完成、禁止打印及原子发布保护保留。该轻量核对不是声称已重新解码每个输出像素。
- 保存计时标明“流式合成、编码与写入”，不冒称仅磁盘时间。保存后的实际全高刀位/真实RGBA验证放在开发回归和本次实图验收测试中，不增加每个生产任务一次大图重新解压。源和最终画布全部通道检查仍是生产阶段，不采用少量样本推断整批安全。
- 本机整批原生旧路径总计34.60秒（保存21.16、保存后复核11.12），新路径初测25.19秒（流式合成保存19.97、最终画布通道0.15，无输出解压复核）；两轮均曾短暂与独立小测试重叠，不能作为严谨独占性能对照，更不能承诺Windows直接达到25秒。厂家release为Windows x86-64 EXE，本Mac无Wine/虚拟机，未实测厂家性能。
- 源码0.1.151，日期2026-09-14、当日第22次更新；相关69项回归通过，覆盖新最终画布底部通道异常拒绝、PNG头CRC/尺寸/结束损坏拒绝、新模式不调用输出解码、两引擎完整黑白单双面三段真实源像素/全高输出刀位、旧编码兼容、缓存、停止与250行规范。仅源码更新，无新Release安装包，未做Windows/RIIN切膜机实物验收。
- 58张真实整批输出独立解码复核：常规区56张（全高35..235539）与旋转区2张（235668..242679）全部刀位通道通过；对旧/新完整PNG全部RGBA逐值比较，最大差异0，两份均929495675字节。独立复核在性能计时之外完成，结果证明该测试批次输出像素一致，不推断所有未来输入或机器实物均通过。

## 110. 2026-09-14：分步耗时当前步骤置顶

- 排版运行期间，当前操作步骤固定显示在耗时表第一行，整行蓝色底色、深蓝粗体；顶部总耗时同时显示当前步骤名称。其余已完成步骤保持实际执行顺序。
- 步骤切换时滚动到表顶，新任务清空旧状态；200毫秒耗时刷新不持续抢占用户查看已完成步骤的滚动位置。完成、失败、停止后恢复原步骤顺序，移除运行高亮，不修改后台原始计时、阶段累计或日志顺序。仅修改GUI展示，不增加图片读取/排版/保存计算。
- 源码0.1.152，日期2026-09-14、当日第23次更新；仅源码更新，无新Release安装包，Windows显示仍需实机验收。

## 111. 2026-09-14：输出文件诊断信息随报告与复制耗时交付

- 用户要求直接修改，不停留在功能说明。每张生成PNG在现有结果/manifest增加明确PNG、RGBA、每通道8位和保留透明通道信息，复用既有像素宽高、写入DPI、实际文件字节数、压缩等级、实际画布引擎、实际保存编码器与保存阶段秒数。
- 可复用output_file_info模块用已有结果生成中文文件诊断，不重新打开、解压、扫描大PNG。文件大小同时给出十进制MB、二进制MiB和字节，避免与Windows统计单位混淆；DPI标为写入值，不冒称已实测打印机。普通/原生引擎按真实使用路径，不照搬用户旧选择。
- 信息进入现有排版报告.txt及主界面“复制耗时”，不新增单独报告或大图后台读取。多段输出逐文件记录各自尺寸、大小和保存秒数，并给出总大小；不把并行段秒数相加冒充墙钟时间。仅预览不声称有输出文件；新任务清空旧复制信息。旧记录缺字段不凭空补猜像素格式。
- 本次只增加输出诊断，不改变压缩策略或承诺600MB降到厂家140MB，不修改图片、刀码/标签、排序、分段或生产安全检查。厂家文件仍需另行读取，不能用我方记录替代厂家实测。
- 源码0.1.153，日期2026-09-14、当日第24次更新；仅源码更新，无新Release安装包，Windows实机仍需更新后测试。
- 相关50项回归通过（5.92秒），包含禁止任何文件读取/查询的纯元数据报告、多段逐文件统计、复制到剪贴板、真实生成报告与manifest信息、仅预览与旧记录、两引擎完整双面批次输出像素/刀位、流式路径、版本及250行规范；git差异空白检查通过。

## 112. 2026-09-14：默认输出跟随生产原图DPI

- 用户明确默认输出跟随图片DPI，避免低分辨率原图被强制插值到300。GUI打印参数“输出与并行”增加默认勾选“跟随原图DPI”，旁边保留原72..1200手动输入值；跟随时禁用手动输入但不清掉原值，取消勾选立即允许手动150/300，选择单独持久化。新模式首次升级默认开启，用户后续关闭选择自动保存，不将未读图片时的手动框数字冒充实际输出DPI。
- 输出前在工作线程以轻量文件头读取整批内嵌DPI，展示当前文件及计数并独立计时“输出DPI确认”；不在GUI或启动时扫描原图。按四舍五入整数的同名DPI归组，以容忍PNG每米像素的微小量化差异；同组采用第一张实际内嵌DPI（例如179.9844而不是硬凑180）作为整批网格，保持每张图片基于自身内嵌DPI的真实毫米尺寸及像素取整。不从源文件名猜DPI。
- 同批DPI不同组、缺可靠DPI、水平/垂直DPI明显不一致时明确列出问题文件/组及数量，提示取消跟随并手动指定统一DPI；不悄悄按最大/最小值混合，不让不同像素网格直接拼接。手动模式不增加额外DPI预扫描，沿用原有可靠DPI/生产检查。低层LayoutSettings默认保持手动300以兼容旧调用，GUI明确传跟随选择。
- 单批、多批、仅预览、参数实时整批预览、分段生成共用解析逻辑；多批各自决定DPI，多段只沿用整批已解析DPI，不能独立重算为不同网格。常规和旋转方案、标记位置、固定刀位均在实际输出网格计算；四例刀码预览解析各生产/示意源的实际DPI，尺寸层使用例图实际DPI而不是未启用的手动框值。报告/复制耗时记录实际DPI与跟随/手动模式，预览也保留output_dpi。改变模式/实际DPI进入几何缓存键，避免旧300坐标复用。
- 真实609142039033的58张原图实际DPI均179.9844。本机8GB内存，完整输出跟随该值后4110×145594，RGBA约2.39GB，文件368523694字节；本机独立整批计时9.42秒，保存6.98秒。参数580毫米可打印宽度、双列/整批及尾部旋转、平台隆丰8毫米、标签CY 1001Mt26，与前轮测试一致；DPI取整使全长像素/毫米存在小幅量化差，不宣称不同网格输出像素全等。Windows性能、肉眼清晰度及切膜实物仍需实测。
- 对该真实保存后PNG另行独立完整解码检查，常规区56张和旋转区2张的全部高度刀位安全通道通过，不把这个开发实图复核加回每次生产重复解码。保持原图、标签/刀码保护、完整订单/双面/颜色/尺码、停止和未完成禁止打印机制。
- 源码0.1.154，日期2026-09-14、当日第25次更新；70项相关回归通过（6.25秒），覆盖两引擎150/180原始像素与毫米尺寸、DPI写入、混合/缺失/非对称DPI拒绝、手动免额外扫描、GUI默认/手动记忆、统一分段DPI、实时预览实际网格及缺DPI不猜测、四例预览实际尺寸、原输出报告/订单双面像素与刀位、版本及250行规范。初轮新分段测试误关闭生产必须的色块、旧预览测试硬编码300DPI对应的30像素，分别启用必要色块及按当前例图DPI换算断言，不放宽生产检查。仅源码更新，无新Release安装包。

## 113. 2026-09-14：四例刀码预览沿用真实排版模式与参数

- 用户截图指出实际文字在刀码与膜标签之间，而四例预览不一致。根因：build_examples强制cutter_mode=dual、强制左上色块/零垂直偏移、强制左侧外置，并统一应用左列变体；自由排版实际使用不同文字位置规则，不能用强制双列示意替代。
- 四例继续按左/右膜标签、旋转/不旋转分组，但使用当前真实free/single/dual模式、色块位置/偏移、外置选择和文字参数；仅在真实单/双列模式应用生产同一external_left_item变换，自由排版保留原item坐标。沿用实际DPI和同一生产字体渲染，不改生产图或为了匹配旧示例擅自挪动输出文字。
- 切换排版模式首次触发合并刷新，状态明确显示当前模式，不再声称所有模式文字都在刀码下方。四例是当前参数、对应源图的左基准示例，不替代整批/右分区实际输出预览和切割安全判定；实际整批预览仍使用Placement。
- 新增3模式×2膜标签侧×2方向的12项对照，以真实generate_layout数据核验普通文字与平台文字相对刀码的x/y坐标。相关46项通过（8.48秒），包括四例加载/参数刷新、跟随DPI、两引擎完整黑白单双面输出像素/刀位、版本和250行规则；差异空白检查通过。源码0.1.155，日期2026-09-14、当日第26次更新；仅源码更新，无新Release安装包，Windows实际示例显示仍需用户验证。

## 114. 2026-09-14：批次结束暂不自动画线

- 用户要求批次结束暂时不主动画线。GUI默认关闭批次/输出分段结束红线，首次升级一次迁移旧开启值为关闭；之后用户手动开启可持久化，不在每次启动重复关闭。预览与输出共用该设置，不修改旧PNG。
- 保留刀码、剪切间距自定义值、完整订单/双面及分段逻辑和安全检查。末尾批次信息仍默认关闭；打印参数保留手动开关。此次不修改双面刀码坐标，此前用户反馈仍待对应整批证据核验。
- 源码0.1.156，日期2026-09-14、当日第27次更新；仅源码推送，无新Release安装包。验证旧参数迁移/手动恢复持久化、两引擎整图/分段无结束线且真实刀码保留，以及预览、末尾信息、完整黑白单双面源像素/刀位与代码行数；不代表Windows切膜机实物验收。

## 115. 2026-09-14：膜标签与图案最小40毫米空白

- 用户确认自动补足膜标签（包含二维码）与下方生产图案之间的垂直空白，默认40毫米。打印参数→排版规则可调整0–200毫米，0关闭；选择自动持久化，已有空白大于目标不缩短。只在原方向插入透明行，然后参与真实旋转；不缩放标签/图案，不删除原像素，不在新增空白放文字或刀码。
- 原文件不修改。先在后台生成同名无损RGBA间距副本，保留源DPI及ICC；整行alpha（包括半透明）找纸卡下方的透明分界，卡片边界缩略图误差仅允许小范围容差。不能确定分界/标签时保留原图、列异常文件和原因，不猜测裁图，不因个别非致命标签异常阻塞其他图片。可靠DPI和真正订单/刀位/像素错误仍拒绝生产。
- 副本路径由原绝对路径、修改时间、大小、间距及实现版本决定；有效副本24小时内复用，参数或源变化自动另建，过期再次使用重新生成。只缓存于本机日志目录的独立“膜标签间距副本”，不在源目录添加文件；副本未主动全盘清理，避免删除仍在预览使用的路径。与既有24小时几何缓存分开，首次补足包含额外解码、无损编码与写盘，不冒称零成本。最多4路有界滚动处理，停止不提交全部后续文件；非Qt线程只操作文件/数据。
- 单/多批、仅预览、实时整批预览、四例刀码与分段生成共用副本；手动旋转及序号路径映射保留，订单身份按同名文件不变。用膜比较、实际画布高度及分段使用补足后的完整图片矩形面积，不把新增空白冒称原油墨覆盖率。结果/analysis/报告记录原文件路径、新增毫米数和分界异常，生成结束检查原文件新鲜度。
- 本机真实609142039033共58张、179.9844DPI、580毫米双列，默认40毫米：32张补足、26张原间距足够，无分界异常；完整生成20.11秒（首次副本准备计入），4110×149238像素。独立逐块核对全部新增副本的原有RGBA像素，全部保留；独立保存后完整常规/旋转刀位通道检查通过。该数据不代表Windows性能或切膜机实物验收，新增空白增加用膜属于明确需求，不承诺与未补足批次同长同速。
- 源码0.1.157，固定日期2026-09-14、当日第28次更新；仅源码更新，无新Release安装包。相关测试覆盖左右膜标签、40毫米补足/原像素和DPI保留、已有大间距不缩小、无分界异常、缓存与人工旋转路径映射、两引擎预览/输出和完整双面三段实际像素/刀位、参数记忆、版本及250行规则。旧PNG不修改，需要重新生成。
- 最终相关82项回归通过（9.97秒），差异空白检查通过。开发者批量仅分析也先使用同一间距副本与实际DPI，测量线程按并发批次预算分配，不生成最终生产文件。

## 116. 2026-09-14：图片上下垂直间距改为8毫米

- 用户明确5毫米太近，改为8毫米。核心默认、GUI初值、无配置恢复值及提示统一8毫米；旧保存5毫米首次升级一次迁移为8毫米，其他自定义值保留。迁移后主动改回5毫米仍可保存，不在每次启动反复覆盖。
- 切膜单/双列只改变上下间距，不把8毫米叠加在水平刀位分区。膜标签与图案内部最小40毫米是独立参数，不改变。旋转、单双面与分段继续走真实占位及安全检查；旧PNG不变，需重新生成。
- 用户同时确认全局左刀码基准：合并任何数量批次、旋转或不旋转，左侧刀码始终固定输出x=0；双排右刀码仍是第二刀分区固定左基准，不随单张图移动。本节记录硬约束，不声称此前双面位置反馈已被本次间距修改修复。
- 源码0.1.158，日期2026-09-14、当日第29次更新；仅源码更新，无新Release安装包。

## 117. 2026-09-14：开发者模式记录真实58张独立基准耗时

- 用户要求知道真实58张原像素及刀位复核耗时，并在开发者模式展示。此前首次40毫米间距完整生成20.111秒，不包含独立复核；当时没有记录独立复核时间，不补猜。该旧测试使用5毫米上下间距。
- 当前8毫米上下间距重新运行同58张生产图，跟随179.9844DPI、580毫米可打印宽度、40毫米标签空白、固定双列及整批/尾部旋转。间距副本已缓存，当前0.1.158几何缓存未命中；Mac ARM/8GB/本地磁盘单次测得完整生成12.933秒，其中PNG保存8.870秒（已包含，不再累加）；32张补足副本全部原有RGBA逐块复核5.503秒、26张直接保留原文件；保存后两个区域完整高度刀位通道复核23.864秒。画布4110×150448，369288374字节。
- 副本原像素复核不是保存后整张PNG每个图案的RGBA全等承诺。两项独立复核在完整生成计时之外，属于开发验证，不增加每次生产等待；不能拿旧无副本缓存的5毫米20.111秒与本次8毫米12.933秒当作同条件算法提速。生成计时从已获得58个路径起算，目录文件名发现不在该数字内。Windows实际耗时与物理生产验收另行测试。
- 开发者模式→算法开销，已有步骤表下分“当前批次耗时 / 58张实图基准”两个标签，参考记录注明真实参数、环境、缓存和测试源码版本，支持一起复制；不覆盖当前任务数据，不在打开页面时扫描源文件或运行基准。参考源码0.1.158不会伪装成用户当前电脑实测。
- scripts/benchmark_header_gap.py可传明确源/输出/副本缓存目录复现，写“独立基准测试.json”保存数值/参数与通过状态；不改源图，输出与原有文件安全避让。软件交付源码0.1.159，日期2026-09-14、当日第30次更新；仅源码更新，无新Release安装包，本次不修改生产布局/保存算法。

## 118. 2026-09-14：统一界面文字复制

- 用户强调全部UI文字可复制，截图是“仅预览，不生成文件”勾选文字。全局可复用GUI事件过滤器：现有和后创建的QLabel保留链接等原交互并启用鼠标/键盘选择；按钮/勾选项、分组标题、下拉当前值、标签页标题、进度文字、表头和单元格右键提供中文“复制文字”，不触发左键动作，不改变勾选或选项。
- 表格/树/列表复制当前鼠标单元格，选中内容可右键或复制快捷键输出行列文本；数值0不能丢失。输入框、数字框、文本编辑区保留原生复制/粘贴/撤销，已有自定义上下文菜单不擅自替换。启动提示也接入；窗口标题可在窗口空白区右键复制，不修改系统原生标题栏。预览中已经烘焙成图片的字不变成可选文字，不声称能复制PNG内部字形。
- 全局只安装一次，Qt对象与菜单创建/操作仍在GUI线程，后续窗口无需逐个重复设置。不读取源图片、不修改排版或输出像素。源码0.1.160，日期2026-09-14、当日第31次更新；源码推送与Release安装包分开，本次无新安装包，Windows右键交互仍需实机测试。
- 提示标签同时提供中文右键“复制文字/复制选中文字”，富文本复制实际显示文本而非HTML。26项复制、开发界面/耗时、既有按钮/加减交互、版本和250行回归通过；不声称复制图片内部烘焙文字或系统标题栏菜单。

## 119. 2026-09-14：平台名称与普通文字统一在刀码下方

- 用户确认平台名也在刀码正下方，顺序为刀码、平台名、普通标签（手输文字/机器号/序号），统一左对齐；不得横向夹在刀码与膜标签之间。GUI默认启用，左右膜标签、旋转/不旋转、单/双列及自由排版示例共用实际坐标。低层调用保留旧默认兼容。
- 文字列的真实宽高在排版前计入占位；左列外置刀码及整列文字固定输出x=0，右列沿用第二刀分区基准，抬高刀码时整列一起移动。不能为了省空间覆盖原图或膜标签与图案之间的空白。平台字号仍可调整，默认6毫米；明确字号的外置文字无需搜索膜标签，自动高度仍基于实际膜标签，异常保留原图并安全省略。
- 真实58张609142039033测试，8毫米上下间距、40毫米膜标签空白、跟随179.9844DPI、平台8毫米：完整生成11.980秒（保存8.463秒包含其中）；32张间距副本原RGBA独立核验5.057秒通过，26张保留原文件；保存后整批两个区域刀位通道独立核验34.955秒通过。该记录生成时版本字段仍为0.1.160，使用本次未提交布局修改，不伪装成已发布版本或Windows实测。独立复核不加回每次生产等待，不宣称保存后全部源图RGBA逐像素全等。
- 源码0.1.161，日期2026-09-14、当日第32次更新；旧PNG不变，需要重新生成。仅源码推送，无新Release安装包，切膜机实物需测试。
- 80项相关回归通过（8.88秒）：三种模式、左右膜标签、两方向、四例与实际坐标一致、两引擎完整双面三段源像素/全高刀位、明确字号免膜标签搜索、40毫米间距及版本/250行规范。旧抬高边界测试改为超过实际默认垂直间距，不继续硬编码旧5毫米。

## 120. 2026-09-14：开发者模式主界面快捷间距

- “仅预览”旁作为常用参数快捷区；开发者模式开启时显示“膜标签与图案间距”及当前可编辑数值。沿用此前明确的40毫米（4厘米），不把用户此次40cm表述擅自放大十倍；范围与打印设置统一0–200毫米。
- 快捷输入与打印参数的同名原控件双向同步，复用原持久化、布局快照、预览刷新信号；关闭开发者模式仅隐藏快捷区，不重置参数。重启显示上次选择，不增加启动扫描。此次仅UI接入，不改图片处理、刀位或排序算法。
- 源码0.1.162，日期2026-09-14、当日第33次更新；源码推送，无新Release安装包。

## 121. 2026-09-14：单排无第二刀位的预览显示修复

- Windows报错位于show_pair刀位文字毫米换算。single_rows明确以cut_knife_x_px=None表示单排无第二刀，不能进行乘法，也不能凭空显示默认第二刀位置。生成过程和后台仅预览统一使用knife_caption，显示“单排，无第二刀位”；常规双列沿用真实坐标，0是合法刀位而非空值，异常非数字只标记数据异常。
- 仅修改展示，不改变实际图片/刀码、排序、刀位计算或安全检查。截图另有QFont点数警告，未证明与本次TypeError相关，不把该警告当作已经修复。源码0.1.163，日期2026-09-14、当日第34次更新；源码推送，无新Release安装包。
- 首组32项回归通过（5.76秒）；追加19项通过（22.45秒），包括直接调用生成预览show_pair和异步预览finished的单排空刀位回归、实际仅预览/双图/刀位刷新、版本及250行规范。此次不重新保存大生产图，因不改变合成或坐标逻辑。

## 122. 2026-09-14：打印参数入口固定主窗口底部

- 用户反馈打印参数按钮太靠下，要求固定位置。主窗口复用原settings_button移到与版本、检查更新同一个固定底栏，不复制按钮或修改打开设置逻辑；独立AutomationDialog仍保留自身底部入口。主界面批次数据和预览滚动不移动此入口。
- 不改变参数、后台计算或生产坐标。回归验证按钮属于主窗口底栏、滚动前后窗口坐标一致、点击可打开原设置窗口。源码0.1.164，日期2026-09-14、当日第35次更新；源码推送，无新Release安装包。

## 123. 2026-09-14：开发者模式间距额外用膜

- 用户要求主动补足膜标签/图案间距造成的额外损耗。开发者模式显示补足张数、原间距参考长度、当前长度、长度及面积有符号增量；面积按当前完整膜宽（可打印宽度加RIIN左右预留）计算。普通模式不执行额外参考计算。
- 同膜宽、同DPI、同旋转策略、手动旋转和序号、订单约束重新排原文件；不补间距，不比较其他膜规格，不保存参考大图。不是逐张新增毫米累加，双排共享行高；参考会按同一策略重新选择几何方向/自动刀位，不能冒称固定每张坐标的纯空白面积。若重排改变方向使增量为负，按真实负值展示，不截为0。分段前计算，额外分段留白不在此数字内。
- 展示实时参考步骤，原图/参考不可读取或几何无解列不可用原因、不因此阻塞主生产；停止信号继续传递。参考进度名称加前缀，不能覆盖生产已确定刀位。原文件路径恢复映射，缓存沿用现有24小时规划缓存。结果保存于analysis.gap_loss、现有排版报告及批次记录，不增加单独大图或后台启动读取。
- 小实图仅预览核验32毫米补足对应0.032米增量、不创建输出目录；20项间距/开发UI/规范回归通过，追加5项含实际方案及参考失败非阻塞通过。源码0.1.165，日期2026-09-14、当日第36次更新；源码推送，无新Release安装包，真实Windows整批性能尚需实测。

## 124. 2026-09-14：混合原图DPI自动降级继续

- 覆盖第112节中混合可靠DPI一律拒绝的规则。各源图可靠且水平/垂直DPI一致时，不同整数DPI组采用（最低组+最高组）/2作为整批输出网格，如100/200采用150；不是按张数加权平均，也不自动选择最高分辨率。保留各图按自身原DPI计算的毫米尺寸，统一重采样，低分辨率插值不提升真实细节，高分辨率降采样可能损失细节。
- 主界面异常/提示区显示原DPI分布、采用值、影响、手动覆盖入口，进度记录同一提示；analysis、现有报告和批次记录保留notice，输出模式标记mixed_source而非手动或单一跟随。缺可靠DPI、非对称DPI仍不可猜测生产尺寸；真正刀位、像素或订单错误继续禁止不安全输出。
- 跨项目原则同时写入本项目AGENTS.md“跨项目交互要求”：有依据的可恢复参数冲突自动降级继续，显著告知、记录并给用户调整入口；不可静默猜值或绕过安全/不可撤销约束。本文件和本项目AGENTS的更新不代表其他项目文件已被修改。
- 源码0.1.166，日期2026-09-14、当日第37次更新；源码推送，无新Release安装包。

## 125. 2026-09-14：批次结束右下角色块

- 用户要求批次结束右下角加独立色块。GUI默认开启正红10×10毫米结束块，打印参数“批次结束提示”有独立可记忆开关，不恢复默认结束横线，也不移动左右原识别刀码。低层默认关闭兼容已有调用。
- 使用输出画布右边缘，放在整批图片、文字、刀码及可选末尾信息/红线之后，至少5毫米空白（沿用更大的结束剪切间距），保留批次结尾留白；真实宽高纳入排版、用膜比较、实时预览和输出。分段只在最后一个文件绘制结束块，中间段不冒充批次结束；多批各批独立结束。
- 输出transition_marks及现有切割报告明确标为“批次结束色块”，不叫红线。验证两引擎完整4个双面订单/8图/3段所有源RGBA和全高度刀位通道、最后段唯一结束块及间距；不以两个样本推断整批安全。
- 源码0.1.167，日期2026-09-14、当日第38次更新；源码推送，无新Release安装包，Windows及切膜机实物仍需测试。

## 126. 2026-09-14：固定底栏暂停批次

- 用户反馈多批次没有暂停键；当前多批begin会启用原按钮，但原批次操作区随工作台滚动。保留与单/多批排版同排的原暂停入口，同时主窗口固定底栏提供同步“暂停批次”，不随预览/数据滚动。
- 新入口复用stop_generation立即退出逻辑，单/多批统一，不能等待所有原生任务完成；保留已完成文件、未完成结果禁止打印。Qt主线程事件过滤器镜像原按钮enabled状态，开始可用、结束禁用，不添加轮询线程或新的任务生命周期。无任务时禁用。
- 源码0.1.168，日期2026-09-14、当日第39次更新；源码推送，无新Release安装包。

## 127. 2026-09-14：暂停批次只保留上方入口

- 用户明确两个暂停按钮有歧义，覆盖第126节新增底栏暂停的要求。删除底栏镜像按钮及专用模块，仅保留上方“单批次排版 / 多批次排版 / 暂停批次”原操作区入口，继续单/多批统一启用及立即退出行为。
- 固定底栏打印参数、版本、检查更新和开发者模式保留，不更改任务、参数、图片或刀位。源码0.1.169，日期2026-09-14、当日第40次更新；源码推送，无新Release安装包。删除的镜像模块可从Git历史恢复。

## 128. 2026-09-14：新增膜标签间距导致膜宽失败时自动回退

- 用户明确统一双列刀位失败不应直接结束，要先rollback程序增加的膜标签与图案间距。对“统一双列刀位不存在”或“两个方向都超单排膜宽”，且本批确实添加透明行的情况，撤销本批所有程序新增行，恢复源路径、原有间距、手动方向和序号，按原生产策略重排一次。
- 不裁原图、不缩放图案、不删源文件、不修改用户持久化40毫米参数，不自动换膜或拆订单。显示回退步骤，每张记录rollback_added_mm、原因及警告，主界面异常区与现有报告说明用户设置未改。回退后仍需整批坐标、订单双面、原像素与刀位安全检查；真正订单/安全错误不靠回退绕过，原间距仍无解时继续明确禁止不安全输出。
- 单/多批生成、分段前规划和后台预览复用同一恢复函数。间距额外用膜按本次实际采用原间距计算，不冒称仍用了新增40毫米。源码0.1.170，日期2026-09-14、当日第41次更新；源码推送，无新Release安装包。Windows截图170张源未在本机，不能声称该实际批次已验证成功。
- 同样覆盖旧固定分区规划的明确宽度无解消息。17项回退/间距/规范回归和24项DPI/用膜损耗/结束标记等回归通过；双引擎完整8图4双面实际输出原图不变、有效RGBA与固定左刀码复核通过；另测截图统一双列报错精确触发一次原路径重试，非宽度订单错误不重试。

## 129. 2026-09-14：排版入口动画标识当前任务

- 用户要求执行排版时按钮显示旋转加载动画，明确哪个入口运行。单/多批共用LayoutActivity，选中入口显示蓝色旋转点阵、“进行中”文字和蓝底边框，即使按钮禁用也不灰掉当前活动标识；另一入口保持原静态图标。仅预览也按所属单/多批入口标识。
- 开始后80毫秒GUI QTimer更新图标，不扫描图片、不伪造百分比；QPainter每次显式结束，Qt对象/绘制均在GUI线程。批次完成、失败或取消后停止定时器、恢复原文本/图标/样式；立即退出不等待动画或生产任务。多批切换查看不同批次不重启动画为单批。
- 源码0.1.171，日期2026-09-14、当日第42次更新；仅UI源码推送，无新Release安装包，不改排版像素/刀位或线程池。
- 动画帧、禁用态可见、两种入口和结束恢复3项回归通过；另9项刀位文字/版本/规范通过，开发者及多批原有6项通过。开始时保存主题实际应用后的图标，避免恢复成构建期间旧图标。Windows动画流畅度需实机观察。

## 130. 2026-09-14：膜标签间距仅限开发者测试

- 最新要求覆盖此前GUI普通模式默认补足40毫米。膜标签与图案间距仍是测试功能，不可作为普通生产功能发版。普通模式主界面快捷区与打印参数对应整行均隐藏；生成/预览参数强制membrane_gap_mm=0，不执行补足、由此触发的回退或额外损耗参考计算。
- 开发者模式才显示并应用保存的间距，保留原已保存40毫米或自定义值，关闭开发者模式不删除或改写用户数值。单/多批、仅预览、例图均通过统一GUI参数门控；低层显式测试调用仍可启用，生产任务使用开始时参数快照。
- 本功能未通过生产验收，不发布Release安装包；本次源码修改用于隔离已有测试功能。

## 131. 2026-09-14：失败订单与原文件可复制诊断

- 用户要求报错明确哪个订单/文件及原因，而非只说做不了。根据错误中的明确原始文件名定位对应完整订单，列全部相关原文件名和路径；未知整批错误标注“整批关联范围，不能归因单一订单”，列涉及范围，不冒称全部图片异常或指责最后读取文件。未获得文件清单明确说明不能定位。
- 生成worker记录批次目录、失败步骤及原始原因，输出目录已存在则写失败诊断.txt；写诊断失败不隐藏原报错。膜标签新增间距回退后仍失败记录已回退张数、原间距仍无解、用户参数未改。后台预览也提供相同外层诊断，不添加图片解码。
- 单批报错改为可滚动只读详情窗口，明确“复制全部报错详情”，按钮不关闭窗口；主界面总结与多批结束错误提供“查看并复制报错详情”，简要展示前8行，完整内容不丢失。仅生产错误仍禁止未完成输出，自动恢复和完整订单安全规则不变。膜标签间距仍仅开发者测试。
- 源码诊断更新0.1.172，日期2026-09-14、当日第43次更新；无Release安装包，不宣称Windows实物验收。
- 13项订单定位/复制窗口/原输出流程/间距回退回归通过，另10项多批/开发UI/规范和4项版本规范通过。旧单批按钮测试补原生选目录模拟，并移除已过时的“禁止整批旋转比较”断言（当前规则允许比较），不放宽订单或像素检查。测试中挂起的本次原生选目录测试进程已定向停止，无用户软件或文件删除。

## 132. 2026-09-14：失败诊断量化图片与限制参数

- 用户要求不能只有订单名：原图参数含像素、格式/通道、内嵌X/Y DPI、原画布毫米尺寸（含透明区）；缺可靠DPI明确不能确定毫米数，文件头读取失败保留原因，不解压源像素。请求配置与失败点实际输出网格分开，不将未解析的跟随DPI配置冒充最终输出DPI。
- 实际规划失败点记录可打印宽度、RIIN预留及完整膜宽、模式、输出DPI、自动/参考或固定刀位、安全距离、垂直间距、开发者补足间距、刀码尺寸与间隔、字体参数。自由图片组逐候选量化需要/允许/超出毫米，列真实方向、图片尺寸、全部标记占位和文字宽度。切膜单/双列失败列实际全部测量占位与全幅差额；双列参考左右分区宽度明确不是无解时的最终刀位，全幅差额为0不代表能双排。
- 原始参数与全部诊断沿用现有复制详情、失败诊断.txt；自动间距回退重试会重新产生实际参数，而不是复用失败前旧尺寸。整批错误虽列全部文件参数，定位仍标整批关联范围，不指责任一订单。源码0.1.173，日期2026-09-14、当日第44次更新；仅源码，无新Release安装包，膜标签间距仍仅开发者测试。

## 133. 2026-09-14：超宽图片先强制旋转，再等比缩小

- 最新用户授权覆盖此前“不缩放图案”的限制，仅对当前安全可用宽度放不下的图片：先强制较短边横向（默认向左旋转），即使未启用自动旋转；仍放不下才等比缩小。刀码、文字及双排刀位安全区一起计入，不裁图、不跨刀位、不拆同订单多件或双面。能直接放下的图片不缩小。
- 使用独立PNG副本，保留原文件、DPI及ICC，副本缓存24小时；缩小后实际烫印尺寸会变，主界面异常区和报告明确原尺寸、采用尺寸、比例、方向及安全宽度上限。打印设置→排版规则提供持久化开关，GUI默认开启，低层API需显式启用。缺可靠DPI或文字/刀码自身无法容纳仍给具体可复制诊断，不能猜值绕过安全。
- 若失败来自开发者新增膜标签间距，先撤销新增间距，再考虑超宽恢复；该间距仍只限开发者测试。单批、多批及预览复用相同恢复流程，继续整批订单、实际像素与刀位检查。
- 源码0.1.174，日期2026-09-14、当日第45次更新；无新Release安装包。14项超宽恢复/间距回退/开发者门控/文件规范回归通过；双引擎各验证完整8图4双面，单列和双列输出原文件不变、缩后RGBA一致、刀码固定左侧与整高刀位透明安全区。

## 134. 2026-09-14：自动超宽恢复仅单排生效

- 最新要求收紧第133节：不得为并排或双列分区自动缩小两个图片。仅明确的单列切膜模式允许超宽先强制较短边横向、仍超宽再等比缩小。双列模式即使局部只有一张也不通过此恢复改变原尺寸；自由排版同样不启用此自动缩小，不能将双列安全失败擅自视作单排。
- 重排入口和底层恢复函数双重门控；既有开发者新增间距回退保留，但回退失败不在双排进入缩小。普通旋转方案比较不变，不自动切换模式或膜宽。设置明确标注“仅单排”，持久化值保留，原有完整订单和双面约束不变。
- 源码0.1.175，2026-09-14当日第46次更新；源码推送，无新Release安装包。验证单排完整双面批次、双排/自由模式不进入恢复及底层直接调用拒绝非单排。

## 135. 2026-09-14：常规双排失败仍尝试原尺寸旋转单排

- 常规宽度/统一刀位无解不能在整批旋转比较前直接退出。已启用整批旋转比较、双列模式且无明确手动方向时，尝试一个整批旋转单排候选；完整订单、双面相邻及颜色尺码顺序保留，每行一张，所有左刀码固定x=0，旋转区共用一个贯穿安全刀位。
- 本恢复不等比缩小、不修改原图、不自动改膜宽。只有所有源图均有可靠旋转占位，订单/刀位/标记检查通过才继续；非宽度错误不吞掉，旋转仍无解列无法容纳的文件。既有仅单列模式超宽缩小规则不变。
- 展示“超宽旋转恢复”步骤及采用方向/刀位，主界面异常与analysis记录原失败原因及采用原尺寸整批旋转；常规基准无解时采用当前长度作为基准，不虚构省膜数。源码0.1.176，2026-09-14当日第47次更新；源码推送，无新Release安装包。两引擎完整6图3双面测试验证563×234毫米原图旋转、源文件及RGBA不变、整高刀位透明安全区；此为测试批次，不代表截图网络目录生产验收。

## 136. 2026-09-14：独立报错区与一键复制

- 报错诊断独立于正常排版数据、进度、耗时、总结与普通警告区，主界面数据区下方红色分组显示完整可滚动详情，不截前8行。正常信息不被错误详情覆盖，进度只显示失败状态及报错区入口指引；预览警告不塞入整份原文件列表。
- 提供“一键复制报错信息”，直接复制完整原因、步骤、订单、路径、原图片参数及限制参数，不需要先打开窗口；保留放大查看与已有弹窗复制。单/多批、后台预览复用同一报错组件，新批次清空旧错误。复制不结束任务、不关闭报错区。
- 源码0.1.177，2026-09-14当日第48次更新；仅UI源码，无新Release安装包，不改变生产像素、排版或安全检查。

## 137. 2026-09-14：所有排版模式超宽先尝试旋转单排

- 用户明确所有模式都采用超宽恢复流程：自由、单列、双列常规宽度失败后，按完整订单尝试原尺寸旋转单排，不因关闭普通自动旋转、快速模式或整批省膜比较而跳过此必要恢复。已保存明确手动方向仍参与实际测量，不能仅因存在任何手动参数就整批跳过。
- 自由/单列恢复无第二刀位、不扣双列安全区；双列恢复仍保留统一旋转区刀位与透明安全通道。所有恢复每行一图、左刀码固定、订单/双面/颜色尺码不散，全部标记重新测量计入。
- 先旋转不缩小；仅明确单列切膜模式旋转仍无解时，才使用已启用的等比缩小恢复。自由/双列不会为了并排缩小。缺可靠DPI及非宽度错误不可猜值绕过，沿用独立可复制报错诊断。
- 源码0.1.178，2026-09-14当日第49次更新；源码推送，无新Release安装包。两引擎三模式各验证完整6图3双面、关闭普通自动旋转与比较仍恢复、原文件和RGBA保留、旋转每行一图及双列整高刀位安全；不是截图网络目录的实物验收。

## 138. 2026-09-14：自由排版旋转恢复刀码高度统一

- 自由模式旋转候选过去未设置外置间隔/抬高像素，但带膜标签的独立检查要求刀码顶端等于图顶减用户抬高量，因此真实标签图会失败。恢复测量使用单列标记坐标规则，最终仍保留自由模式和无第二刀位，不绕过或删除高度检查。
- 测试补完整膜标签卡片、平台名、普通标签和1.5毫米抬高，而非只用无标签实心图；覆盖两引擎三模式完整双面批次，源RGBA与固定左刀码、全高安全通道复核。测试显式关闭刀位点线，与GUI现行规则一致，不能把合法点线像素误判为通道侵入。
- 源码0.1.179，2026-09-14当日第50次更新；源码更新，无新Release安装包。截图网络源文件不在本机，测试通过不代表已在测试电脑完成实物验收。

## 139. 2026-09-14：批次结束色块按膜宽右边缘定位

- 用户明确结束红块在当前膜宽右下角，不在批次内容宽度右下角。按有效可打印膜宽定位：物理膜宽减RIIN左右预留；不重复添加预留。启用结束块时，单列、自由及双列输出画布保留该完整有效宽度，色块右边缘与其对齐，预览和输出用同一坐标。
- 不平移原图或左识别刀码，不改变纵向结束间距。分段仍只最后一段显示结束块，各段保留统一全幅坐标。关闭结束块仍沿用原内容宽度裁减规则；旧PNG不修改。
- 源码0.1.180，2026-09-14当日第51次更新；源码更新，无新Release安装包。两引擎三模式完整8图4双面三段复核源像素、结束块位置及双列整高刀位；预览矩形输入内容宽120仍按可打印580定位。

## 140. 2026-09-14：自定义膜规格与打印设置重新分组

- 膜规格下拉除45/60厘米预设，新增“自定义膜宽”；选中即显示厘米输入，可编辑5–500厘米（0.01厘米精度），内部继续毫米，扣RIIN预留后用于全部生成/预览和结束红块，不自动用最省规格替代用户选择。自定义数值与是否选用独立保存，切回预设不丢数值，重启恢复自定义及生产模式；自定义数值变化不重置已选生产模式。
- 设置按操作逻辑均衡：膜的设置含膜规格、自定义宽度、RIIN预留、刀位选择/距离/安全区/右侧基准偏移、膜规格比较；排版规则含处理/生产模式、间距留白、旋转方向/策略、超宽恢复和批次提示；刀码抬高归标签与文字。复用原控件和持久化，不在两个分类重复展示。
- 主界面高亮膜规格读取当前实际宽度，不能对自定义字符串做数值计算。普通用膜比较仍45/60，40–80研究规格仅开发者模式；自定义用于当前实际生产，不暗示设备一定支持该宽度。
- 源码0.1.181，2026-09-14当日第52次更新；源码更新，无新Release安装包。自定义52.5厘米→可打印505毫米、一次交互更新、保存重启、切预设后找回自定义及设置分类回归通过；不改订单、原像素及刀码安全规则。
